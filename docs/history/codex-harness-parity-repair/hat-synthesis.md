# Plan consult: Codex harness parity repair

## Availability

The Codex dispatch defect blocks the native advisor seats. The two herding seats prepared successfully, but their exact payload calls `.bee/bin/bee`. The isolated control clone has no installed vendored binary.

All five seats are recorded as dropped for this first slice. The reviewed audit supplies the verified facts. Run the full hat wave after `chpr-1` repairs Codex dispatch and before preparing the next slice.

## Leader check

The smallest valid slice has two disjoint changes. One fixes the worker door. One adds activity only where Codex already emits events.

No unsupported event enters the Codex projection. The full-suite and release requirements stay locked for the next slice.
