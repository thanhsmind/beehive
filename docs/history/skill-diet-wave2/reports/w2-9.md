# w2-9 — bee-evolving thin-body migration

**Status:** [DONE]
**Outcome:** `skills/bee-evolving/SKILL.md` migrated to the thin-body
doctrine: 8,828 → **7,952 bytes** (≤ 8,192). All seven decision-ID
parentheticals (`D3`, `D2b`, `D4`/`ff26725d`, `decision 0019`, `D5` ×2) exiled
into new `references/provenance.md` as a rule → decision-IDs → rationale
table; the body now states every rule bare. Added a compact flow-diagram
fence (`Guard -> Rank -> Gate A -> Fix -> Suites -> Gate B -> Push`) and a
References table row at the bottom pointing to `provenance.md`. Cut came from
compression only — merging redundant reassurance clauses (e.g. dropping
prose already covered by the Rationalization Table), tightening bullets to
one idea per line — never from dropping a rule. Grepped
`packages/bee/tests/test_misc.mjs` for `evolving`/`Gate A`/`Gate B`/
`mergeDigests`/`feedback rank`/`Iron Law`/`bee-writing-skills` before
trimming: zero pinned-phrase hits, so no phrase was under a census lock.
`scripts/skill-body-budget.json` untouched (MAIN's ledger update at the wave
barrier). No other skill's SKILL.md touched.

**Files:** `skills/bee-evolving/SKILL.md`,
`skills/bee-evolving/references/provenance.md` (new).

## Side-by-side excerpts

**s1 — HARD-GATE guard and refusal (byte-identical, unmoved):**

Before/after both read verbatim:
> ```bash
> test -f packages/bee/lib/feedback.mjs && test -f skills/bee-writing-skills/SKILL.md
> ```
> ...
> bee-evolving runs only in the bee repository. This repo is a bee *host*. I
> will not rank, patch, or "prepare" bee changes here — invoke me from the
> bee repo checkout.

**s2 — decision citation exiled, meaning unchanged:**

Before (`## 0. HARD-GATE` heading + body):
> ## 0. HARD-GATE — prove you are in the bee repo (D3)
> ...
> This skill is **invoked by the human, never triggered automatically**
> (D3), and is **never dispatched to an external CLI executor** (decision
> 0019: self-modifying work stays on native tiers where the orchestrator's
> goal-check applies).

After (bare rule in body; citation now in `provenance.md`):
> ## 0. HARD-GATE — prove you are in the bee repo
> ...
> Invoked by the human only, never triggered automatically, and never
> dispatched to an external CLI executor — self-modifying work stays on
> native tiers where the orchestrator's goal-check applies.

`references/provenance.md` maps both clauses: "Invoked by the human only…"
→ D3, "Never dispatched to an external CLI executor…" → decision 0019, each
with its one-line rationale — same facts, same authority, just relocated.

**s3 — Gate A rendering rule (compressed, not weakened):**

Before (~430 bytes):
> a representative **stored** title — copied byte-for-byte from a cluster
> entry's `title` field. Foreign titles are stored datamark-wrapped (`«…»`)
> and MUST be rendered still wrapped, exactly as stored. **The cluster `key`
> field is an internal clustering handle and never reaches a prompt or a
> rendering surface** — it is the datamark-*stripped* form, and rendering it
> would remove the very neutralization D2b applied. Render `title`, never
> `key`.

After (~250 bytes, same three facts — stored/byte-for-byte, wrapped-as-stored,
`key` forbidden and why):
> a representative **stored** `title`, copied byte-for-byte; foreign titles
> stay datamark-wrapped (`«…»`), exactly as stored. **Never render the
> cluster `key`** — it is the datamark-*stripped*, internal clustering
> handle; rendering it undoes the merge step's neutralization.

Full trace: `.bee/cells/w2-9.json`.
