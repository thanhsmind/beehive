# w2-8 — bee-writing-skills thin-body migration

**Status:** [DONE]
**Outcome:** `skills/bee-writing-skills/SKILL.md` migrated to the thin-body
doctrine: 8,958 → **5,157 bytes** (≤ 8,192). PHASE 1-4 collapsed into one
`## The Core Cycle` table (Phase | Action | Gate); the description-trap
example, dependency-metadata YAML, and persuasion-principle table moved to
new `references/checklist-examples.md`; the Rationalization Table moved to
new `references/rationalization-table.md`; a new `references/provenance.md`
maps the Iron Law, regrowth law, and per-turn-rules-never-exiled rule to
their decision IDs (5a1b3228, 4ae4f40b). No pinned phrases for this skill
found in `packages/bee/tests/test_misc.mjs` (grepped before trimming). Two
load-bearing checklist bullets — the regrowth law and the
per-turn-rules-never-exiled exception — kept verbatim in the body per cell
instruction (self-referential rules about this skill's own body-vs-reference
discipline; exiling them would be self-defeating). `pressure-test-template.md`
and `creation-log-template.md` untouched. `scripts/skill-body-budget.json`
untouched (MAIN's ledger update at the wave barrier). No other skill's
SKILL.md touched.

**Files:** `skills/bee-writing-skills/SKILL.md`,
`skills/bee-writing-skills/references/checklist-examples.md` (new),
`skills/bee-writing-skills/references/rationalization-table.md` (new),
`skills/bee-writing-skills/references/provenance.md` (new).

## Side-by-side excerpts

**s1 — Iron Law (byte-identical, unmoved):**

Before/after both read verbatim:
> **THE IRON LAW: NO SKILL WITHOUT A FAILING TEST FIRST.**
> Applies to edits too — no exceptions, not for "simple additions," "just
> a section," or "reference only."

**s2 — Regrowth law (kept in body, load-bearing per cell instruction):**

Before (`SKILL.md checklist` bullet):
> Regrowth law: a new learning lands in the knowledge bundle or
> `references/` by default; edit the body itself only for a load-bearing
> invariant, and only if the edit still fits the recorded budget in
> `scripts/skill-body-budget.json` — over budget, trim the body first
> (one in, one out) before adding

After (same bullet, unchanged meaning, still in body):
> **Regrowth law:** a new learning lands in the knowledge bundle or
> `references/` by default; edit the body itself only for a load-bearing
> invariant, and only if it still fits the recorded budget in
> `scripts/skill-body-budget.json` — over budget, trim the body first
> (one in, one out)

**s3 — Description trap example (exiled, pointer resolves):**

Before (inline in body, ~230 bytes with the yaml fence):
> **Description trap:** a workflow summary in the description makes Claude
> follow the description and skip the skill body. Every time.
> ```yaml
> # ❌ BAD
> description: Use when creating skills — run baseline test, ...
> ```

After (body slot, ~15 bytes plus pointer):
> Description-trap example: `references/checklist-examples.md`
> ("Description trap").

The pointer resolves to `## Description trap` in the new reference, which
carries the same before/after yaml block byte-identical.

Full trace: `.bee/cells/w2-8.json`.
