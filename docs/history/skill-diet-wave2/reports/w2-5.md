# w2-5 — bee-executing thin-body migration

**Status:** [DONE] (capped `--feature-verify-pending`; MAIN verifies at feature close)

**wc -c skills/bee-executing/SKILL.md → 8026** (budget ledger update deferred to MAIN at the wave barrier, unchanged here).

**Outcome:** `skills/bee-executing/SKILL.md` migrated to the thin-body
doctrine: 9225 -> 8026 bytes (<=8192). Steps 1-9 collapsed into one
Operating Contract table (bee-swarming/bee-validating pattern); deviation
rules, compaction, fresh-session handoff, headless, and red flags kept in
full meaning, condensed prose. All provenance citations (D1, main-verifies
D4, decision 0009, AO3/AO13, A2, A4, fresh-session-handoff D1, no-clear-stop
D1) exiled to new `skills/bee-executing/references/provenance.md`.
`references/worker-details.md` is untouched — both pointers the body cites
("Verify in full", "Advisor consult in full") still resolve to real
headings. Grepped `packages/bee/tests/test_misc.mjs` first: the only
census-pinned content tied to this skill is inside `worker-details.md`
(non-authoritative worktree identity language), which was not touched.

**Files:** `skills/bee-executing/SKILL.md`,
`skills/bee-executing/references/provenance.md` (new).

Full trace: `.bee/cells/w2-5.json`.

## Before/after excerpts (same meaning, different location/shape)

### 1. Validate-never-claim rule

Before (step 2, prose):

> **Validate, never claim (D1):** the orchestrator already claimed this cell
> (`cells claim --id` or `claim-next`) before spawning you. Confirm it:
> `node .bee/bin/bee.mjs cells show --id <id>` must show `status: "claimed"`
> with `trace.worker` matching your nickname. A worker never runs `cells
> claim` itself — anything else … is not yours to touch → return
> `[BLOCKED]` (or `[NOOP]` …), never claim it yourself to make the cell fit.

After (Operating Contract row 2, `D1` moved to provenance.md):

> **Validate, never claim:** the orchestrator already claimed it before
> spawning you — confirm `status: "claimed"` and `trace.worker` matches
> your nickname; anything else is not yours to touch → `[BLOCKED]`/`[NOOP]`,
> never claim it yourself.

Same confirmation command implied, same status/trace.worker check, same
NOOP/BLOCKED branching, same "never claim it yourself" prohibition.

### 2. Cap / behavior_change evidence rule

Before (step 7, decision cited inline):

> Classic path: cap only after a verify pass is recorded (`cells verify`);
> `behavior_change: true` cells add `--behavior-change --evidence-stdin`
> and pipe `verification_evidence` (tests inspected, tests added/changed,
> red-failure/before-state evidence, verification run) — never a written
> evidence file (decision 0009).

After (Operating Contract row 7, citation exiled):

> Classic: cap only after a recorded verify pass (`cells verify`);
> `behavior_change: true` cells add `--behavior-change --evidence-stdin`,
> piped, never a written evidence file.

`provenance.md` row: "`behavior_change: true` pipes `verification_evidence`
via `--evidence-stdin`, never a written evidence file | decision 0009 |
Evidence lives in one place — the cell trace — never duplicated on disk."
Same command flags, same never-a-file rule; decision id one hop away.

### 3. Advisor consult trigger

Before (step 6):

> High-risk/hard-gate cells require a recorded advisor consult before the
> execution gate — the CLI throws without a fresh `advisor_ref`
> (AO3/AO13; staleness is hash-and-decision-anchored, never a TTL).

After (Operating Contract row 6):

> High-risk/hard-gate cells require a recorded advisor consult before the
> execution gate — capping throws without a fresh `advisor_ref` (staleness
> is hash-and-decision-anchored, never a TTL).

Identical trigger condition and staleness rule; `AO3/AO13` moved to
`provenance.md`.
