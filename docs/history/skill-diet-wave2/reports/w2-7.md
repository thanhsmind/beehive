# w2-7 — bee-grooming thin-body migration

**Status:** [DONE] (capped `--feature-verify-pending`; MAIN verifies at feature close)
**Outcome:** `skills/bee-grooming/SKILL.md` migrated to the thin-body doctrine:
9185 -> 7025 bytes (<= 8192). No content was cut — detail already duplicated
in `references/grooming-reference.md` was compressed to pointers, and every
provenance citation (test-economy D4/D8, Delegation contract D2/D3,
decisions 0001/0002, decision 0014) was exiled into new
`skills/bee-grooming/references/provenance.md`. Zero provenance-regex matches
remain in the body.

**Files:** `skills/bee-grooming/SKILL.md`,
`skills/bee-grooming/references/provenance.md` (new).

Full trace: `.bee/cells/w2-7.json`.

## Before/after excerpts (meaning unchanged, location/wording compressed)

**s1 — hunt-list "stale, missing, or duplicated area truth" bullet**

Before (~700 chars, inline elaboration of the bundle/no-bundle branches and a
bare `decisions 0001/0002` citation):

> stale, missing, or duplicated area truth — **with a knowledge bundle**
> (`bundleMode`: ...) the mechanical proof is `node .bee/bin/bee.mjs
> knowledge check --json`: surface every OKF error, profile error, and
> profile warning it reports ... **With no bundle, this check is
> unchanged**: behavior changed after the spec's `updated` date; ... or two
> specs covering one surface — decisions 0001/0002; proposed sync/harvest/
> merge work routes through `bee-scribing`

After (one line, full elaboration already lived in
`references/grooming-reference.md` ("Hunt Checklists") so nothing was lost):

> stale, missing, or duplicated area truth — bundle-aware (`knowledge check
> --json`) or spec-frontmatter-based; areas with code but no concept/spec

The `decisions 0001/0002` citation for "two specs covering one surface" moved
to `references/provenance.md` verbatim (same decision IDs, same rule).

**s2 — test-prune hard gate**

Before:

> **Test-prune's hard gate (test-economy D4):** the action is merging
> near-duplicate cases into one table-driven test, or deleting a genuinely
> dead case — never a raw line-count cut. ... And because pruning removes
> coverage, the test-economy D8 negative-control principle still applies to
> what remains: the surviving case(s) must still demonstrably catch what the
> pruned duplicates caught — a quieter suite is not proof of a safe prune, a
> still-triggering guard is.

After:

> **Test-prune's hard gate:** the action is merging near-duplicate cases into
> one table-driven test, or deleting a genuinely dead case — never a raw
> line-count cut. Deleting a test changes guard *behavior*: every suite
> touched by the prune must run and show **green AFTER the prune, in the
> same batch** — proposed-now-verified-later is not ready to execute. The
> surviving case(s) must still demonstrably catch what the pruned duplicates
> caught. Citations: `references/provenance.md`.

Same hard-gate rule (merge-or-delete-only, green-after-prune-same-batch,
surviving-case-still-catches), citations relocated, meaning identical.

**s3 — execute-phase delegation line**

Before:

> §1 entropy inputs and §2's mechanical debt scans delegate as extraction/
> generation-tier I/O workers per the Delegation contract (D2/D3,
> `bee-hive/references/routing-and-contracts.md`) — dead-code proof stays
> generation; any other ad-hoc dispatch grooming makes while investigating a
> kill candidate defaults to the generation slot model, and ceiling requires
> the [bee-tier: ceiling] marker plus a one-line justification.

After:

> §1/§2 mechanical scans delegate as extraction/generation-tier I/O workers
> per the Delegation contract (`bee-hive/references/routing-and-contracts.md`);
> dead-code proof stays generation; any other ad-hoc dispatch defaults to the
> generation slot model, and ceiling requires the `[bee-tier: ceiling]`
> marker plus a one-line justification. Citations: `references/provenance.md`.

Same delegation rule and marker requirement; the `D2/D3` inline citation
moved to `references/provenance.md`.

## Notes

- Provenance exile check: `grep -E '\((D\d|AO\d|decision [0-9a-f]|hardening-\d|plan \d)' skills/bee-grooming/SKILL.md` — 0 matches (verified by hand-running the fence's `PROVENANCE_RE` against the new body).
- `skill_lint` pointer check: all three `references/grooming-reference.md ("...")` pointers in the new body (`"Entropy Computation"`, `"Hunt Checklists"`, `"Outcome Template"`) resolve to real headings in the unmodified reference file.
- `packages/bee/tests/test_misc.mjs` was grepped for pinned bee-grooming phrases before trimming — no hits (no census pin on this skill).
- `scripts/skill-body-budget.json` was NOT touched (shared ledger, MAIN's job at the wave barrier) — new size (7025) reported here and in this report's first line for MAIN to record.
- Did not touch mirror/onboard/manifest regen (wave-barrier, per cell's `regen_obligation_ack`).
