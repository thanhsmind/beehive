# Plan check

Date: 2026-09-17
Lane: standard
Scope: four owned files, with two product files and no hard-gate flag

The standard-lane inline rule applies. No advisor dispatch is required for this plan size.

## Facts and gaps

- The plan cites the current token equation, real extension harness, Pi lifecycle APIs, doctor embed, and release fingerprint.
- The existing harness can carry the needed branch and UI observations without a second harness.
- The never-throw registry test requires rows for each new event registration. The cell includes both rows.
- No unresolved implementation fact blocks execution.

Verdict: ready.

## Alternatives

- `setStatus` is smaller than a custom footer and preserves Pi defaults.
- Lifecycle events are smaller than polling and avoid background work.
- One cell is safer than splitting extension and contract changes across commits.
- Full provider/model keys prevent collisions that model-only keys can create.

Verdict: the plan uses the smallest honest shape.

## User impact

- The segment is absent until positive usage exists.
- Session restore and branch navigation cannot keep a stale branch total.
- Compact counts control footer width.
- A status error remains advisory and cannot interrupt the session.
- The verification map names the exact user-facing behavior and proof command.

Verdict: footer continuity and failure isolation are protected.

## Checklist

- Context decisions mapped: yes.
- Exact source anchors present: yes.
- Smaller path stated: yes.
- Rejected alternatives stated: yes.
- Risk-to-proof map complete: yes.
- Every configured role classified: yes.
- Current-slice cell packet complete: yes.
- Red-first bug or behavior proof required: yes.
- User-facing verification map included: yes.
- Execution can start after preview and gate approval: yes.
- Semantic goal-check has an approved review-role stage: yes.

Revision note: execution exposed the missing semantic-goal-check stage. The role plan now includes it without changing the cell packet.

Final verdict: READY.
