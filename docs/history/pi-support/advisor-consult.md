# Advisor consult — pi-support high-risk gate (dispatch f4be2431, fable, 2026-08-29)

CONSULT VERDICT: proceed-with-conditions —
1. pis-1 enumerates Pi 0.84.3's full built-in tool registry and records the unknown-tool default; pis-3 fixtures an unmapped tool name.
2. Decide and record per-call vs load-time `.bee` passivity; fixture the linked-worktree edge (extension in worktree, binary only at main root).
3. pis-4 marks pi herding non-production until pi-result-mailbox lands.
4. Model-guard on Pi binds to a real surface or a named exclusion — never a vacuous parity row.

Axis verdicts: (1) enforcement soundness red-flag — unmapped/custom tool names could leak a write without registry enumeration + recorded unknown-tool posture; (2) fail-closed integrity green-flag — directory-keyed passivity vs binary-keyed blocking is sound; per-call check requested; Pi's own subdirectory-session discovery gap recorded as outside this feature; (3) D7 split green-flag conditioned on the non-production caveat; (4) D8 once+delta green-flag — safety never rides the preamble.

All four conditions are folded into plan.md rev 2 (Approach hardenings 1-4, cells pis-1/pis-3/pis-4).
