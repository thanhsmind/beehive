# Pi parity review fixes — advisor digest

Five advisor views checked the locked context and gate-ready plan.

## Findings applied

- Defined the accepted runtime set and malformed-input fallback.
- Added direct normal and compact command-extraction proof.
- Defined Pi prompt nesting, unmatched events, and missing tool fields.
- Replaced the ambiguous race test with two explicit public behavior tests.
- Defined handoff refusal effects and the full active, closed, missing, unreadable, and C1 matrix.
- Named the compile-time Pi extension bytes used by doctor.
- Defined every Pi doctor row, status, overall verdict, and exit result.
- Made installed-sandbox proof mandatory. Unit harness evidence cannot replace it.

## Finding not applied as stated

Pi 0.84.4 does not emit `session_start` after compaction. The plan does not add a carrier Pi lacks.

Instead, session-init passes the selected runtime into its compact-capsule branch. Direct contract proof covers that branch.

## Result

The plan still resolves F1–F4 and keeps D1–D8 unchanged. No advisor required a product decision or scope reduction.
