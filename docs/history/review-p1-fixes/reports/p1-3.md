# p1-3 — F3 + F5: the feature-swap door and the close blast radius

**[DONE]**

Closed the feature-verify guard's second door (`state set --feature <other>` no
longer abandons pending caps), widened its phase condition from the literal
`swarming` to `SCRIBING_RUN_FROM`, and turned a failed active-feature
resolution in `workflows close` from a silent widening into a typed refusal.

Files touched:

- `packages/bee/bee.mjs`
- `packages/bee/tests/test_bee_cli.mjs`

Commit: `9681437e`

Full trace and evidence: `.bee/cells/p1-3.json`
