# p1-1 — F1+F2: the verify cache must never report green for checks it did not run

[DONE]

**Outcome:** the verify-cache suite no longer fails under `CI=true` (its fixture
was leaking the parent's cache-disabling env into every child), and three ways
the suite-result cache could serve a green result for work that never executed
are closed: `BEE_CHECK_ONLY` now disables the cache outright and says so in the
summary, suites may declare extra (non-import) input paths/globs that are hashed
into their cache key, and any suite that reads a live repo surface it has not
declared is opted out of caching by default.

**Files touched**

- `scripts/run_verify.mjs`
- `scripts/tests/test_verify_cache.mjs` (8 → 12 cases; no existing case weakened)

Full trace and evidence: `.bee/cells/p1-1.json`.
