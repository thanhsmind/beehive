# p2-3 — Re-anchor the owner-declaration census

**[DONE]**

File-target deviation: the cell's `files`/`read_first`/`verify` all named
`packages/bee/tests/test_misc.mjs`, but the described check and its
byte-identical repro ("expected five shipped routing state-set calls, got
[bee-exploring, bee-compounding]") live in
`packages/bee/tests/test_cli_state.mjs` — confirmed by running it before
touching anything. `test_misc.mjs` has no such check and was left
untouched.

Fixed in `packages/bee/tests/test_cli_state.mjs`: the census now
enumerates `skills/*/SKILL.md` + `skills/*/references/*.md` from the tree
(no hardcoded skill list), matches `state set` with the `node .bee/bin/bee.mjs`
prefix optional, and requires `--owner` inside the same match. Excludes
"never/don't hand-write" teaching examples and the generic placeholder-owner
syntax line in `routing-and-contracts.md`. Expected count updated 5 -> 4.

Mutation proof: removed `--owner compounding` from
`skills/bee-compounding/SKILL.md` in place — assertion went red (`expected
four ... got [...3 calls]`), then reverted (`git diff --stat` empty).

Verify: `node packages/bee/tests/test_cli_state.mjs` -> 120 passed, 0 failed.

Files: `packages/bee/tests/test_cli_state.mjs`. Commit: `76068b99`.
Full trace/evidence: `.bee/cells/p2-3.json`.
