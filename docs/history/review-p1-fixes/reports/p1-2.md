# p1-2 — pin BEE_CHECK_ONLY safety properties + invalid-regex refusal

[DONE]. Added `scripts/tests/test_check_filter.mjs`, a new standalone suite
that pins the 5 safety properties of `BEE_CHECK_ONLY` (unfiltered
byte-identical to pre-filter output, substring/regex filtering actually
gates check-body execution — not just the printed line, regex flags
honored, zero-match refuses non-zero, invalid regex refuses cleanly). Also
fixed the P3 the reviewer flagged: `checkOnlyPredicate` in
`scripts/lib/test-fixture.mjs` called `new RegExp(...)` unguarded at module
load, so a malformed filter crashed every importing suite with an uncaught
`SyntaxError` before any check ran — now a typed refusal, same shape as the
existing zero-match refusal.

Suite run: `6 passed, 0 failed`.

Files touched: `scripts/lib/test-fixture.mjs`, `scripts/tests/test_check_filter.mjs`.

Full trace/evidence: `.bee/cells/p1-2.json`.
