# p2-1 — Debt guards: guard every departure, and an unreadable store means DEBT

**[DONE]**

The reviewer's repro (cap `--feature-verify-pending` in `compounding` → `state set --phase compounding-complete` → `state start-feature --feature beta`) walked through both doors before the fix and is refused at the first door after it. The origin allowlist is gone: the guarded set is derived from where debt can exist (cells, which are phase-independent), so every phase departure asks, `startFeature` asks about the outgoing feature, and an unreadable cell store is a refusal naming the store rather than a silent pass.

Files touched:

- `packages/bee/lib/state.mjs` — the shared DEBT CORE (`readFeatureCellsStrict`, `isDebtGuardedDeparture`, `featureVerifyDebt`, `testCellDebt`, the FIX tails) + `startFeature`'s outgoing-feature door
- `packages/bee/bee.mjs` — both phase guards and the swap guard now own only their refusal head and call the shared core
- `packages/bee/tests/test_bee_cli.mjs` — coverage for the repro, every phase in `KNOWN_PHASES`, the scribing-run test-cell door from `reviewing`/`scribing`, start-feature over both debt kinds, unreadable store (file and directory), the missing-store and no-op-re-set exemptions, and `gate_bypass: "total"`
- `packages/bee/tests/test_cli_state.mjs` — **deviation**: one check asserted the old swarming-only scoping and was inverted

Full trace, evidence and deviations: `.bee/cells/p2-1.json`.
