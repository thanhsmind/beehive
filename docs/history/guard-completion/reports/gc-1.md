# gc-1 — One debt question, asked by every door

**[DONE]**

The feature-swap door asked only a subset of the unwaivable debt set, so a
capped `behavior` cell with no test cell walked out at EXIT 0 while the phase
door and start-feature refused the identical state at EXIT 1. `FEATURE_DEBT_KINDS`
+ `guardFeatureDebt` (lib/state.mjs) are now the single question; all four doors
(phase departure, scribing-run, feature swap, startFeature) supply only a
refusal head, and the test matrix is generated by iterating the kind registry.

Files touched:

- `packages/bee/lib/state.mjs` — the debt registry + the single door call
- `packages/bee/bee.mjs` — `guardPhaseDepartureDebt` / `guardFeatureSwapDebt`
- `packages/bee/tests/test_bee_cli.mjs` — door × kind matrix, fixture-coverage
  check, structural no-subset check, the reviewer's repro
- `packages/bee/tests/test_misc.mjs` — deviation: state.mjs's export allowlist
  is exact-set, so the two new exports had to be registered there

Full trace, evidence and deviations: `.bee/cells/gc-1.json`.
