# Codex parity plan consult

Five configured perspectives read this scope. Facts, alternatives and risks used native inherited sessions; value and user impact used configured agy-flash workers. Native model diversity was not observed.

## Accepted

- Test the installed manifest selection path. Handler-only tests cannot prove hooks receive native tools.
- Capture actual Codex hook names before choosing aliases. Preserve Claude behavior and Windows command rendering.
- Use one resolved dispatch policy for preparation and guard checks. Test model, effort, history and transport mismatches.
- A prompt cannot enforce read-only filesystem access. Use an explicit restricted CLI fallback where native dispatch has no supported isolation field.
- Resolve the current session transcript explicitly and normalize native usage without cumulative double counting.
- Source and onboarding catalogs diverge: source shared activity already exists, onboarding omits it.
- Readiness attestation is distinct from live evidence. Publish exact tested runtime limits.

## Dismissed or qualified

- No live evidence yet proves that every native patch bypasses hooks. Tool-name normalization must be measured in cpc-1.
- No evidence supports saying all plugin hooks fail or all Codex transcripts return nothing. The current fallback can select a Claude file without a session id.
- Audit-only child records do not establish trusted write authorization.
- Custom-agent sandbox configuration is not a usable native spawn field in this session. Do not invent a selector.
- Cost estimates from value are not measured. User-impact observations mostly came from the brief, so they are not independent source proof.

## Plan check

Four bounded cells cover all five assessed gaps and owning documentation. cpc-1 and cpc-3 have disjoint source files. cpc-2 consumes actual canary evidence; cpc-4 consumes both implementation outcomes. Cargo builds and final regeneration share resources and are serialized. No scope was removed. The CLI cell dry run currently refuses only because execution is not approved; retry after approval. The supported positional gate preview succeeded; the flag spelling refused.

## Evidence homes

Source references and the twelve-dimension acceptance matrix are in plan.md. External reports:
- .bee/mailbox/job-1789222680483-1059041-1/report-1.md
- .bee/mailbox/job-1789222681743-1059603-1/report-1.md
Native perspective results are in this session. The leader retains synthesis and gate decisions.

