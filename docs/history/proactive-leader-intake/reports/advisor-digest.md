# Advisor consult digest — proactive-leader-intake

Consult: hat-risks seat (fable, session-class) of the intake hat wave,
2026-08-30 — the wave absorbs the advisor consult per D4; full digest in
docs/history/proactive-leader-intake/hat-wave-intake.md.

Verdict carried into the plan: the feature is reversible only if
(1) the pre-Lock wave is absorbed with a recorded supersession and
revisit path — plan keeps the window (D5) and the decisions store
carries the supersession trail; (2) the trigger is per-feature, never
per-message — locked as D2; (3) wave output enters Lock only through a
human-answered question — locked as D1 synthesis law. Ceremony floor
(clear/tiny skip) locked as D2; headless collapse to Outstanding
Questions locked as D6. Undo path: prose-only cells, git revert plus
regen re-run; no data, no migration, no external surface.

Conditions all landed in CONTEXT.md D1-D7 and plan.md (rollback
section). No open advisor condition remains.
