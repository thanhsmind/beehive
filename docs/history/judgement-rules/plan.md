---
artifact_contract: bee-plan/v1
mode: standard
plan_rev: 2
approved_gate2: 2026-07-26 (auto, gate_bypass=total; rev 2 re-approved after plan-check absorbed 3 BLOCKERs + 4 CRITICALs — see reports/validation-slice1.md)
---

# judgement-rules — Critical rules 16 → 14, judgement form

## Mode-gate record

Risk flags counted: **2** — (1) changes behavior an existing test asserts (test_misc.mjs
censuses pin rule literals that must change: `>3 files`, rule-15 inline wait contract),
(2) the change replaces existing proof (census assertions rewritten in lockstep, with
negative controls). No hard-gate flag. Product files ~7 (template, 4 canonical skill
files, 2 test files; root AGENTS.md + 4 skill mirrors are generated projections — not
counted, D6). Story-sized sweep across doctrine + skills + tests → **standard**.
Smaller modes insufficient: >3 product files, and the census rewrite is exactly the
"weakening a guard vs correcting what is measured" seam that needs a validated plan.

## Discovery (L1 — repo evidence gathered this session)

- Canonical template: `packages/bee/AGENTS.block.md` (heading :34, rules :36-51), Red
  flags section at `AGENTS.block.md:90` / rendered root `AGENTS.md:97`. Root `AGENTS.md`
  is a generated byte-identical projection — edited never by hand, re-rendered via
  `node packages/bee/scripts/onboard_bee.mjs --repo-root . --apply`.
- Mirror sync: canonical `skills/` → `.claude-plugin/ + .codex-plugin/` via
  `node scripts/render_plugin_skill_trees.mjs`; → `.claude/skills/ + .agents/skills/`
  via the same onboard `--apply` run.
- Census map (from gather digest, this session):
  - `packages/bee/tests/test_misc.mjs` 2073-2100 (rule-13 literals incl. `>3 files`),
    2104-2148 (AO14 rider incl. `never zero *execution* workers` in the block),
    2151-2296 (rule-15 single-line regex + `assertOrderedWaitContract` + mutation
    self-test), 2298-2361 (handoff/etiquette literals), 2042 (`on user request:
    bee-reviewing`), 1972 (banned phrases), 2887-2925 (cli-gather census on BOTH
    AGENTS surfaces — literals `cli gather branch` AND `not an Agent dispatch`, both
    in the D2 keep list).
  - `scripts/tests/test_gate_bypass_doctrine.mjs:450-454` — rule-9 literal
    ``whether or not the lane produced a `plan.md` (D3/D4)`` (kept verbatim → no test
    edit needed there); 429-444 Working-files literals (`frozen at Gate 2`,
    `plan.md is opt-in`, `cell is the micro-plan`) — Working files section untouched.
  - `scripts/tests/test_doctrine_parity.mjs` — retired-sentence + hook-count census;
    Guardrails prose changes in exactly ONE spot — the `:81` cite "see critical rule 12"
    → 11 (no retired sentence, no hook-count prose touched, so the census stays green).
  - `scripts/tests/test_agents_budget.mjs` — 20 KiB budget + byte-parity template vs
    root block (shrinking helps it; re-render restores parity).
  - No third anchor census location exists — `test_lib.mjs` was retired in the cs-2a/2b
    monolith split (`fd test_lib` → only `scripts/tests/test_lib_mirror.mjs`, no
    AGENTS/rule anchors); verified during validation. Stale rule-number cites in
    comments/messages (`test_bee_cli.mjs:2819` 13→12, `test_guards.mjs:54` 5→4,
    `test_gate_bypass_doctrine.mjs:446,451,453` 9→8) are renumbered by jr-1.
- Locked doctrine honored: 9c01ac17 (fan-out lives in the always-loaded block — kept),
  placement-and-anchoring B3a (guard-required mechanics stay on the standing sheet:
  tier-marker transport stays inline), router-triage R5/R6 (every cut leaves a pointer;
  pointer chains terminate — rule 15 → routing-and-contracts.md, which is self-contained).

## Approach

Rewrite `## Critical rules` in `packages/bee/AGENTS.block.md` from 16 rules
(~1475 words) to 14 rules (~450 words), per D1-D4 (decisions 5f0147ff, 72a617dd,
d745a8e4, 32c99ea4):

- **Keep hard, near-verbatim:** old 1 (Gate 3), 4+5 merged (reservations +
  BEE_AGENT_NAME), 7 (decisions via CLI), 8 (commit per cell), 10 (agent runs
  machinery).
- **Shrink to invariant + pointer (interface already enforces):** old 2 (cap-proof),
  3 (orchestrator assigns), 14 (multi-session etiquette — keeps literals
  `Multi-session etiquette`, `names the holder`, `expiry`, `pick other`), 16 (CI gate).
- **Judgement form:** old 6 (drop `65%` → "pause cleanly before context runs out"),
  9 (one-sentence capture duty; keeps literal ``whether or not the lane produced a
  `plan.md` (D3/D4)``), 11 (one style sentence: work language + purpose-first),
  13 (drop `>3 files` numeric + narrative; KEEP anchors: title `Fan out the gathering;
  keep the deciding`, `digest, not verbatim`, `Decide-altitude never delegates`,
  `[bee-tier:` + `` `model` `` + `anchored`/`first`, `never zero *execution* workers`,
  `no skill is running`, cli-gather rider sentence).
- **Hybrid:** old 12 (keep "an unblocked write is not an approved write", cut the
  c2c46488 narrative — it lives in pattern docs).
- **Pointer:** old 15 → one **standalone unnumbered line directly under rule 14**
  (never a 15th numbered item — the must_have pins exactly 14) naming the ordered-wait
  rule and its home (`bee-hive` → `references/routing-and-contracts.md`); the full
  contract keeps its census on the reference + bee-swarming copies.
- **Delete** the `## Red flags — stop and re-route` section (D4). `docs/03-workflow.md`'s
  own "Red flags (chain-wide)" list is a dev-doctrine authoring doc, not the standing
  sheet — it stays.
- **Intra-block cite renumber (complete list):** rule 14's `as rule 12` → 11 and
  Guardrails `:81` `see critical rule 12` → 11. The two cites "Full rule: `bee-hive`
  skill, critical rule 10/11" reference bee-hive SKILL.md's OWN hive-law list
  (items 10/11 there) — they do NOT track AGENTS numbering and stay byte-unchanged.
- **Numbering authority:** the old→new map (1→1, 2→2, 3→3, 4+5→4, 6→5, 7→6, 8→7, 9→8,
  10→9, 11→10, 12→11, 13→12, 14→13, 16→14) is authoritative; jr-1 preserves relative
  order exactly and records the map in its cell trace — jr-2's sweep consumes that map.
- **Deferred to bee-scribing (same feature, not silently dropped):**
  `delegation-threshold.md` B3/R3 "more than three sources" numeric restatement;
  `doctrine-layer/overview.md:65` glossary "red-flag list" mention + `:10` rule-13 cite;
  `delegation-threshold.md:63`, `placement-and-anchoring.md:11` (13→12);
  `worktree-parallelism/routing-and-visibility.md:30` (14→13);
  `docs/specs/reading-map.md:156` (11→10). The `~65%` number's home becomes bee-hive
  hive law 2 (jr-2 rewords it to carry the number itself, pointing at new rule 5 for
  the principle).
- Handoff/startup literals (`planned-next`, `never auto-resume`, `bee state handoff
  adopt`, `fresh-session boundary`, …) live in the Startup section, untouched.

Tests move in lockstep, guard-intent intact (D2): the rule-13 census drops its
`/>3 files/` line and gains assertions for the new compressed anchors; the rule-15
census block reshapes to (a) assert the AGENTS pointer line exists and names the home,
(b) keep `assertOrderedWaitContract` running on `routing-and-contracts.md`,
`bee-swarming/SKILL.md`, `swarming-reference.md`; the mutation-resistance self-test is
updated to prove the reshaped census still bites (negative control — pattern 20260723).

Skill cross-refs renumber (old→new: 1→1, 6→5, 7→6, 8→7, 10→9, 12→11, 13→12) in
canonical `skills/bee-hive/SKILL.md`, `skills/bee-hive/references/routing-and-contracts.md`,
`skills/bee-validating/SKILL.md`, `skills/bee-compounding/SKILL.md`; bee-hive's Red
Flags section becomes standalone (keeps its four router-only flags, drops the pointer
to the deleted AGENTS section). Then re-render mirrors + root AGENTS.md.

**Rejected alternatives:** (a) rewrite without touching censuses — impossible, `>3
files` and the rule-15 single-line regex are pinned to current text; (b) move rule 15
to a brand-new reference file — routing-and-contracts.md already carries the full
census-checked contract, a new file would duplicate it (R6 risk, double maintenance);
(c) full 16→judgement including etiquette/handoff literals — breaks censuses for zero
word gain; those phrases are already minimal.

## Risk map

| Component | Risk | Proof needed |
|---|---|---|
| test_misc.mjs census rewrite | HIGH — widening-not-fixing trap | negative control: mutated fixture must still FAIL the reshaped census |
| AGENTS.block.md byte budget/parity | LOW | test_agents_budget after re-render |
| Skill renumber sweep completeness | MEDIUM — a missed `critical rule N` cite goes silently stale | `rg -n "critical rule" skills/` clean sweep + test_skill_pointers |
| test_lib.mjs unknown anchors | MEDIUM | grep before edit; update in lockstep if hit |
| Semantic loss in compression | MEDIUM | every kept anchor phrase enumerated in D2; goal-check judge per cell |

## Slices

**Slice 1 (current, only):**
- Cell jr-1: rewrite AGENTS.block.md rules + delete Red flags + census updates
  (test_misc.mjs, test_lib.mjs if hit) + re-render root AGENTS.md.
- Cell jr-2: skill cross-ref renumber + bee-hive Red Flags standalone + mirror
  re-render + pointer/parity suites green.

## Test matrix sketch (edge dimensions that bite here)

- **Text-anchor drift:** censuses green on BOTH template and rendered root.
- **Negative control:** reshaped censuses proven to still fail on a violating fixture.
- **Idempotency:** second onboard `--apply` run is a no-op (byte-idempotent install).
- **Cross-runtime parity:** 4 mirror roots byte-identical to canonical after render.
- **Budget:** AGENTS.md under the 20 KiB hard cap (shrink only helps).
