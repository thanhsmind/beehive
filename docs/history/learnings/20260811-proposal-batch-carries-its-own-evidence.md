# 2026-08-11 — A proposal batch is reviewable in one pass when each bullet carries its evidence

Batch: 9 promote proposals (finish-records-deviations, state-swap-hygiene,
evolving-rank-freshness, guard-heredoc-fencing, revision-deadlock-visibility,
island-read-filter, schedule-regen-awareness, timings-report, backlog-anchor).
Decision: ba138c93.

- Of 9 proposals only 2 carried anything the bundle did not already state
  (sra-2's CLI rendering; the backlog-row anchor arm). The scribing loop that
  syncs specs at feature close is keeping the bundle ahead of the mining —
  the proposal review is confirmation, not discovery. Cheap to verify,
  expensive to skip: the one real gap (sra-2) sat in the only proposal whose
  bullet named payload-level facts the area spec had only summarized.
- The frd-1 mining fix is confirmed live: deviation-derived pattern candidates
  appeared in exactly the 6 proposals whose capped traces carry structural
  `--deviation` values, zero in the 2 whose traces are clean.
- All 6 pattern candidates were declined — each was feature-specific or
  already merged into its area spec by the same-day scribe. A deviation line
  that names a moved refusal site or a widened file list is spec material,
  not a reusable pattern.
