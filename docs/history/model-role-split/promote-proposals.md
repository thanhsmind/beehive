promote proposal for work item "model-role-split" (docs/history/model-role-split/CONTEXT.md + docs/history/model-role-split/plan.md) — 34 capped cell(s): mrs-0, mrs-1, mrs-2, mrs-3, mrs-4, mrs-5, mrs-6, mrs-7, mrs-8, mrs-9, mrs-10, mrs-11, mrs-12, mrs-13, mrs-14, mrs-15, mrs-16, mrs-17, mrs-18, mrs-19, mrs-20, mrs-21, mrs-22, mrs-23, mrs-24, mrs-25, mrs-26, mrs-27, mrs-28, mrs-29, mrs-30, mrs-31, mrs-32, mrs-33
anchor: history — docs/history/model-role-split/CONTEXT.md, docs/history/model-role-split/plan.md
PROPOSAL ONLY — nothing was written. Applying any section below is a human or agent decision.

(a) DELIVERY DRAFT — save as docs/knowledge/work/model-role-split/delivery.md

---
type: bee.delivery
title: model-role-split — delivery
description: "Delivery record proposed by bee knowledge promote for work item model-role-split: 34 capped cell(s), 165 recorded deviation(s)."
timestamp: 2026-08-25
bee:
  id: model-role-split-delivery
  lifecycle: active
  areas: [doctrine-layer, workflow-state]
  required_context: [docs/history/model-role-split/CONTEXT.md, docs/history/model-role-split/plan.md]
  sources: [docs/history/model-role-split/CONTEXT.md, docs/history/model-role-split/plan.md, .bee/cells/mrs-0.json, .bee/cells/mrs-1.json, .bee/cells/mrs-2.json, .bee/cells/mrs-3.json, .bee/cells/mrs-4.json, .bee/cells/mrs-5.json, .bee/cells/mrs-6.json, .bee/cells/mrs-7.json, .bee/cells/mrs-8.json, .bee/cells/mrs-9.json, .bee/cells/mrs-10.json, .bee/cells/mrs-11.json, .bee/cells/mrs-12.json, .bee/cells/mrs-13.json, .bee/cells/mrs-14.json, .bee/cells/mrs-15.json, .bee/cells/mrs-16.json, .bee/cells/mrs-17.json, .bee/cells/mrs-18.json, .bee/cells/mrs-19.json, .bee/cells/mrs-20.json, .bee/cells/mrs-21.json, .bee/cells/mrs-22.json, .bee/cells/mrs-23.json, .bee/cells/mrs-24.json, .bee/cells/mrs-25.json, .bee/cells/mrs-26.json, .bee/cells/mrs-27.json, .bee/cells/mrs-28.json, .bee/cells/mrs-29.json, .bee/cells/mrs-30.json, .bee/cells/mrs-31.json, .bee/cells/mrs-32.json, .bee/cells/mrs-33.json]
---

# model-role-split — Delivery

## What shipped

- **mrs-0** — Lane rows now sort by feature id at the display builder; lanes_summary_vs_full green (2 file(s) changed)
- **mrs-1** — The model-guard's private models parser is deleted; verbs::drivers is now the one parser, called with an explicit purpose at every guard call site (1 file(s) changed)
- **mrs-2** — resolve_role walks an ordered role list; the unknown-name coercion to generation is gone and warns instead (2 file(s) changed)
- **mrs-3** — The model-guard asks whether a role is configured instead of matching four hardcoded names (1 file(s) changed)
- **mrs-4** — slot_for_kind resolves every dispatch kind through an explicit arm; an unmapped kind earns a typed refusal instead of the advisor slot (1 file(s) changed)
- **mrs-5** — Keyed the pinned agent map on roles, retired the phantom --kind remedy, and gave the one dispatch door --role so every rendered bee agent is reachable (5 file(s) changed)
- **mrs-6** — Agent files render from the role an agent declares, resolved through the one shared resolver (3 file(s) changed)
- **mrs-7** — Rewrote swarming-reference's read-only worker spawn instructions and Subagent-type table row to route through bee dispatch prepare --kind gather --role extraction; agent names appear only as what prepare returns (2 file(s) changed)
- **mrs-8** — bee cells add requires a non-empty role with a typed FIX line; membership never checked; 7 dispatched fixtures given roles, 159 never-resolved ones left roleless by rule (2 file(s) changed)
- **mrs-9** — A cell execution resolves the model from the role the cell declares, heading 561e1bda's ordered list with the historical tail intact (3 file(s) changed)
- **mrs-10** — A fresh .bee/config.json publishes code and read alongside the kept extraction/generation tail; review and advisor stay unshipped (1 file(s) changed)
- **mrs-11** — Added role to every cell payload example and stated D8's vocabulary as guidance; brought .bee/config-sample.json's models block in line with a fresh onboarding write (4 file(s) changed)
- **mrs-12** — The registry now declares bee herding pane, agent-start, pane-id and result; the registry side was the wrong one (2 file(s) changed)
- **mrs-13** — bee cells backfill-roles gives every stored cell a role: dry-runnable, idempotent to the byte, ceiling tiers untouched, declared in the registry (6 file(s) changed)
- **mrs-14** — ceiling became an escalation flag; flag, stored-record conversion and the 40% ration moved in one commit (9 file(s) changed)
- **mrs-15** — bee cells tier retired with its enum; its escalation half kept the ration as bee cells escalate, and the reason key moved with its surfaces (11 file(s) changed)
- **mrs-16** — Worker rows record the role their dispatch resolved; the closed three-value enum is gone from both worker doors, the persisted key is unchanged (2 file(s) changed)
- **mrs-17** — The mix counts roles with the escalation share beside it; both advice lines read the flag and count against the feature's cells; tier_mix renamed to role_mix as a stated public-contract decision; logical_tier deliberately left named (10 file(s) changed)
- **mrs-18** — Retarget the claim-unwind pin to a blank-role registration failure and prove it by mutation (1 file(s) changed)
- **mrs-19** — dispatch prepare publishes an explicit-only retry.fallbackChains contract with D11's error gate beside the model; no retry loop, and no chain at all when unconfigured (4 file(s) changed)
- **mrs-20** — The dispatch door publishes the host's configured roles and --role instead of a fixed tier list, and stops printing an effort no dispatch carries (4 file(s) changed)
- **mrs-21** — Rewrote the tier rubric as a role rubric across the swarming skill tree, README, the two handbook stage pages, the delegation transport bullet and register.md; regen wrote the mirrors (8 file(s) changed)
- **mrs-22** — Four tier-era surfaces now teach the role rubric; two trace-depth headings renamed off the retired word; down-tier left intact (5 file(s) changed)
- **mrs-23** — Retargeted the role fall-through tests onto host-only model values, added the codex variant and a fixture-integrity guard; mutation-checked red (1 file(s) changed)
- **mrs-24** — Narrowed the code/read warn exemption to a per-runtime pre-roles migration window, made the missing-role FIX text true, and collapsed resolve_advisor onto the one leaf parser (4 file(s) changed)
- **mrs-25** — Rewrote docs/config-reference.md onto roles, escalation and the publish-only chain, and gave bee cells add --help the role field and recommended vocabulary (2 file(s) changed)
- **mrs-26** — status reads the one models parser; the third copy and its closed four-slot list are gone (3 file(s) changed)
- **mrs-27** — bee status prints every configured role and validates every configured role (5 file(s) changed)
- **mrs-28** — Every shipped statement of the role fall-through rule now names mrs-24's pre-roles migration window, matching validate.rs (7 file(s) changed)
- **mrs-29** — One job now gets one rendered agent on any host vocabulary, and the chain gate is pinned on the published payload (2 file(s) changed)
- **mrs-30** — Narrowed the dispatch escalation arm with !from_role so a cell declared role ceiling can no longer take the session model uncounted; added the two-layer test binding the ration predicate to the dispatch channel (5 file(s) changed)
- **mrs-31** — backfill-roles re-reads under the lock and merges only its three owned keys, so a write that lands during the scan survives (2 file(s) changed)
- **mrs-32** — Refuse an unconfigured dispatch-door slot as a role name, and derive the two-agent pinned-type refusal from the agent table instead of the literal generation (2 file(s) changed)
- **mrs-33** — A null role slot now reads as off at every dispatch door, and both doors share one case-insensitive predicate (4 file(s) changed)

## Verify

Each cell below was capped only against a recorded passing verify result — bee refuses a cap without one.

- **mrs-0** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml status_full`
- **mrs-1** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-2** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-3** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-4** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-5** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-6** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-7** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml devtools && .bee/bin/bee dev release-manifest --check`
- **mrs-8** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-9** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-10** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml onboard`
- **mrs-11** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --manifest-path packages/bee-rs/Cargo.toml devtools && .bee/bin/bee dev release-manifest --check`
- **mrs-12** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml --test registry_dispatch`
- **mrs-13** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-14** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-15** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-16** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml state_group`
- **mrs-17** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-18** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml --bin bee -- verbs::drivers`
- **mrs-19** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-20** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml session_preamble`
- **mrs-21** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml devtools && .bee/bin/bee dev release-manifest --check`
- **mrs-22** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml devtools && .bee/bin/bee dev release-manifest --check`
- **mrs-23** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml --bin bee -- verbs::drivers`
- **mrs-24** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-25** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml --bin bee -- catalog:: hooks::cli_shape:: && .bee/bin/bee dev release-manifest --check`
- **mrs-26** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-27** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-28** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml devtools && .bee/bin/bee dev release-manifest --check`
- **mrs-29** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml --bin bee -- verbs::drivers`
- **mrs-30** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-31** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-32** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`
- **mrs-33** — `PATH="$HOME/.cargo/bin:$PATH" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml`

## Deviations

- **mrs-0** — The builder the cell placed in build.rs actually lives in verbs/status_full/topology.rs (build.rs only calls it); reserved topology.rs under mrs-w0 and fixed there — build.rs needed no change and was left untouched.
- **mrs-0** — Fixed the builder side, not the test: the rows are a display surface (bee status prints them, build_lane_summary derives its ids from them), so enumeration order was the defect. list_lanes keeps its documented Node-parity read order.
- **mrs-0** — Added a regression test (lane_rows_order_by_feature_id_not_by_directory_enumeration) with names whose creation order and sort order disagree, since the original test only caught this by filesystem luck. No assertion was deleted or weakened.
- **mrs-1** — PURPOSE CHOICE (a): all six guard call sites take GUARD_PURPOSE = "cell", the one non-gather purpose (purpose_is_gather is kind != "cell"), so a cli-shaped slot still resolves to Resolved::Refused exactly as the deleted purpose-blind parser refused it. Reason: the guard's whole surface is Agent/Task/spawn_agent, an in-family subagent that can no more BE an external cli process than it can be a herding pane. Per site: :725 marker+param, :818 marker-only, :882 pinned-type, :936 bare-FIX all reach a subagent verdict, so cell. configured_model_set is cell so a cli slot contributes no member, as today. derive_dispatch_economics (the ambiguous [bee-tier]-on-a-claude-Task site) is cell because the audit line must record the model the verdict above was reached on; it is behavior-free either way, since Resolved::Refused and Resolved::Cli both name no model.
- **mrs-1** — NATIVE KEPT OUT (b): configured_model_set matches Resolved::Model only, on both the slot loop and the advisor arm; Resolved::Native carries a readable model string in the drivers enum and a naive port would have widened membership. Commented in place as deliberate.
- **mrs-1** — Herding fallback now reads the resolved value (Resolved::Herding { fallback }) instead of the raw slot; the deleted enum's Herding was a unit variant, which is the only reason the old code peeked at the raw slot. Same review-falls-back-to-generation rule, applied inside the shared resolve_tier.
- **mrs-1** — Deleted more than the cell named literally: the guard's Slot/Slots/Models types and its normalize_tier_value/normalize_models went with resolve_tier/resolve_advisor. They ARE the second parser of the models.<runtime> shape, so leaving them would break must-have 1.
- **mrs-1** — packages/bee-rs/crates/bee/src/verbs/drivers/models.rs was reserved and read but needed NO change: it already holds the surviving purpose-parameterized implementation. A collapse deletes the duplicate; it does not edit the survivor.
- **mrs-1** — PRE-EXISTING RED for the orchestrator: verbs::status_full::tests::lanes_summary_vs_full asserts rows[0].feature == "alpha" and gets "beta" — an unsorted lane-directory read. Verified red at HEAD without this change. The file (verbs/status_full/) is not in this cell's scope, so it is a fix-first cell to triage, not a write I may make.
- **mrs-2** — Added packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs to scope and reserved it under mrs-w2 — the cell requires retargeted tests be named. Retargeted 2 assertions in resolve_tier_covers_every_documented_slot_shape: resolve_tier(advisor) no longer coerces to generation/sonnet (now Budget), and the cli refusal names slot generation (the slot actually read) instead of review (the slot asked for). Added resolve_role_walks_the_list_and_refuses_to_guess_an_unknown_name. No test deleted.
- **mrs-2** — MODEL_NORMALIZE_SLOTS deleted outright (nothing used it once normalize_models opened to every configured key). CONFIGURABLE_SLOTS kept, no longer a membership test — it survives as the default_models key list because prepare.rs still imports it; that sweep is mrs-4.
- **mrs-2** — The stderr warning fires for a name absent from BOTH models.<runtime> and default_models (the typo case). A present-but-null slot yields silently: warning there would print on every dispatch of an unconfigured runtime, where every codex/opencode slot ships null by default.
- **mrs-2** — The last-entry default_models floor is consulted ONLY for a key the table does not carry. An explicit null resolves to Budget: answering it with the built-in default resurrected the model the config had just cleared, and model_guard::cli_slot_and_empty_and_malformed_model_sets caught it red before this was fixed.
- **mrs-2** — resolve_tier is kept as the single-name spelling of resolve_role rather than left beside it, so the fix reaches the model-guard and prepare.rs now instead of becoming dead code until mrs-3/mrs-4 sweep their call sites.
- **mrs-3** — CLAUDE_TIERS/CODEX_TIERS retired: a [bee-tier: <role>] marker naming a role nothing configures now REFUSES by name (transports role-not-configured / codex-spawn-role-unconfigured) where it used to read as no marker and fall to the bare/unmarked deny — louder, per D2 "warned, never silently accepted"
- **mrs-3** — advisor is now a legal marker name on claude too (it was legal on codex only — the 4-vs-5 drift D2 names), because known_roles derives from slot_for_kind over DISPATCH_KINDS
- **mrs-3** — pinned_type_for now returns Option and the general-purpose repair is skipped for a role with no rendered agent: its old unwrap_or("undefined") was unreachable under four names and would have rewritten subagent_type to a non-existent type under an open set. PINNED_AGENT_TYPE and dispatch_kind_for_tier are untouched, as the cell scoped
- **mrs-3** — configured_model_set drops its separate resolve_advisor call: after mrs-2, resolve_tier reads the advisor slot directly and advisor is one of the walked config keys, so the second read was the same value
- **mrs-3** — No guard test needed retargeting or deleting — all 38 existing tests pass unchanged; 6 new tests cover the open set (derived known-role set, a role outside the old three slots as a member, any-configured-role marker, unconfigured-role refusal on both runtimes, no undefined repair)
- **mrs-3** — Pre-existing flake found, NOT caused here: doctor::tests::binary_freshness_* fails intermittently (3 identical runs of the same binary: green, green, red — a different test of the family each time). Unrelated to this cell; worth its own capture
- **mrs-4** — Tests added as an inline #[cfg(test)] mod slot_map_tests inside prepare.rs rather than drivers/tests.rs — tests.rs was not in the cell's files and a sibling worker holds the drivers module for mrs-2.
- **mrs-4** — Cap names a latent hazard closed, not a live bug fixed: kind is enum-gated against DISPATCH_KINDS at both doors, so the old catch-all was unreachable today.
- **mrs-4** — Full declared verify shows 4 pre-existing environment-limited reds in tests/opencode_plugin_contracts.rs; the scoped drivers proof is green.
- **mrs-4** — The worktree also carried a sibling worker's uncommitted models.rs (mrs-2); the commit used --only to stage prepare.rs alone, but the test run compiled against that in-flight edit.
- **mrs-5** — The a2f85972 herding-fallback member set does NOT widen by hand here: since mrs-3 it is a derived view of slot_for_kind over DISPATCH_KINDS, so it admits a slot the same day the door can reach it. --role does not widen slot_for_kind, so prepare still publishes only generation/review/advisor fallbacks and the guard stays exactly as wide as the door.
- **mrs-5** — Cell was blocked mid-flight on job 2 and widened by the coordinator (decision 8ff6e79e); the work therefore lands in TWO commits, b832e6b4 (jobs 1 and 3) and 64291f89 (the --role flag), instead of one
- **mrs-5** — docs/history/model-role-split/plan.md, named in read_first, does not exist; worked from CONTEXT.md, the store decisions and the cell action
- **mrs-5** — Added prepare_dispatch_with_role beside prepare_dispatch as an arity adapter (one line, no second implementation) so the ~50 existing call sites in verbs/drivers/tests.rs, a file this cell does not own, stay untouched
- **mrs-5** — Reserved and edited packages/bee-rs/crates/bee/src/generated/registry_payload.json, which the widened file list did not name: it is where a flag is declared, so the catalog entry the cell DOES name cannot be right without it
- **mrs-6** — plan.md is absent from the worktree; read the copy in the main checkout at docs/history/model-role-split/plan.md
- **mrs-6** — Edited onboard/apply.rs beyond the cell files, reserved first: it held the two call sites of the retired tier lookup, and leaving them would make the new resolver dead code
- **mrs-6** — Kept the AGENT_TIER_DEFAULTS_CLAUDE/_OPENCODE identifiers: renaming forces an edit in verbs/status_full/store.rs, and the identifier sweep belongs to the tier-retirement slice
- **mrs-6** — Kept the {{TIER_MODEL}} template token: renaming it rewrites packages/bee/agents/*.md.tmpl and the release-manifest-hashed trees, which the regen cell owns
- **mrs-6** — HOST-VISIBLE: a models.<rt>.<role> value of {kind:cli} with no command is junk to the shared parser, so the role default stands and the agent file is now rendered instead of removed
- **mrs-6** — HOST-VISIBLE: a models.<rt>.<role> value of {kind:native,model:x} now renders x; the retired local normalizer dropped the shape and shipped the default
- **mrs-6** — This repo .claude/agents and .opencode/agent trees are byte unchanged; the committed-projection pin test proves it
- **mrs-7** — gates-and-delegation.md stayed in scope but unedited: its subagent_type mention already reads as prepare-returns-only plus documented guard leniency (D2), never an instruction to hand-pick; editing it risked reinterpreting a locked decision outside the named contradiction at swarming-reference.md:104-114 and :294.
- **mrs-7** — sync-ack: gates-and-delegation.md (bee-hive skill) was in cell scope but deliberately left unedited: its subagent_type mention already reads as prepare-returns-only plus documented guard leniency (D2), not an instruction to hand-pick, so no edit was needed there — only bee-swarming's swarming-reference.md carried the named contradiction (lines 104-114 and 294)
- **mrs-8** — write_guard/tests.rs was reserved but left untouched: all 7 of its .bee/cells/ mentions are guard INPUT path strings (and one stage_file writing the literal x), never cell-shaped JSON, so it holds zero fixtures the role field can reach
- **mrs-8** — Fixture sweep scoped by ownership, not abandoned: nothing in production reads a cell role today (proved by grep — the cell record tier is read at prepare.rs:823/:1414/:1699 and status_full/cells.rs:584 only), so the fixtures that will resolve a role live in drivers/tests.rs, which is cell mrs-9 own declared file. Reported rather than reached into.
- **mrs-8** — D8 vocabulary shipped in the FIX line only; the help string and every authoring surface belong to mrs-11, which is open and names those files
- **mrs-8** — sync-ack: The authoring-surface half of D7/D8 is cell mrs-11's declared scope (skills/bee-planning/references/planning-reference.md, skills/bee-herding/references/dispatch-dry-run.md, docs/knowledge/areas/workflow-state/cells-authoring-and-revision.md); mrs-8's files list carries only the validator and its tests, so editing those skills here would write through another cell's scope.
- **mrs-9** — Reserved and edited packages/bee-rs/crates/bee/src/verbs/drivers/models.rs, a fourth file the cell did not list: BOTH warn fixes the cell offered live in that file (default_models, resolve_role/warn_unknown_role), so the cell mandated work there while listing only two files
- **mrs-9** — Rejected the cell hinted fix (a): mrs-10 claim that mid-list entries never consult default_models is FALSE in the path that matters — normalize_models seeds default_models into the very table resolve_role walks, so adding code/read there makes a MID-list code resolve and silently outrank every existing host generation model. Took (b), narrowed by provenance rather than by list position: head-only narrowing also fails, because on an existing host the head IS code for nearly every cell
- **mrs-9** — Changed the [bee-tier: …] marker, logical_tier, pinned_agent_type and the herding fallback lookup to name the role that RESOLVED rather than the role asked for. Not in the cell text, but required by its own must-have: model_guard classify_marker DENIES a marker naming a role nothing configures, so an unresolved code marker would refuse every cell dispatch on every host that has not opted in
- **mrs-9** — Left the pre-mrs-8 (role-less) cell path on its recorded tier, refusal included, so no legacy record changes model or answer; the cell body kept its ceiling handling
- **mrs-9** — The two warn properties are proven empirically, not only by predicate: running the fall-through test with --nocapture emits exactly one warn line, for the invented role design, and none for code
- **mrs-10** — Shipped 2 of the 4 names the plan S4 row listed (code, read, not review, advisor). Named reason: publishing and asking are different acts (561e1bda), and review/advisor already resolve without a config key, so a default for either is a product decision rather than a publishing one.
- **mrs-10** — mrs-6 left a note that D3 prepends its published names to AGENT_ROLES_BY_NAME. I did NOT prepend: bee-gather serves the gather dispatch at generation strength, so prepending read there would downgrade it haiku-ward, and that is the asking half call. AGENT_ROLES_BY_NAME is unchanged.
- **mrs-10** — REGEN OWED (mrs-11 or a docs cell): .bee/config-sample.json documents models.claude/codex with extraction, generation, review and advisor, and now lacks code and read. It is embedded via include_str! and no longer mirrors what onboarding writes. No rendered agent file changed, so no agent-tree regen is owed.
- **mrs-10** — FOLLOWUP for the asking half (S4): resolve_role warns on every dispatch for a name absent from both the host config and drivers::default_models. code and read are exactly that for EXISTING hosts, which a fresh-config default cannot fix. S4 must decide whether default_models gains code/read or the warn is narrowed.
- **mrs-10** — FOUND, not fixed: a fresh claude host reviewer dispatch resolves to sonnet, not opus, because mid-list entries never consult default_models, while bee-review rendered agent file is pinned to opus by AGENT_TIER_DEFAULTS_CLAUDE. The two paths already disagree today; shipping a review default would have hidden that, not fixed it.
- **mrs-10** — CAPTURE: a published config default is only worth shipping for a name the config file itself must carry for the tool own door to accept it. Every other asked-for name already resolves, and writing it turns a publishing act into a product decision.
- **mrs-11** — skills/bee-herding/references/dispatch-dry-run.md left untouched: it is a verbatim recorded proof transcript with no cell-payload example and no lane/role/tier field to update (its only lane mentions are PBI-classifier output, unrelated to the cell schema) -- confirmed by full-file search before leaving it out of the commit
- **mrs-11** — sync-ack: bee-herding predicted but not touched: skills/bee-herding/references/dispatch-dry-run.md was read and confirmed to hold no cell-payload example and no lane/role/tier field needing a change (a verbatim recorded proof transcript whose only lane mentions are PBI-classifier output) -- left untouched by named deviation, not an oversight
- **mrs-12** — router.rs was in the cell file list but needed no change: its PORTED list already named all four verbs, so only the registry was out of date
- **mrs-12** — registry_payload.json is not in the cell file list; reserved it before writing (it is where declarations live and is edited by hand, no generator remains)
- **mrs-12** — No regen chain was run: bee dev regen renders skill trees, onboard and the release manifest, none of which writes registry_payload.json
- **mrs-13** — verbs/cells/util.rs added to scope (reserved under mrs-w13 first): try_mutating dispatch table lives there, so the verb would be unreachable without one routing arm
- **mrs-13** — named the verb bee cells backfill-roles rather than migrate-roles: backfill is D9 own word and cannot be misread as routine maintenance
- **mrs-13** — archived cells (.bee/cells/archive/<feature>/) are migrated too, since D9 says the stored cells and readCell/unarchive reach them
- **mrs-13** — a tier outside the legal three is counted and named as unmapped rather than defaulted to code, so a not-fully-migrated store says so
- **mrs-13** — did not run the migration against this repo live store, per the cell — proof is unit-level over tempfile fixture stores
- **mrs-13** — sync door acked: no workflow-state owned skill teaches a one-time operator migration; S10 carries the author-facing role guidance
- **mrs-13** — sync-ack: No owned skill of workflow-state teaches this verb: bee cells backfill-roles is a one-time operator migration over stored records, not a step in the planning/swarming/reviewing/capturing loop, and the cell scoped no skill file. The feature plan puts author-facing role guidance and surface text in S10; teaching a migration an orchestrator runs once would be noise in a worker skill.
- **mrs-14** — Five files outside the cell list, each reserved under mrs-w14 first: handlers_write.rs (the conversion extends `cells backfill-roles` rather than adding a second migration verb — one pass, so no store can answer half flag and half tier), cells/tests.rs and drivers/tests.rs (probes), generated/registry_payload.json (the verb description had to follow its behaviour), docs/handbook/register.md (the refusal row named the old denominator).
- **mrs-14** — trace.tier_reason is NOT renamed. docs/handbook/register.md publishes the key and 20 stored records carry it; renaming it here without those surfaces is a migration half-done. The tier-retirement slice renames it with the field.
- **mrs-14** — The `tier` string is still written beside the flag and left on stored records. status_full/cells.rs and session_preamble/store.rs still count it for their advice lines and are outside this cell; dropping it here would blind two counters this cell cannot fix.
- **mrs-14** — The ration denominator moved from `cells that recorded a tier` to `the feature's cells`. Named, not silent: `role` is required now, so no new cell records a tier and a tier-shaped denominator would be 0 for every new feature — the refusal could never fire again. All five existing ration probes keep their exact arithmetic.
- **mrs-14** — One test retargeted, none deleted: resolve_role over [ceiling, generation] now falls through instead of ending the walk, because D5 makes ceiling not a role. The Inherit assertion moves to resolve_tier, where the marker callers live.
- **mrs-14** — Open for the tier-retirement slice: status_full/cells.rs and session_preamble/store.rs still key their advice off `tier`, so a cell authored with `escalate: true` and no tier is invisible to those two ADVICE lines. The enforcing door (the 40% refusal) reads the flag.
- **mrs-14** — sync-ack: No instruction in the area's skills went stale from this change. bee-swarming's reference teaches escalation through bee cells tier --tier ceiling and the anchored [bee-tier: ceiling] marker, and both still read and behave exactly as written — only what the verb WRITES moved onto the flag. The reference's remaining tier doctrine (resolveTier, the tier-judgement rubric, the per-tier dispatch table) is retired wholesale by the tier-retirement and surfaces slices; editing it piecemeal now would leave the instruction layer half-migrated while bee cells tier and the tier field both still exist.
- **mrs-15** — The tier verb's escalation half is RENAMED, not deleted: set_tier/run_tier become set_escalation/run_escalate behind `bee cells escalate --id <id> [--reason <text>] [--off]`. Deleting it outright would have deleted the only door that enforces D5's 40 percent ration - which D5 keeps unchanged in force - and would have forced deleting the ten probes that pin it. Named, not silent.
- **mrs-15** — cell_is_escalated still reads the legacy `tier: ceiling` string, DELIBERATELY. That read is what keeps the ration firing on a store nobody has migrated; it stays until every host has run the migration, and it is not this cell's to remove.
- **mrs-15** — Two files outside the cell list, each reserved under mrs-w15 first: verbs/cells/tests.rs (ten ration probes retargeted onto set_escalation, two probes added) and docs/handbook/register.md (it publishes trace.tier_reason, the cells verb list and the cell-field table - the rename is half-done without it).
- **mrs-15** — No test deleted to reach green. The three cli_shape tests that pinned cells.tier move onto backlog.pbi.status, its exact structural twin (two required fields, id first, an enum on the second); every assertion is the original one.
- **mrs-15** — --off is a new flag name: PINNED_FLAG_COUNT 182 -> 183, with the reuse candidates checked and each rejected by name in catalog.rs. Nothing in the vocabulary meant take this marking back off, and spelling the reversal --tier generation is exactly what D4 retires.
- **mrs-15** — The stored records are carried by `bee cells backfill-roles`, not by a hand-edit: the pass now renames trace.tier_reason to trace.escalation_reason wherever it finds it, idempotently, beside the role and the flag. It has still not been run against the live store (9 records there carry the key) - an operator run, unchanged from mrs-13.
- **mrs-15** — A stored record may keep carrying a tier string: D4 retires the field as a SELECTOR and does not order history rewritten. validate.rs now ignores the field entirely rather than refusing it.
- **mrs-15** — OPEN for the surfaces slice, flagged not touched: five skill trees, README.md and docs/handbook/stages/swarming.md + planning.md still teach `bee cells tier`. Outside this cell's files and reservation; mrs-14 already recorded this as the tier-retirement and surfaces slices shared residue.
- **mrs-15** — sync-ack: not: bee-swarming's SKILL.md and its swarming-reference still teach the whole tier doctrine — the extraction/generation/ceiling judgement rubric, resolveTier, the per-tier dispatch table — and inside it the now-dead spelling `bee cells tier --id <id> --tier <tier>` (SKILL.md step 2; swarming-reference.md around the "Record the choice" line). That spelling is a REAL residue as of this commit, not a false positive: the verb is gone and the sentence now points at nothing. It is not edited here because the fix is not a find-and-replace — the surrounding rubric has to become a ROLE rubric, which is the surfaces slice's design job (plan.md S10), and mrs-14 already recorded this same block as the tier-retirement + surfaces slices' shared residue. Editing only the one command line would leave the instruction layer teaching a cost-tier judgement that nothing in the binary implements any more, which is the worse half-migration. Also still stale for the same reason: README.md:360, docs/handbook/stages/swarming.md:51, docs/handbook/stages/planning.md:50. The one thing that changed here and is fully carried: `docs/handbook/register.md`, which publishes the verb list, the cell fields and `trace.tier_reason` — all three moved with the code in this commit.
- **mrs-16** — De-enum, NOT rename: the persisted key stays tier. Nothing in the tree reads workers[].tier — the cap door matches nickname plus cell alone — so a rename buys no behavior, while the flag spelling it would have to follow lives in catalog.rs, generated/registry_payload.json, cli_shape.rs and the cap-refusal FIX text, all sibling mrs-15 files.
- **mrs-16** — Removed the now-dead MODEL_TIERS const from verbs/state_group/mod.rs (outside the cell files, reserved under mrs-w16): the two checks this cell retired were its only users.
- **mrs-16** — Extracted one shared worker_role_value shape check rather than editing two copies of the same rule in add and update.
- **mrs-16** — verbs/state_group/tests.rs named by the cell needed no edit: its one tier reference is a node-shape fixture that stays exact with the key unchanged.
- **mrs-16** — Verify was blocked about 6 minutes by sibling mrs-15 mid-edit: 13 set_tier compile errors in verbs/cells/tests.rs, none in my files. Waited for their fix (13 to 9 to 0) rather than working around it.
- **mrs-16** — The untested-lines advisory misreads this commit: the three new tests live in workers.rs own cfg(test) mod tests, which is not a test-shaped path.
- **mrs-16** — sync-ack: No owned skill states anything this cell made untrue. The CLI flag spelling and the persisted record key are both unchanged, so the swarming reference line that spells out a manual worker registration stays exact; only the retired enum membership check changed, which no skill quoted. Re-wording that surface from tier to role is S10 work, in a file mrs-7 already capped.
- **mrs-17** — devtools/statusline.rs left untouched: the cell listed it for a `tiered` flag at :368-372, but that flag classifies prompt-cache ephemeral 5m/1h token buckets, not model tiers. The plan inventory misclassified it. No model-tier reference exists in that file.
- **mrs-17** — Reserved 4 paths beyond the cell's list, all consequences of the assigned change: status_full/mod.rs and session_preamble/mod.rs (their private MODEL_TIERS copies became dead and SCARCITY_MIN_TIERED had to become SCARCITY_MIN_CELLS), and status_full/tests.rs and session_preamble/tests.rs (the published-contract probe and the scarcity-wording assertions go red on the rename; updated to pin the new contract, plus 4 new probes).
- **mrs-17** — tier_mix -> role_mix is a clean rename, not a dual-emit. Reasoned at the emit site: no truthful content survives the split, so a retained key would hand every existing field reader a permanent silent zero, where a missing key fails loudly.
- **mrs-17** — logical_tier in drivers/guard.rs left named, per its FROZEN comment. Consumers named at the site: .bee/logs/dispatch.jsonl readers, hooks/model_guard.rs:994 (a second writer of the same key), and verbs/drivers/tests.rs. Only the parameter and the doc comment changed.
- **mrs-17** — Base red NOT mine and NOT fixed: verbs::drivers::tests::a_registration_failure_never_unwinds_the_standing_claim asserts an invalid tier refuses worker registration. Sibling commit 81e7a4b4 opened that enum, so "bogus" is now a legal role and the test is stale. The file is neither in this cell nor reserved by me — it belongs to the cell that landed 81e7a4b4.
- **mrs-18** — Mutation check needed a temporary edit to verbs/drivers/prepare.rs (not a cell file): reserved it under mrs-w18 first, made the failure path unclaim the cell, confirmed the test goes red, then git checkout -- reverted it to pristine. The committed diff touches tests.rs only.
- **mrs-18** — FINDING: the pre-existing assertions would NOT have caught a claim unwind. They read status off the Value returned by claim_and_reserve_for_dispatch, which is the pre-registration in-memory copy and still says claimed even when the store was unwound. Under the mutation the first assert passed and only the new on-disk read failed. The guarantee was nominally pinned but actually unpinned before this cell.
- **mrs-18** — Also corrected a stale doc comment on the sibling test dispatch_prepare_claim_payload_pins_worker_registered_true, which still named an invalid cell tier as the one deterministic registration failure.
- **mrs-19** — Chain published only when the payload actually names a model — an escalated, cli-exec or herding dispatch has nothing to fall from
- **mrs-19** — Byte-identity pinned by pre-change whole-payload goldens for gather/reviewer/advisor plus an add-exactly-one-key equality for the cell payload, whose prompt golden would churn on every worker-contract edit
- **mrs-20** — Effort resolution: STOP RENDERING it rather than qualify it. The block is injected into every session and re-injected after every compaction, so a qualifier is repeated length spent on a value nothing sends; the non-delivery is written down at the render site (render_role doc comment) and in decision fbbadad5 instead.
- **mrs-20** — The codex drop is recorded as its OWN fact, not folded into the claude harness reason: on claude every Resolved::Model site in prepare.rs destructures { model, .. } and the Agent tool takes no effort parameter (harness limit); on codex the spawn_agent arm emits neither model nor reasoning_effort even though the runtime accepts it and the native arm does emit it (bee's own gap, still open).
- **mrs-20** — Scope: two files beyond the cell, both reserved under mrs-w20 first. hooks/model_guard.rs holds tier_slot_display, the fixed-4-slot renderer that could not serve an open set — replaced by role_slot_display + dispatch_door_lines. hooks/compaction.rs carried a byte-copy of the same two lines and would otherwise have told a compacted session the list the preamble stopped saying; it now calls the one home.
- **mrs-20** — hooks/session_preamble/store.rs was reserved but needed no change — its D6 escalation-share work already landed.
- **mrs-20** — No regen needed: no skill, doc or generated artifact carries the old "Tier slots" text (rg over the tree, excluding target/). Regen stays with mrs-w21.
- **mrs-21** — skills/bee-swarming/references/worker-details.md:7 still lists model tier among the dispatch inputs and keeps its Trace Field Tiers By Lane heading — file not in this cell files, left for the orchestrator
- **mrs-21** — docs/04-skills-spec.md:143, docs/01-distillation.md:38 and :146, docs/knowledge/areas/doctrine-layer/delegation-threshold.md:83 still teach the three cost tiers — same reason, out of this cell declared files
- **mrs-21** — down-tier survives in gates-and-delegation.md:125 and :128 as delegation-altitude language, kept for parity with AGENTS.md rather than split vocabulary
- **mrs-21** — sync-ack: affects_skills named the two skills by name (bee-swarming, bee-hive) while the door matches path-shaped entries; both predicted skills WERE touched, at skills/bee-swarming/SKILL.md, skills/bee-swarming/references/swarming-reference.md and skills/bee-hive/references/gates-and-delegation.md, and the cell is claimed so cells update refuses the repair
- **mrs-22** — Renamed two headings that spelled trace DEPTH as tiers: worker-details.md Trace Field Tiers By Lane -> Trace Field Depth By Lane, and 01-distillation.md:118 Trace tiers by lane -> Trace depth by lane; both inside declared files, so the retired word stops carrying a second meaning
- **mrs-22** — Pointer parity inside a declared file: 04-skills-spec.md:81 trace field tiers -> trace field depth by lane, and :80 lane-tier trace -> lane-scaled trace depth, following the heading rename
- **mrs-22** — AMBIGUOUS, LEFT ALONE: 04-skills-spec.md:123 a weaker-tier (Codex / extraction/generation) run reads as model STRENGTH for a pressure re-run, and extraction/generation are still legal configured role names, so it is not the retired selector. Flagged, not edited
- **mrs-22** — OUT OF SCOPE, OBSERVED: docs/03-workflow.md:128 still says lane-tier trace (trace depth, not cost tier) — undeclared file, left for the orchestrator to triage
- **mrs-22** — down-tier as delegation altitude left standing in delegation-threshold.md and named there as the different axis; R6 tier marker respelled to the literal [bee-tier:] marker, which keeps its historical spelling
- **mrs-22** — SYNC_DOOR acked: the R3 rule block is byte-unchanged and its applied_at file gates-and-delegation.md already carries the role rubric from mrs-21
- **mrs-22** — sync-ack: The rule block itself (R3, doctrine-layer-delegation-threshold) is byte-unchanged; only R6 wording and the implementation pointers below it moved. Its applied_at file skills/bee-hive/references/gates-and-delegation.md already teaches the role rubric after mrs-21 rewrote it in c532502a, and it is outside this cell's declared files.
- **mrs-23** — Mutation applied to verbs/drivers/models.rs (outside the cell files) as a temporary working-copy break; reserved it first, reverted it, and git status confirms it carries no diff
- **mrs-23** — First mutation attempt (tail-only) was too broad and left the read path passing, because the read list falls through at index 1, not at the tail; narrowed it to any post-head step so all three tests bite
- **mrs-24** — Reserved and edited 2 files beyond the cell list: drivers/tests.rs (new window and advisor-parity tests) and cells/tests.rs (2 copies of the FIX string are pinned there byte-for-byte, so changing validate.rs alone is a red)
- **mrs-24** — Chose option (c) narrowing over (a) and (b): a bee process IS one dispatch, so a per-process warn still fires every dispatch; (b) needs doctor.rs, outside this cell files. Recorded with alternatives as store fef79243
- **mrs-24** — Left option (b) unbuilt: bee doctor should tell an un-migrated host to add the two keys. doctor.rs is outside this cell files and knows nothing of models config today — needs its own cell
- **mrs-24** — Same promise, uncorrected, in 3 owned-skill lines (skills/bee-planning/references/planning-reference.md:167,:215; skills/bee-swarming/references/swarming-reference.md:378), 2 docs (docs/knowledge/areas/workflow-state/cells-authoring-and-revision.md, docs/handbook/stages/planning.md) and .bee/config-sample.json. Capped with --sync-ack: the fix is a regen chain over 6 rendered trees and mrs-w25 holds the regen output reservation
- **mrs-24** — resolve_advisor collapse adopts resolve_configured cli leniency for a {kind:cli} value with no command string (Cli with empty command instead of None). Unreachable through read_models: normalize_tier_value drops that leaf, so the slot is absent
- **mrs-24** — sync-ack: The area's owned skills DO carry the same now-narrowed promise (skills/bee-planning/references/planning-reference.md:167,:215 and skills/bee-swarming/references/swarming-reference.md:378 all say an unconfigured role falls through and warns, with no exception), but that sentence is rendered into 6 skill trees plus .bee/config-sample.json and the fix is a bee dev regen chain — whose output tree is reserved by live sibling mrs-w25 (registry_payload.json) right now. Editing through that reservation is refused by contract, so the skill/doc sweep is handed back as its own cell rather than half-done here.
- **mrs-25** — Verified live behavior against packages/bee-rs/target/release/bee: the vendored .bee/bin/bee symlinks to the main checkout and predates --role, so it refuses the flag
- **mrs-25** — bee dev regen not run — release-manifest --check reports 270/270 match, so neither edited file is hashed by the manifest (mrs-12: registry_payload.json is hand-edited and regen does not write it)
- **mrs-25** — Also corrected role keys in the OpenCode and cli-executor examples, the bottom full sample, and the in-page anchor at line 19 — same page, same contradiction, all within the cell's named file
- **mrs-26** — Widened scope to status_full/mod.rs (deleted MODEL_NORMALIZE_SLOTS and EFFORT_LEVELS, dead once the copy went) and status_full/tests.rs (3 regression tests); both reserved under mrs-w26 first.
- **mrs-26** — Kept the opencode SEED in status: drivers::default_models leaves opencode null, but the drift check compares against agent files that pin AGENT_TIER_DEFAULTS_OPENCODE. It is a default, not a parser — same split onboard::agents already uses.
- **mrs-26** — Behaviour improvement not named by the cell: a composite {primary,fallback_policy,fallback} slot now resolves to its primary model in the drift check. The old copy read .get("model") off the composite and reported a correctly-rendered file as stale.
- **mrs-26** — Did not run a red-first demo of the bug: siblings mrs-w24/mrs-w25 are live in this worktree, so stashing to rebuild the old code was unsafe. The drop is a code-level fact — the old overlay walked MODEL_NORMALIZE_SLOTS and could not insert any other key.
- **mrs-27** — Bound justification, measured not asserted: the printed role list is capped by a 120-character WIDTH budget, not a role count, then +N more (--json). Measured on the built binary — 12 short-named claude roles cost 153 chars of role text, the same 12 on opencode cost ~380; a count cannot tell those apart. The first suffix spelling (+N more (bee status --json for all)) cost 35 chars to drop 35 chars of short entries, so it was shortened.
- **mrs-27** — Printed order changed and two existing assertions with it: order is now derived (slot_for_kind over DISPATCH_KINDS first, then map order), the same rule hooks::model_guard::role_slot_display uses. extraction reads third rather than second because no dispatch kind resolves it any more.
- **mrs-27** — Fourth defect fixed, same family, one axis over: render.rs printed claude and opencode by hand out of the three RUNTIMES status carries, so a configured models.codex was invisible. A codex line prints when configured; absent when not, so no existing host gains an all-null row.
- **mrs-27** — Reserved and edited one file outside the cell: status_full/build.rs, two call sites, because Problem.slot had to stop being &'static str for an open name set. Reserved under mrs-w27 before writing.
- **mrs-27** — ADVICE_CLASS_SLOTS (advisor/review) deliberately LEFT closed, with the reason in code: those are bee's own door names and read-only is a property of what bee does there, not of any name an operator may choose.
- **mrs-28** — Reserved the 15 regen-written mirror paths under .agents/, .claude/, .claude-plugin/, .codex-plugin/, .opencode/ before commit; all granted, none hand-edited
- **mrs-28** — Left .bee/decisions.jsonl and .bee/onboarding.json unstaged: a sibling store write and a regen timestamp, not this cell work
- **mrs-28** — Capped with --sync-ack: affects_skills predicted the two skills by name, the door wants reference paths; bee cells update refuses inside a granted worktree
- **mrs-28** — sync-ack: affects_skills predicted the two skills by NAME (bee-planning, bee-swarming); the touched paths are those same two skills spelled as reference paths, so the prediction is correct and the mirrors are in step (bee dev regen ran, release-manifest --check 270/270). The spelling fix needs bee cells update, which refuses inside a granted feature worktree and is unreachable from this isolated worker session.
- **mrs-29** — The audit's before-state was half wrong and the cap records the measured one: an un-migrated host does not return bee-extract for --role read, it refuses role_not_configured. The divergence is real either way (general-purpose vs a refusal) and the fix is the same.
- **mrs-29** — ROLE_AGENTS rows left unchanged rather than re-keyed: model_guard.rs and prepare.rs iterate the constant and depend on its rows being agent-unique, and both files are outside this cell. The job normalization lives in agent_for_role instead, which is also the honest split — role_for_agent's answer is resolved against models.<runtime> and must stay the name the agent was rendered from.
- **mrs-29** — CEILING ON THE CHAIN TEST, so the plan can be corrected rather than satisfied: bee publishes a chain and never walks one (51341f84), so no bee-side test can prove an executor obeys the gate. The new test pins what is provable — every published dispatch carries the gate correctly in both directions. The plan's claim that this test makes the feature "implemented" overstates it.
- **mrs-29** — --role read on a host that configures no read model is still refused by name (D2). Left as is deliberately and pinned in the test: there is no model to select, and answering an agent while selecting none is the silent-migration defect this feature removes.
- **mrs-29** — Verify ran ~4 min late: the shared crate was transiently red from sibling mrs-w27's in-flight status_full/tests.rs (their reservation). Polled until it compiled rather than writing through it; zero errors in my files at any point.
- **mrs-30** — Took fix (b) narrow prepare.rs over fix (a) reserve the name in validate.rs; recorded as decision 52ac81bf touching 97ce5225 — (a) puts a closed word back in D2 open set, inverts the existing D2-guard assertion in cells/tests.rs, and protects only cells authored after it lands while every stored role-ceiling cell keeps the bypass. The add-time assertion is therefore unchanged, deliberately, with the reason written beside it.
- **mrs-30** — Chose fix (b) narrow prepare.rs over fix (a) reserve the name in validate.rs; recorded as decision 52ac81bf touching 97ce5225. (a) would put a closed word back in D2 open role set, invert the existing D2-guard assertion, and leave every already-stored role-ceiling cell bypassing the ration. The add-time assertion in cells/tests.rs is unchanged on purpose, with the reason written beside it.
- **mrs-30** — New tests confirmed RED on pre-fix code before the fix landed: the_ration_and_the_dispatch_agree_on_which_cells_are_escalated failed at the c-role-ceiling row (ration says escalated=false, dispatch said true), and a_cell_role_of_ceiling_falls_through_the_resolver_but_the_flag_door_stays_open failed with model None instead of sonnet.
- **mrs-30** — Landed the commit through the guard-prescribed temp-index route (GIT_INDEX_FILE plus commit-tree plus update-ref) because the plain staging and commit verbs are refused while 2 workers are live in this checkout; the commit carries only my five files.
- **mrs-30** — Noted but NOT touched: verbs/drivers/guard.rs known_roles doc still says resolve_role answers ceiling with Resolved::Inherit, which the D5 resolver no longer does. That file is reserved by mrs-w32 and their in-flight diff already rewrites that same doc block.
- **mrs-30** — sync-ack: No skill edit is owed: skills/bee-swarming/references/swarming-reference.md:368 already publishes the correct rule — Escalation is not a role, running on the session model is the cell escalate FLAG. This cell fixes the CODE to match that published text; the skill was right and the dispatch was wrong, so nothing the skills teach changes.
- **mrs-31** — Shape 2 chosen over one-lock-spans-both: a lock held across a 40k-cell scan refuses every other writer for ~1s, and this migration must run against a live multi-session store
- **mrs-31** — Added a scan/lock seam (backfill_roles_interleaved) so the race is pinned deterministically instead of by sleep or thread timing
- **mrs-31** — Added report field changed_during_pass (JSON + text) to name records that moved under the pass instead of writing over them
- **mrs-31** — Verified both race tests fail on pre-fix code (whole-object write restored escalate=true on e-1 and recreated g-1) and that the lock test fails when the lock name is mutated
- **mrs-31** — Reset stale index entries (validate.rs, drivers/models.rs, drivers/prepare.rs, drivers/tests.rs, cells/tests.rs) that held a pre-sibling-commit revert; worktree content untouched
- **mrs-31** — Capped with --inline-reason: no worker row registered for mrs-w31 and state worker add refuses inside the worktree
- **mrs-31** — Capped with --sync-ack: no skill documents this verb's locking or report shape
- **mrs-31** — sync-ack: Concurrency fix inside cells backfill-roles' write half. No bee skill teaches this verb's locking or its report shape, and the operator-facing contract is unchanged: same flags, still idempotent, still re-runnable. The only new surface is one report line naming records another writer moved.
- **mrs-32** — known_roles now derives from the runtime table plus ceiling only; the slot_for_kind union is deleted rather than gated, because every gated insert would be a strict no-op against the table keys (default_models already seeds generation/review/extraction into every runtime)
- **mrs-32** — open_role_config() and codex_spawn_rules asserted the defect (an unconfigured advisor accepted on claude and on codex); both fixtures now state their precondition and the negative case moved to a named regression test
- **mrs-32** — cap ran with --inline-reason: the dispatcher never registered worker mrs-w32 in state.json workers[], and bee state worker add refuses inside a granted feature worktree
- **mrs-32** — follow-up outside this cell's files: onboard/templates.rs default_config table still reads reachable-with-no-config-key = yes for advisor, now false. Doc comment only; its conclusion (ship no advisor default) is unchanged.
- **mrs-33** — F1 fixed at the advisor slot (the one role with no Budget floor) rather than by refusing every null-valued key: a blanket non-null rule would empty known_roles on codex, whose default_models seeds every slot null, and would contradict .bee/config-sample.json ("A missing/null slot = prompt-budget"). The null-versus-absent rule is still made once, in role_is_declarable, which asks resolve_advisor instead of re-testing the value.
- **mrs-33** — F3 fixed by making the comment TRUE rather than rewriting it: prepare now asks known_role_named (case-insensitive, answering in the config spelling) and rebinds role to that spelling, so --role Advisor still reaches the advisor resolver.
- **mrs-33** — F4 also corrected the extraction row of the templates.rs decision table, which claimed extraction was unreachable without a config key; default_models seeds it on every runtime. The shipping call is unchanged, only its stated reason.
- **mrs-33** — The dispatcher never registered this worker in state.json workers[], so the cap refused; registered it from the main checkout (bee state worker add --nickname mrs-w33 --cell mrs-33 --tier generation) rather than capping with a false inline reason.

## Provenance

Proposed by `bee knowledge promote --work model-role-split` from 34 capped cell trace(s) in `.bee/cells/` and the anchor `docs/history/model-role-split/CONTEXT.md`, `docs/history/model-role-split/plan.md`. Every line above is copied from a trace or from the work item; nothing here is curated truth until a human or agent accepts it.

(b) AREA UPDATES — candidate spec-sync bullets, each citing its cell

areas: from the scribing stamp for "model-role-split" — .bee/logs/scribing-runs.jsonl's most recent entry (2026-08-25T11:47:19.372Z), the work item declares no bee.areas.

area doctrine-layer:
  - [mrs-0] Lane rows now sort by feature id at the display builder; lanes_summary_vs_full green — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-0.json)
  - [mrs-2] resolve_role walks an ordered role list; the unknown-name coercion to generation is gone and warns instead — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-2.json)
  - [mrs-3] The model-guard asks whether a role is configured instead of matching four hardcoded names — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-3.json)
  - [mrs-4] slot_for_kind resolves every dispatch kind through an explicit arm; an unmapped kind earns a typed refusal instead of the advisor slot — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-4.json)
  - [mrs-5] Keyed the pinned agent map on roles, retired the phantom --kind remedy, and gave the one dispatch door --role so every rendered bee agent is reachable — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-5.json)
  - [mrs-6] Agent files render from the role an agent declares, resolved through the one shared resolver — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-6.json)
  - [mrs-8] bee cells add requires a non-empty role with a typed FIX line; membership never checked; 7 dispatched fixtures given roles, 159 never-resolved ones left roleless by rule — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-8.json)
  - [mrs-9] A cell execution resolves the model from the role the cell declares, heading 561e1bda's ordered list with the historical tail intact — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-9.json)
  - [mrs-10] A fresh .bee/config.json publishes code and read alongside the kept extraction/generation tail; review and advisor stay unshipped — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-10.json)
  - [mrs-12] The registry now declares bee herding pane, agent-start, pane-id and result; the registry side was the wrong one — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-12.json)
  - [mrs-13] bee cells backfill-roles gives every stored cell a role: dry-runnable, idempotent to the byte, ceiling tiers untouched, declared in the registry — feature-wide sync per the scribing stamp, 6 file(s) changed (trace .bee/cells/mrs-13.json)
  - [mrs-14] ceiling became an escalation flag; flag, stored-record conversion and the 40% ration moved in one commit — feature-wide sync per the scribing stamp, 9 file(s) changed (trace .bee/cells/mrs-14.json)
  - [mrs-15] bee cells tier retired with its enum; its escalation half kept the ration as bee cells escalate, and the reason key moved with its surfaces — feature-wide sync per the scribing stamp, 11 file(s) changed (trace .bee/cells/mrs-15.json)
  - [mrs-16] Worker rows record the role their dispatch resolved; the closed three-value enum is gone from both worker doors, the persisted key is unchanged — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-16.json)
  - [mrs-17] The mix counts roles with the escalation share beside it; both advice lines read the flag and count against the feature's cells; tier_mix renamed to role_mix as a stated public-contract decision; logical_tier deliberately left named — feature-wide sync per the scribing stamp, 10 file(s) changed (trace .bee/cells/mrs-17.json)
  - [mrs-19] dispatch prepare publishes an explicit-only retry.fallbackChains contract with D11's error gate beside the model; no retry loop, and no chain at all when unconfigured — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-19.json)
  - [mrs-20] The dispatch door publishes the host's configured roles and --role instead of a fixed tier list, and stops printing an effort no dispatch carries — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-20.json)
  - [mrs-24] Narrowed the code/read warn exemption to a per-runtime pre-roles migration window, made the missing-role FIX text true, and collapsed resolve_advisor onto the one leaf parser — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-24.json)
  - [mrs-26] status reads the one models parser; the third copy and its closed four-slot list are gone — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-26.json)
  - [mrs-27] bee status prints every configured role and validates every configured role — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-27.json)
  - [mrs-29] One job now gets one rendered agent on any host vocabulary, and the chain gate is pinned on the published payload — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-29.json)
  - [mrs-30] Narrowed the dispatch escalation arm with !from_role so a cell declared role ceiling can no longer take the session model uncounted; added the two-layer test binding the ration predicate to the dispatch channel — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-30.json)
  - [mrs-31] backfill-roles re-reads under the lock and merges only its three owned keys, so a write that lands during the scan survives — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-31.json)
  - [mrs-32] Refuse an unconfigured dispatch-door slot as a role name, and derive the two-agent pinned-type refusal from the agent table instead of the literal generation — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-32.json)
  - [mrs-33] A null role slot now reads as off at every dispatch door, and both doors share one case-insensitive predicate — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-33.json)

area workflow-state:
  - [mrs-0] Lane rows now sort by feature id at the display builder; lanes_summary_vs_full green — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-0.json)
  - [mrs-2] resolve_role walks an ordered role list; the unknown-name coercion to generation is gone and warns instead — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-2.json)
  - [mrs-3] The model-guard asks whether a role is configured instead of matching four hardcoded names — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-3.json)
  - [mrs-4] slot_for_kind resolves every dispatch kind through an explicit arm; an unmapped kind earns a typed refusal instead of the advisor slot — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-4.json)
  - [mrs-5] Keyed the pinned agent map on roles, retired the phantom --kind remedy, and gave the one dispatch door --role so every rendered bee agent is reachable — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-5.json)
  - [mrs-6] Agent files render from the role an agent declares, resolved through the one shared resolver — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-6.json)
  - [mrs-8] bee cells add requires a non-empty role with a typed FIX line; membership never checked; 7 dispatched fixtures given roles, 159 never-resolved ones left roleless by rule — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-8.json)
  - [mrs-9] A cell execution resolves the model from the role the cell declares, heading 561e1bda's ordered list with the historical tail intact — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-9.json)
  - [mrs-10] A fresh .bee/config.json publishes code and read alongside the kept extraction/generation tail; review and advisor stay unshipped — feature-wide sync per the scribing stamp, 1 file(s) changed (trace .bee/cells/mrs-10.json)
  - [mrs-12] The registry now declares bee herding pane, agent-start, pane-id and result; the registry side was the wrong one — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-12.json)
  - [mrs-13] bee cells backfill-roles gives every stored cell a role: dry-runnable, idempotent to the byte, ceiling tiers untouched, declared in the registry — feature-wide sync per the scribing stamp, 6 file(s) changed (trace .bee/cells/mrs-13.json)
  - [mrs-14] ceiling became an escalation flag; flag, stored-record conversion and the 40% ration moved in one commit — feature-wide sync per the scribing stamp, 9 file(s) changed (trace .bee/cells/mrs-14.json)
  - [mrs-15] bee cells tier retired with its enum; its escalation half kept the ration as bee cells escalate, and the reason key moved with its surfaces — feature-wide sync per the scribing stamp, 11 file(s) changed (trace .bee/cells/mrs-15.json)
  - [mrs-16] Worker rows record the role their dispatch resolved; the closed three-value enum is gone from both worker doors, the persisted key is unchanged — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-16.json)
  - [mrs-17] The mix counts roles with the escalation share beside it; both advice lines read the flag and count against the feature's cells; tier_mix renamed to role_mix as a stated public-contract decision; logical_tier deliberately left named — feature-wide sync per the scribing stamp, 10 file(s) changed (trace .bee/cells/mrs-17.json)
  - [mrs-19] dispatch prepare publishes an explicit-only retry.fallbackChains contract with D11's error gate beside the model; no retry loop, and no chain at all when unconfigured — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-19.json)
  - [mrs-20] The dispatch door publishes the host's configured roles and --role instead of a fixed tier list, and stops printing an effort no dispatch carries — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-20.json)
  - [mrs-24] Narrowed the code/read warn exemption to a per-runtime pre-roles migration window, made the missing-role FIX text true, and collapsed resolve_advisor onto the one leaf parser — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-24.json)
  - [mrs-26] status reads the one models parser; the third copy and its closed four-slot list are gone — feature-wide sync per the scribing stamp, 3 file(s) changed (trace .bee/cells/mrs-26.json)
  - [mrs-27] bee status prints every configured role and validates every configured role — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-27.json)
  - [mrs-29] One job now gets one rendered agent on any host vocabulary, and the chain gate is pinned on the published payload — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-29.json)
  - [mrs-30] Narrowed the dispatch escalation arm with !from_role so a cell declared role ceiling can no longer take the session model uncounted; added the two-layer test binding the ration predicate to the dispatch channel — feature-wide sync per the scribing stamp, 5 file(s) changed (trace .bee/cells/mrs-30.json)
  - [mrs-31] backfill-roles re-reads under the lock and merges only its three owned keys, so a write that lands during the scan survives — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-31.json)
  - [mrs-32] Refuse an unconfigured dispatch-door slot as a role name, and derive the two-agent pinned-type refusal from the agent table instead of the literal generation — feature-wide sync per the scribing stamp, 2 file(s) changed (trace .bee/cells/mrs-32.json)
  - [mrs-33] A null role slot now reads as off at every dispatch door, and both doors share one case-insensitive predicate — feature-wide sync per the scribing stamp, 4 file(s) changed (trace .bee/cells/mrs-33.json)

(c) PATTERN CANDIDATES — candidate bee.pattern concepts, bee.polarity pitfall

from cell mrs-0 — save as docs/knowledge/patterns/model-role-split-mrs-0-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-0 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-0's capped trace: The builder the cell placed in build.rs actually lives in verbs/status_full/topology.rs (build.rs only calls it); reserved topology.rs under mrs-w0 and fixed t…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-0-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-0.json]
  polarity: pitfall
---

# model-role-split cell mrs-0 — pitfall candidate

## What the cell did

Lane rows now sort by feature id at the display builder; lanes_summary_vs_full green

## Recorded evidence (verbatim from .bee/cells/mrs-0.json)

- **deviation** — The builder the cell placed in build.rs actually lives in verbs/status_full/topology.rs (build.rs only calls it); reserved topology.rs under mrs-w0 and fixed there — build.rs needed no change and was left untouched.
- **deviation** — Fixed the builder side, not the test: the rows are a display surface (bee status prints them, build_lane_summary derives its ids from them), so enumeration order was the defect. list_lanes keeps its documented Node-parity read order.
- **deviation** — Added a regression test (lane_rows_order_by_feature_id_not_by_directory_enumeration) with names whose creation order and sort order disagree, since the original test only caught this by filesystem luck. No assertion was deleted or weakened.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-1 — save as docs/knowledge/patterns/model-role-split-mrs-1-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-1 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-1's capped trace: PURPOSE CHOICE (a): all six guard call sites take GUARD_PURPOSE = \"cell\", the one non-gather purpose (purpose_is_gather is kind != \"cell\"), so a cli-shaped slo…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-1-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-1.json]
  polarity: pitfall
---

# model-role-split cell mrs-1 — pitfall candidate

## What the cell did

The model-guard's private models parser is deleted; verbs::drivers is now the one parser, called with an explicit purpose at every guard call site

## Recorded evidence (verbatim from .bee/cells/mrs-1.json)

- **deviation** — PURPOSE CHOICE (a): all six guard call sites take GUARD_PURPOSE = "cell", the one non-gather purpose (purpose_is_gather is kind != "cell"), so a cli-shaped slot still resolves to Resolved::Refused exactly as the deleted purpose-blind parser refused it. Reason: the guard's whole surface is Agent/Task/spawn_agent, an in-family subagent that can no more BE an external cli process than it can be a herding pane. Per site: :725 marker+param, :818 marker-only, :882 pinned-type, :936 bare-FIX all reach a subagent verdict, so cell. configured_model_set is cell so a cli slot contributes no member, as today. derive_dispatch_economics (the ambiguous [bee-tier]-on-a-claude-Task site) is cell because the audit line must record the model the verdict above was reached on; it is behavior-free either way, since Resolved::Refused and Resolved::Cli both name no model.
- **deviation** — NATIVE KEPT OUT (b): configured_model_set matches Resolved::Model only, on both the slot loop and the advisor arm; Resolved::Native carries a readable model string in the drivers enum and a naive port would have widened membership. Commented in place as deliberate.
- **deviation** — Herding fallback now reads the resolved value (Resolved::Herding { fallback }) instead of the raw slot; the deleted enum's Herding was a unit variant, which is the only reason the old code peeked at the raw slot. Same review-falls-back-to-generation rule, applied inside the shared resolve_tier.
- **deviation** — Deleted more than the cell named literally: the guard's Slot/Slots/Models types and its normalize_tier_value/normalize_models went with resolve_tier/resolve_advisor. They ARE the second parser of the models.<runtime> shape, so leaving them would break must-have 1.
- **deviation** — packages/bee-rs/crates/bee/src/verbs/drivers/models.rs was reserved and read but needed NO change: it already holds the surviving purpose-parameterized implementation. A collapse deletes the duplicate; it does not edit the survivor.
- **deviation** — PRE-EXISTING RED for the orchestrator: verbs::status_full::tests::lanes_summary_vs_full asserts rows[0].feature == "alpha" and gets "beta" — an unsorted lane-directory read. Verified red at HEAD without this change. The file (verbs/status_full/) is not in this cell's scope, so it is a fix-first cell to triage, not a write I may make.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-2 — save as docs/knowledge/patterns/model-role-split-mrs-2-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-2 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-2's capped trace: Added packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs to scope and reserved it under mrs-w2 — the cell requires retargeted tests be named. Retargeted 2 a…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-2-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-2.json]
  polarity: pitfall
---

# model-role-split cell mrs-2 — pitfall candidate

## What the cell did

resolve_role walks an ordered role list; the unknown-name coercion to generation is gone and warns instead

## Recorded evidence (verbatim from .bee/cells/mrs-2.json)

- **deviation** — Added packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs to scope and reserved it under mrs-w2 — the cell requires retargeted tests be named. Retargeted 2 assertions in resolve_tier_covers_every_documented_slot_shape: resolve_tier(advisor) no longer coerces to generation/sonnet (now Budget), and the cli refusal names slot generation (the slot actually read) instead of review (the slot asked for). Added resolve_role_walks_the_list_and_refuses_to_guess_an_unknown_name. No test deleted.
- **deviation** — MODEL_NORMALIZE_SLOTS deleted outright (nothing used it once normalize_models opened to every configured key). CONFIGURABLE_SLOTS kept, no longer a membership test — it survives as the default_models key list because prepare.rs still imports it; that sweep is mrs-4.
- **deviation** — The stderr warning fires for a name absent from BOTH models.<runtime> and default_models (the typo case). A present-but-null slot yields silently: warning there would print on every dispatch of an unconfigured runtime, where every codex/opencode slot ships null by default.
- **deviation** — The last-entry default_models floor is consulted ONLY for a key the table does not carry. An explicit null resolves to Budget: answering it with the built-in default resurrected the model the config had just cleared, and model_guard::cli_slot_and_empty_and_malformed_model_sets caught it red before this was fixed.
- **deviation** — resolve_tier is kept as the single-name spelling of resolve_role rather than left beside it, so the fix reaches the model-guard and prepare.rs now instead of becoming dead code until mrs-3/mrs-4 sweep their call sites.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-3 — save as docs/knowledge/patterns/model-role-split-mrs-3-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-3 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-3's capped trace: CLAUDE_TIERS/CODEX_TIERS retired: a [bee-tier: <role>] marker naming a role nothing configures now REFUSES by name (transports role-not-configured / codex-spaw…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-3-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-3.json]
  polarity: pitfall
---

# model-role-split cell mrs-3 — pitfall candidate

## What the cell did

The model-guard asks whether a role is configured instead of matching four hardcoded names

## Recorded evidence (verbatim from .bee/cells/mrs-3.json)

- **deviation** — CLAUDE_TIERS/CODEX_TIERS retired: a [bee-tier: <role>] marker naming a role nothing configures now REFUSES by name (transports role-not-configured / codex-spawn-role-unconfigured) where it used to read as no marker and fall to the bare/unmarked deny — louder, per D2 "warned, never silently accepted"
- **deviation** — advisor is now a legal marker name on claude too (it was legal on codex only — the 4-vs-5 drift D2 names), because known_roles derives from slot_for_kind over DISPATCH_KINDS
- **deviation** — pinned_type_for now returns Option and the general-purpose repair is skipped for a role with no rendered agent: its old unwrap_or("undefined") was unreachable under four names and would have rewritten subagent_type to a non-existent type under an open set. PINNED_AGENT_TYPE and dispatch_kind_for_tier are untouched, as the cell scoped
- **deviation** — configured_model_set drops its separate resolve_advisor call: after mrs-2, resolve_tier reads the advisor slot directly and advisor is one of the walked config keys, so the second read was the same value
- **deviation** — No guard test needed retargeting or deleting — all 38 existing tests pass unchanged; 6 new tests cover the open set (derived known-role set, a role outside the old three slots as a member, any-configured-role marker, unconfigured-role refusal on both runtimes, no undefined repair)
- **deviation** — Pre-existing flake found, NOT caused here: doctor::tests::binary_freshness_* fails intermittently (3 identical runs of the same binary: green, green, red — a different test of the family each time). Unrelated to this cell; worth its own capture

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-4 — save as docs/knowledge/patterns/model-role-split-mrs-4-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-4 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-4's capped trace: Tests added as an inline #[cfg(test)] mod slot_map_tests inside prepare.rs rather than drivers/tests.rs — tests.rs was not in the cell's files and a sibling wo…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-4-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-4.json]
  polarity: pitfall
---

# model-role-split cell mrs-4 — pitfall candidate

## What the cell did

slot_for_kind resolves every dispatch kind through an explicit arm; an unmapped kind earns a typed refusal instead of the advisor slot

## Recorded evidence (verbatim from .bee/cells/mrs-4.json)

- **deviation** — Tests added as an inline #[cfg(test)] mod slot_map_tests inside prepare.rs rather than drivers/tests.rs — tests.rs was not in the cell's files and a sibling worker holds the drivers module for mrs-2.
- **deviation** — Cap names a latent hazard closed, not a live bug fixed: kind is enum-gated against DISPATCH_KINDS at both doors, so the old catch-all was unreachable today.
- **deviation** — Full declared verify shows 4 pre-existing environment-limited reds in tests/opencode_plugin_contracts.rs; the scoped drivers proof is green.
- **deviation** — The worktree also carried a sibling worker's uncommitted models.rs (mrs-2); the commit used --only to stage prepare.rs alone, but the test run compiled against that in-flight edit.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-5 — save as docs/knowledge/patterns/model-role-split-mrs-5-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-5 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-5's capped trace: The a2f85972 herding-fallback member set does NOT widen by hand here: since mrs-3 it is a derived view of slot_for_kind over DISPATCH_KINDS, so it admits a slo…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-5-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-5.json]
  polarity: pitfall
---

# model-role-split cell mrs-5 — pitfall candidate

## What the cell did

Keyed the pinned agent map on roles, retired the phantom --kind remedy, and gave the one dispatch door --role so every rendered bee agent is reachable

## Recorded evidence (verbatim from .bee/cells/mrs-5.json)

- **deviation** — The a2f85972 herding-fallback member set does NOT widen by hand here: since mrs-3 it is a derived view of slot_for_kind over DISPATCH_KINDS, so it admits a slot the same day the door can reach it. --role does not widen slot_for_kind, so prepare still publishes only generation/review/advisor fallbacks and the guard stays exactly as wide as the door.
- **deviation** — Cell was blocked mid-flight on job 2 and widened by the coordinator (decision 8ff6e79e); the work therefore lands in TWO commits, b832e6b4 (jobs 1 and 3) and 64291f89 (the --role flag), instead of one
- **deviation** — docs/history/model-role-split/plan.md, named in read_first, does not exist; worked from CONTEXT.md, the store decisions and the cell action
- **deviation** — Added prepare_dispatch_with_role beside prepare_dispatch as an arity adapter (one line, no second implementation) so the ~50 existing call sites in verbs/drivers/tests.rs, a file this cell does not own, stay untouched
- **deviation** — Reserved and edited packages/bee-rs/crates/bee/src/generated/registry_payload.json, which the widened file list did not name: it is where a flag is declared, so the catalog entry the cell DOES name cannot be right without it
- **failure_signature** — 10eaa4270676

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-6 — save as docs/knowledge/patterns/model-role-split-mrs-6-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-6 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-6's capped trace: plan.md is absent from the worktree; read the copy in the main checkout at docs/history/model-role-split/plan.md"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-6-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-6.json]
  polarity: pitfall
---

# model-role-split cell mrs-6 — pitfall candidate

## What the cell did

Agent files render from the role an agent declares, resolved through the one shared resolver

## Recorded evidence (verbatim from .bee/cells/mrs-6.json)

- **deviation** — plan.md is absent from the worktree; read the copy in the main checkout at docs/history/model-role-split/plan.md
- **deviation** — Edited onboard/apply.rs beyond the cell files, reserved first: it held the two call sites of the retired tier lookup, and leaving them would make the new resolver dead code
- **deviation** — Kept the AGENT_TIER_DEFAULTS_CLAUDE/_OPENCODE identifiers: renaming forces an edit in verbs/status_full/store.rs, and the identifier sweep belongs to the tier-retirement slice
- **deviation** — Kept the {{TIER_MODEL}} template token: renaming it rewrites packages/bee/agents/*.md.tmpl and the release-manifest-hashed trees, which the regen cell owns
- **deviation** — HOST-VISIBLE: a models.<rt>.<role> value of {kind:cli} with no command is junk to the shared parser, so the role default stands and the agent file is now rendered instead of removed
- **deviation** — HOST-VISIBLE: a models.<rt>.<role> value of {kind:native,model:x} now renders x; the retired local normalizer dropped the shape and shipped the default
- **deviation** — This repo .claude/agents and .opencode/agent trees are byte unchanged; the committed-projection pin test proves it

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-7 — save as docs/knowledge/patterns/model-role-split-mrs-7-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-7 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-7's capped trace: gates-and-delegation.md stayed in scope but unedited: its subagent_type mention already reads as prepare-returns-only plus documented guard leniency (D2), neve…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-7-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-7.json]
  polarity: pitfall
---

# model-role-split cell mrs-7 — pitfall candidate

## What the cell did

Rewrote swarming-reference's read-only worker spawn instructions and Subagent-type table row to route through bee dispatch prepare --kind gather --role extraction; agent names appear only as what prepare returns

## Recorded evidence (verbatim from .bee/cells/mrs-7.json)

- **deviation** — gates-and-delegation.md stayed in scope but unedited: its subagent_type mention already reads as prepare-returns-only plus documented guard leniency (D2), never an instruction to hand-pick; editing it risked reinterpreting a locked decision outside the named contradiction at swarming-reference.md:104-114 and :294.
- **deviation** — sync-ack: gates-and-delegation.md (bee-hive skill) was in cell scope but deliberately left unedited: its subagent_type mention already reads as prepare-returns-only plus documented guard leniency (D2), not an instruction to hand-pick, so no edit was needed there — only bee-swarming's swarming-reference.md carried the named contradiction (lines 104-114 and 294)

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-8 — save as docs/knowledge/patterns/model-role-split-mrs-8-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-8 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-8's capped trace: write_guard/tests.rs was reserved but left untouched: all 7 of its .bee/cells/ mentions are guard INPUT path strings (and one stage_file writing the literal x)…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-8-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-8.json]
  polarity: pitfall
---

# model-role-split cell mrs-8 — pitfall candidate

## What the cell did

bee cells add requires a non-empty role with a typed FIX line; membership never checked; 7 dispatched fixtures given roles, 159 never-resolved ones left roleless by rule

## Recorded evidence (verbatim from .bee/cells/mrs-8.json)

- **deviation** — write_guard/tests.rs was reserved but left untouched: all 7 of its .bee/cells/ mentions are guard INPUT path strings (and one stage_file writing the literal x), never cell-shaped JSON, so it holds zero fixtures the role field can reach
- **deviation** — Fixture sweep scoped by ownership, not abandoned: nothing in production reads a cell role today (proved by grep — the cell record tier is read at prepare.rs:823/:1414/:1699 and status_full/cells.rs:584 only), so the fixtures that will resolve a role live in drivers/tests.rs, which is cell mrs-9 own declared file. Reported rather than reached into.
- **deviation** — D8 vocabulary shipped in the FIX line only; the help string and every authoring surface belong to mrs-11, which is open and names those files
- **deviation** — sync-ack: The authoring-surface half of D7/D8 is cell mrs-11's declared scope (skills/bee-planning/references/planning-reference.md, skills/bee-herding/references/dispatch-dry-run.md, docs/knowledge/areas/workflow-state/cells-authoring-and-revision.md); mrs-8's files list carries only the validator and its tests, so editing those skills here would write through another cell's scope.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-9 — save as docs/knowledge/patterns/model-role-split-mrs-9-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-9 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-9's capped trace: Reserved and edited packages/bee-rs/crates/bee/src/verbs/drivers/models.rs, a fourth file the cell did not list: BOTH warn fixes the cell offered live in that …"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-9-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-9.json]
  polarity: pitfall
---

# model-role-split cell mrs-9 — pitfall candidate

## What the cell did

A cell execution resolves the model from the role the cell declares, heading 561e1bda's ordered list with the historical tail intact

## Recorded evidence (verbatim from .bee/cells/mrs-9.json)

- **deviation** — Reserved and edited packages/bee-rs/crates/bee/src/verbs/drivers/models.rs, a fourth file the cell did not list: BOTH warn fixes the cell offered live in that file (default_models, resolve_role/warn_unknown_role), so the cell mandated work there while listing only two files
- **deviation** — Rejected the cell hinted fix (a): mrs-10 claim that mid-list entries never consult default_models is FALSE in the path that matters — normalize_models seeds default_models into the very table resolve_role walks, so adding code/read there makes a MID-list code resolve and silently outrank every existing host generation model. Took (b), narrowed by provenance rather than by list position: head-only narrowing also fails, because on an existing host the head IS code for nearly every cell
- **deviation** — Changed the [bee-tier: …] marker, logical_tier, pinned_agent_type and the herding fallback lookup to name the role that RESOLVED rather than the role asked for. Not in the cell text, but required by its own must-have: model_guard classify_marker DENIES a marker naming a role nothing configures, so an unresolved code marker would refuse every cell dispatch on every host that has not opted in
- **deviation** — Left the pre-mrs-8 (role-less) cell path on its recorded tier, refusal included, so no legacy record changes model or answer; the cell body kept its ceiling handling
- **deviation** — The two warn properties are proven empirically, not only by predicate: running the fall-through test with --nocapture emits exactly one warn line, for the invented role design, and none for code

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-10 — save as docs/knowledge/patterns/model-role-split-mrs-10-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-10 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-10's capped trace: Shipped 2 of the 4 names the plan S4 row listed (code, read, not review, advisor). Named reason: publishing and asking are different acts (561e1bda), and revie…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-10-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-10.json]
  polarity: pitfall
---

# model-role-split cell mrs-10 — pitfall candidate

## What the cell did

A fresh .bee/config.json publishes code and read alongside the kept extraction/generation tail; review and advisor stay unshipped

## Recorded evidence (verbatim from .bee/cells/mrs-10.json)

- **deviation** — Shipped 2 of the 4 names the plan S4 row listed (code, read, not review, advisor). Named reason: publishing and asking are different acts (561e1bda), and review/advisor already resolve without a config key, so a default for either is a product decision rather than a publishing one.
- **deviation** — mrs-6 left a note that D3 prepends its published names to AGENT_ROLES_BY_NAME. I did NOT prepend: bee-gather serves the gather dispatch at generation strength, so prepending read there would downgrade it haiku-ward, and that is the asking half call. AGENT_ROLES_BY_NAME is unchanged.
- **deviation** — REGEN OWED (mrs-11 or a docs cell): .bee/config-sample.json documents models.claude/codex with extraction, generation, review and advisor, and now lacks code and read. It is embedded via include_str! and no longer mirrors what onboarding writes. No rendered agent file changed, so no agent-tree regen is owed.
- **deviation** — FOLLOWUP for the asking half (S4): resolve_role warns on every dispatch for a name absent from both the host config and drivers::default_models. code and read are exactly that for EXISTING hosts, which a fresh-config default cannot fix. S4 must decide whether default_models gains code/read or the warn is narrowed.
- **deviation** — FOUND, not fixed: a fresh claude host reviewer dispatch resolves to sonnet, not opus, because mid-list entries never consult default_models, while bee-review rendered agent file is pinned to opus by AGENT_TIER_DEFAULTS_CLAUDE. The two paths already disagree today; shipping a review default would have hidden that, not fixed it.
- **deviation** — CAPTURE: a published config default is only worth shipping for a name the config file itself must carry for the tool own door to accept it. Every other asked-for name already resolves, and writing it turns a publishing act into a product decision.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-11 — save as docs/knowledge/patterns/model-role-split-mrs-11-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-11 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-11's capped trace: skills/bee-herding/references/dispatch-dry-run.md left untouched: it is a verbatim recorded proof transcript with no cell-payload example and no lane/role/tier…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-11-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-11.json]
  polarity: pitfall
---

# model-role-split cell mrs-11 — pitfall candidate

## What the cell did

Added role to every cell payload example and stated D8's vocabulary as guidance; brought .bee/config-sample.json's models block in line with a fresh onboarding write

## Recorded evidence (verbatim from .bee/cells/mrs-11.json)

- **deviation** — skills/bee-herding/references/dispatch-dry-run.md left untouched: it is a verbatim recorded proof transcript with no cell-payload example and no lane/role/tier field to update (its only lane mentions are PBI-classifier output, unrelated to the cell schema) -- confirmed by full-file search before leaving it out of the commit
- **deviation** — sync-ack: bee-herding predicted but not touched: skills/bee-herding/references/dispatch-dry-run.md was read and confirmed to hold no cell-payload example and no lane/role/tier field needing a change (a verbatim recorded proof transcript whose only lane mentions are PBI-classifier output) -- left untouched by named deviation, not an oversight

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-12 — save as docs/knowledge/patterns/model-role-split-mrs-12-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-12 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-12's capped trace: router.rs was in the cell file list but needed no change: its PORTED list already named all four verbs, so only the registry was out of date"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-12-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-12.json]
  polarity: pitfall
---

# model-role-split cell mrs-12 — pitfall candidate

## What the cell did

The registry now declares bee herding pane, agent-start, pane-id and result; the registry side was the wrong one

## Recorded evidence (verbatim from .bee/cells/mrs-12.json)

- **deviation** — router.rs was in the cell file list but needed no change: its PORTED list already named all four verbs, so only the registry was out of date
- **deviation** — registry_payload.json is not in the cell file list; reserved it before writing (it is where declarations live and is edited by hand, no generator remains)
- **deviation** — No regen chain was run: bee dev regen renders skill trees, onboard and the release manifest, none of which writes registry_payload.json

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-13 — save as docs/knowledge/patterns/model-role-split-mrs-13-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-13 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-13's capped trace: verbs/cells/util.rs added to scope (reserved under mrs-w13 first): try_mutating dispatch table lives there, so the verb would be unreachable without one routin…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-13-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-13.json]
  polarity: pitfall
---

# model-role-split cell mrs-13 — pitfall candidate

## What the cell did

bee cells backfill-roles gives every stored cell a role: dry-runnable, idempotent to the byte, ceiling tiers untouched, declared in the registry

## Recorded evidence (verbatim from .bee/cells/mrs-13.json)

- **deviation** — verbs/cells/util.rs added to scope (reserved under mrs-w13 first): try_mutating dispatch table lives there, so the verb would be unreachable without one routing arm
- **deviation** — named the verb bee cells backfill-roles rather than migrate-roles: backfill is D9 own word and cannot be misread as routine maintenance
- **deviation** — archived cells (.bee/cells/archive/<feature>/) are migrated too, since D9 says the stored cells and readCell/unarchive reach them
- **deviation** — a tier outside the legal three is counted and named as unmapped rather than defaulted to code, so a not-fully-migrated store says so
- **deviation** — did not run the migration against this repo live store, per the cell — proof is unit-level over tempfile fixture stores
- **deviation** — sync door acked: no workflow-state owned skill teaches a one-time operator migration; S10 carries the author-facing role guidance
- **deviation** — sync-ack: No owned skill of workflow-state teaches this verb: bee cells backfill-roles is a one-time operator migration over stored records, not a step in the planning/swarming/reviewing/capturing loop, and the cell scoped no skill file. The feature plan puts author-facing role guidance and surface text in S10; teaching a migration an orchestrator runs once would be noise in a worker skill.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-14 — save as docs/knowledge/patterns/model-role-split-mrs-14-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-14 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-14's capped trace: Five files outside the cell list, each reserved under mrs-w14 first: handlers_write.rs (the conversion extends `cells backfill-roles` rather than adding a seco…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-14-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-14.json]
  polarity: pitfall
---

# model-role-split cell mrs-14 — pitfall candidate

## What the cell did

ceiling became an escalation flag; flag, stored-record conversion and the 40% ration moved in one commit

## Recorded evidence (verbatim from .bee/cells/mrs-14.json)

- **deviation** — Five files outside the cell list, each reserved under mrs-w14 first: handlers_write.rs (the conversion extends `cells backfill-roles` rather than adding a second migration verb — one pass, so no store can answer half flag and half tier), cells/tests.rs and drivers/tests.rs (probes), generated/registry_payload.json (the verb description had to follow its behaviour), docs/handbook/register.md (the refusal row named the old denominator).
- **deviation** — trace.tier_reason is NOT renamed. docs/handbook/register.md publishes the key and 20 stored records carry it; renaming it here without those surfaces is a migration half-done. The tier-retirement slice renames it with the field.
- **deviation** — The `tier` string is still written beside the flag and left on stored records. status_full/cells.rs and session_preamble/store.rs still count it for their advice lines and are outside this cell; dropping it here would blind two counters this cell cannot fix.
- **deviation** — The ration denominator moved from `cells that recorded a tier` to `the feature's cells`. Named, not silent: `role` is required now, so no new cell records a tier and a tier-shaped denominator would be 0 for every new feature — the refusal could never fire again. All five existing ration probes keep their exact arithmetic.
- **deviation** — One test retargeted, none deleted: resolve_role over [ceiling, generation] now falls through instead of ending the walk, because D5 makes ceiling not a role. The Inherit assertion moves to resolve_tier, where the marker callers live.
- **deviation** — Open for the tier-retirement slice: status_full/cells.rs and session_preamble/store.rs still key their advice off `tier`, so a cell authored with `escalate: true` and no tier is invisible to those two ADVICE lines. The enforcing door (the 40% refusal) reads the flag.
- **deviation** — sync-ack: No instruction in the area's skills went stale from this change. bee-swarming's reference teaches escalation through bee cells tier --tier ceiling and the anchored [bee-tier: ceiling] marker, and both still read and behave exactly as written — only what the verb WRITES moved onto the flag. The reference's remaining tier doctrine (resolveTier, the tier-judgement rubric, the per-tier dispatch table) is retired wholesale by the tier-retirement and surfaces slices; editing it piecemeal now would leave the instruction layer half-migrated while bee cells tier and the tier field both still exist.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-15 — save as docs/knowledge/patterns/model-role-split-mrs-15-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-15 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-15's capped trace: The tier verb's escalation half is RENAMED, not deleted: set_tier/run_tier become set_escalation/run_escalate behind `bee cells escalate --id <id> [--reason <t…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-15-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-15.json]
  polarity: pitfall
---

# model-role-split cell mrs-15 — pitfall candidate

## What the cell did

bee cells tier retired with its enum; its escalation half kept the ration as bee cells escalate, and the reason key moved with its surfaces

## Recorded evidence (verbatim from .bee/cells/mrs-15.json)

- **deviation** — The tier verb's escalation half is RENAMED, not deleted: set_tier/run_tier become set_escalation/run_escalate behind `bee cells escalate --id <id> [--reason <text>] [--off]`. Deleting it outright would have deleted the only door that enforces D5's 40 percent ration - which D5 keeps unchanged in force - and would have forced deleting the ten probes that pin it. Named, not silent.
- **deviation** — cell_is_escalated still reads the legacy `tier: ceiling` string, DELIBERATELY. That read is what keeps the ration firing on a store nobody has migrated; it stays until every host has run the migration, and it is not this cell's to remove.
- **deviation** — Two files outside the cell list, each reserved under mrs-w15 first: verbs/cells/tests.rs (ten ration probes retargeted onto set_escalation, two probes added) and docs/handbook/register.md (it publishes trace.tier_reason, the cells verb list and the cell-field table - the rename is half-done without it).
- **deviation** — No test deleted to reach green. The three cli_shape tests that pinned cells.tier move onto backlog.pbi.status, its exact structural twin (two required fields, id first, an enum on the second); every assertion is the original one.
- **deviation** — --off is a new flag name: PINNED_FLAG_COUNT 182 -> 183, with the reuse candidates checked and each rejected by name in catalog.rs. Nothing in the vocabulary meant take this marking back off, and spelling the reversal --tier generation is exactly what D4 retires.
- **deviation** — The stored records are carried by `bee cells backfill-roles`, not by a hand-edit: the pass now renames trace.tier_reason to trace.escalation_reason wherever it finds it, idempotently, beside the role and the flag. It has still not been run against the live store (9 records there carry the key) - an operator run, unchanged from mrs-13.
- **deviation** — A stored record may keep carrying a tier string: D4 retires the field as a SELECTOR and does not order history rewritten. validate.rs now ignores the field entirely rather than refusing it.
- **deviation** — OPEN for the surfaces slice, flagged not touched: five skill trees, README.md and docs/handbook/stages/swarming.md + planning.md still teach `bee cells tier`. Outside this cell's files and reservation; mrs-14 already recorded this as the tier-retirement and surfaces slices shared residue.
- **deviation** — sync-ack: not: bee-swarming's SKILL.md and its swarming-reference still teach the whole tier doctrine — the extraction/generation/ceiling judgement rubric, resolveTier, the per-tier dispatch table — and inside it the now-dead spelling `bee cells tier --id <id> --tier <tier>` (SKILL.md step 2; swarming-reference.md around the "Record the choice" line). That spelling is a REAL residue as of this commit, not a false positive: the verb is gone and the sentence now points at nothing. It is not edited here because the fix is not a find-and-replace — the surrounding rubric has to become a ROLE rubric, which is the surfaces slice's design job (plan.md S10), and mrs-14 already recorded this same block as the tier-retirement + surfaces slices' shared residue. Editing only the one command line would leave the instruction layer teaching a cost-tier judgement that nothing in the binary implements any more, which is the worse half-migration. Also still stale for the same reason: README.md:360, docs/handbook/stages/swarming.md:51, docs/handbook/stages/planning.md:50. The one thing that changed here and is fully carried: `docs/handbook/register.md`, which publishes the verb list, the cell fields and `trace.tier_reason` — all three moved with the code in this commit.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-16 — save as docs/knowledge/patterns/model-role-split-mrs-16-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-16 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-16's capped trace: De-enum, NOT rename: the persisted key stays tier. Nothing in the tree reads workers[].tier — the cap door matches nickname plus cell alone — so a rename buys …"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-16-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-16.json]
  polarity: pitfall
---

# model-role-split cell mrs-16 — pitfall candidate

## What the cell did

Worker rows record the role their dispatch resolved; the closed three-value enum is gone from both worker doors, the persisted key is unchanged

## Recorded evidence (verbatim from .bee/cells/mrs-16.json)

- **deviation** — De-enum, NOT rename: the persisted key stays tier. Nothing in the tree reads workers[].tier — the cap door matches nickname plus cell alone — so a rename buys no behavior, while the flag spelling it would have to follow lives in catalog.rs, generated/registry_payload.json, cli_shape.rs and the cap-refusal FIX text, all sibling mrs-15 files.
- **deviation** — Removed the now-dead MODEL_TIERS const from verbs/state_group/mod.rs (outside the cell files, reserved under mrs-w16): the two checks this cell retired were its only users.
- **deviation** — Extracted one shared worker_role_value shape check rather than editing two copies of the same rule in add and update.
- **deviation** — verbs/state_group/tests.rs named by the cell needed no edit: its one tier reference is a node-shape fixture that stays exact with the key unchanged.
- **deviation** — Verify was blocked about 6 minutes by sibling mrs-15 mid-edit: 13 set_tier compile errors in verbs/cells/tests.rs, none in my files. Waited for their fix (13 to 9 to 0) rather than working around it.
- **deviation** — The untested-lines advisory misreads this commit: the three new tests live in workers.rs own cfg(test) mod tests, which is not a test-shaped path.
- **deviation** — sync-ack: No owned skill states anything this cell made untrue. The CLI flag spelling and the persisted record key are both unchanged, so the swarming reference line that spells out a manual worker registration stays exact; only the retired enum membership check changed, which no skill quoted. Re-wording that surface from tier to role is S10 work, in a file mrs-7 already capped.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-17 — save as docs/knowledge/patterns/model-role-split-mrs-17-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-17 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-17's capped trace: devtools/statusline.rs left untouched: the cell listed it for a `tiered` flag at :368-372, but that flag classifies prompt-cache ephemeral 5m/1h token buckets,…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-17-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-17.json]
  polarity: pitfall
---

# model-role-split cell mrs-17 — pitfall candidate

## What the cell did

The mix counts roles with the escalation share beside it; both advice lines read the flag and count against the feature's cells; tier_mix renamed to role_mix as a stated public-contract decision; logical_tier deliberately left named

## Recorded evidence (verbatim from .bee/cells/mrs-17.json)

- **deviation** — devtools/statusline.rs left untouched: the cell listed it for a `tiered` flag at :368-372, but that flag classifies prompt-cache ephemeral 5m/1h token buckets, not model tiers. The plan inventory misclassified it. No model-tier reference exists in that file.
- **deviation** — Reserved 4 paths beyond the cell's list, all consequences of the assigned change: status_full/mod.rs and session_preamble/mod.rs (their private MODEL_TIERS copies became dead and SCARCITY_MIN_TIERED had to become SCARCITY_MIN_CELLS), and status_full/tests.rs and session_preamble/tests.rs (the published-contract probe and the scarcity-wording assertions go red on the rename; updated to pin the new contract, plus 4 new probes).
- **deviation** — tier_mix -> role_mix is a clean rename, not a dual-emit. Reasoned at the emit site: no truthful content survives the split, so a retained key would hand every existing field reader a permanent silent zero, where a missing key fails loudly.
- **deviation** — logical_tier in drivers/guard.rs left named, per its FROZEN comment. Consumers named at the site: .bee/logs/dispatch.jsonl readers, hooks/model_guard.rs:994 (a second writer of the same key), and verbs/drivers/tests.rs. Only the parameter and the doc comment changed.
- **deviation** — Base red NOT mine and NOT fixed: verbs::drivers::tests::a_registration_failure_never_unwinds_the_standing_claim asserts an invalid tier refuses worker registration. Sibling commit 81e7a4b4 opened that enum, so "bogus" is now a legal role and the test is stale. The file is neither in this cell nor reserved by me — it belongs to the cell that landed 81e7a4b4.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-18 — save as docs/knowledge/patterns/model-role-split-mrs-18-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-18 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-18's capped trace: Mutation check needed a temporary edit to verbs/drivers/prepare.rs (not a cell file): reserved it under mrs-w18 first, made the failure path unclaim the cell, …"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-18-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-18.json]
  polarity: pitfall
---

# model-role-split cell mrs-18 — pitfall candidate

## What the cell did

Retarget the claim-unwind pin to a blank-role registration failure and prove it by mutation

## Recorded evidence (verbatim from .bee/cells/mrs-18.json)

- **deviation** — Mutation check needed a temporary edit to verbs/drivers/prepare.rs (not a cell file): reserved it under mrs-w18 first, made the failure path unclaim the cell, confirmed the test goes red, then git checkout -- reverted it to pristine. The committed diff touches tests.rs only.
- **deviation** — FINDING: the pre-existing assertions would NOT have caught a claim unwind. They read status off the Value returned by claim_and_reserve_for_dispatch, which is the pre-registration in-memory copy and still says claimed even when the store was unwound. Under the mutation the first assert passed and only the new on-disk read failed. The guarantee was nominally pinned but actually unpinned before this cell.
- **deviation** — Also corrected a stale doc comment on the sibling test dispatch_prepare_claim_payload_pins_worker_registered_true, which still named an invalid cell tier as the one deterministic registration failure.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-19 — save as docs/knowledge/patterns/model-role-split-mrs-19-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-19 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-19's capped trace: Chain published only when the payload actually names a model — an escalated, cli-exec or herding dispatch has nothing to fall from"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-19-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-19.json]
  polarity: pitfall
---

# model-role-split cell mrs-19 — pitfall candidate

## What the cell did

dispatch prepare publishes an explicit-only retry.fallbackChains contract with D11's error gate beside the model; no retry loop, and no chain at all when unconfigured

## Recorded evidence (verbatim from .bee/cells/mrs-19.json)

- **deviation** — Chain published only when the payload actually names a model — an escalated, cli-exec or herding dispatch has nothing to fall from
- **deviation** — Byte-identity pinned by pre-change whole-payload goldens for gather/reviewer/advisor plus an add-exactly-one-key equality for the cell payload, whose prompt golden would churn on every worker-contract edit

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-20 — save as docs/knowledge/patterns/model-role-split-mrs-20-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-20 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-20's capped trace: Effort resolution: STOP RENDERING it rather than qualify it. The block is injected into every session and re-injected after every compaction, so a qualifier is…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-20-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-20.json]
  polarity: pitfall
---

# model-role-split cell mrs-20 — pitfall candidate

## What the cell did

The dispatch door publishes the host's configured roles and --role instead of a fixed tier list, and stops printing an effort no dispatch carries

## Recorded evidence (verbatim from .bee/cells/mrs-20.json)

- **deviation** — Effort resolution: STOP RENDERING it rather than qualify it. The block is injected into every session and re-injected after every compaction, so a qualifier is repeated length spent on a value nothing sends; the non-delivery is written down at the render site (render_role doc comment) and in decision fbbadad5 instead.
- **deviation** — The codex drop is recorded as its OWN fact, not folded into the claude harness reason: on claude every Resolved::Model site in prepare.rs destructures { model, .. } and the Agent tool takes no effort parameter (harness limit); on codex the spawn_agent arm emits neither model nor reasoning_effort even though the runtime accepts it and the native arm does emit it (bee's own gap, still open).
- **deviation** — Scope: two files beyond the cell, both reserved under mrs-w20 first. hooks/model_guard.rs holds tier_slot_display, the fixed-4-slot renderer that could not serve an open set — replaced by role_slot_display + dispatch_door_lines. hooks/compaction.rs carried a byte-copy of the same two lines and would otherwise have told a compacted session the list the preamble stopped saying; it now calls the one home.
- **deviation** — hooks/session_preamble/store.rs was reserved but needed no change — its D6 escalation-share work already landed.
- **deviation** — No regen needed: no skill, doc or generated artifact carries the old "Tier slots" text (rg over the tree, excluding target/). Regen stays with mrs-w21.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-21 — save as docs/knowledge/patterns/model-role-split-mrs-21-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-21 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-21's capped trace: skills/bee-swarming/references/worker-details.md:7 still lists model tier among the dispatch inputs and keeps its Trace Field Tiers By Lane heading — file not …"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-21-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-21.json]
  polarity: pitfall
---

# model-role-split cell mrs-21 — pitfall candidate

## What the cell did

Rewrote the tier rubric as a role rubric across the swarming skill tree, README, the two handbook stage pages, the delegation transport bullet and register.md; regen wrote the mirrors

## Recorded evidence (verbatim from .bee/cells/mrs-21.json)

- **deviation** — skills/bee-swarming/references/worker-details.md:7 still lists model tier among the dispatch inputs and keeps its Trace Field Tiers By Lane heading — file not in this cell files, left for the orchestrator
- **deviation** — docs/04-skills-spec.md:143, docs/01-distillation.md:38 and :146, docs/knowledge/areas/doctrine-layer/delegation-threshold.md:83 still teach the three cost tiers — same reason, out of this cell declared files
- **deviation** — down-tier survives in gates-and-delegation.md:125 and :128 as delegation-altitude language, kept for parity with AGENTS.md rather than split vocabulary
- **deviation** — sync-ack: affects_skills named the two skills by name (bee-swarming, bee-hive) while the door matches path-shaped entries; both predicted skills WERE touched, at skills/bee-swarming/SKILL.md, skills/bee-swarming/references/swarming-reference.md and skills/bee-hive/references/gates-and-delegation.md, and the cell is claimed so cells update refuses the repair

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-22 — save as docs/knowledge/patterns/model-role-split-mrs-22-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-22 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-22's capped trace: Renamed two headings that spelled trace DEPTH as tiers: worker-details.md Trace Field Tiers By Lane -> Trace Field Depth By Lane, and 01-distillation.md:118 Tr…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-22-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-22.json]
  polarity: pitfall
---

# model-role-split cell mrs-22 — pitfall candidate

## What the cell did

Four tier-era surfaces now teach the role rubric; two trace-depth headings renamed off the retired word; down-tier left intact

## Recorded evidence (verbatim from .bee/cells/mrs-22.json)

- **deviation** — Renamed two headings that spelled trace DEPTH as tiers: worker-details.md Trace Field Tiers By Lane -> Trace Field Depth By Lane, and 01-distillation.md:118 Trace tiers by lane -> Trace depth by lane; both inside declared files, so the retired word stops carrying a second meaning
- **deviation** — Pointer parity inside a declared file: 04-skills-spec.md:81 trace field tiers -> trace field depth by lane, and :80 lane-tier trace -> lane-scaled trace depth, following the heading rename
- **deviation** — AMBIGUOUS, LEFT ALONE: 04-skills-spec.md:123 a weaker-tier (Codex / extraction/generation) run reads as model STRENGTH for a pressure re-run, and extraction/generation are still legal configured role names, so it is not the retired selector. Flagged, not edited
- **deviation** — OUT OF SCOPE, OBSERVED: docs/03-workflow.md:128 still says lane-tier trace (trace depth, not cost tier) — undeclared file, left for the orchestrator to triage
- **deviation** — down-tier as delegation altitude left standing in delegation-threshold.md and named there as the different axis; R6 tier marker respelled to the literal [bee-tier:] marker, which keeps its historical spelling
- **deviation** — SYNC_DOOR acked: the R3 rule block is byte-unchanged and its applied_at file gates-and-delegation.md already carries the role rubric from mrs-21
- **deviation** — sync-ack: The rule block itself (R3, doctrine-layer-delegation-threshold) is byte-unchanged; only R6 wording and the implementation pointers below it moved. Its applied_at file skills/bee-hive/references/gates-and-delegation.md already teaches the role rubric after mrs-21 rewrote it in c532502a, and it is outside this cell's declared files.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-23 — save as docs/knowledge/patterns/model-role-split-mrs-23-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-23 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-23's capped trace: Mutation applied to verbs/drivers/models.rs (outside the cell files) as a temporary working-copy break; reserved it first, reverted it, and git status confirms…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-23-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-23.json]
  polarity: pitfall
---

# model-role-split cell mrs-23 — pitfall candidate

## What the cell did

Retargeted the role fall-through tests onto host-only model values, added the codex variant and a fixture-integrity guard; mutation-checked red

## Recorded evidence (verbatim from .bee/cells/mrs-23.json)

- **deviation** — Mutation applied to verbs/drivers/models.rs (outside the cell files) as a temporary working-copy break; reserved it first, reverted it, and git status confirms it carries no diff
- **deviation** — First mutation attempt (tail-only) was too broad and left the read path passing, because the read list falls through at index 1, not at the tail; narrowed it to any post-head step so all three tests bite

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-24 — save as docs/knowledge/patterns/model-role-split-mrs-24-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-24 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-24's capped trace: Reserved and edited 2 files beyond the cell list: drivers/tests.rs (new window and advisor-parity tests) and cells/tests.rs (2 copies of the FIX string are pin…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-24-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-24.json]
  polarity: pitfall
---

# model-role-split cell mrs-24 — pitfall candidate

## What the cell did

Narrowed the code/read warn exemption to a per-runtime pre-roles migration window, made the missing-role FIX text true, and collapsed resolve_advisor onto the one leaf parser

## Recorded evidence (verbatim from .bee/cells/mrs-24.json)

- **deviation** — Reserved and edited 2 files beyond the cell list: drivers/tests.rs (new window and advisor-parity tests) and cells/tests.rs (2 copies of the FIX string are pinned there byte-for-byte, so changing validate.rs alone is a red)
- **deviation** — Chose option (c) narrowing over (a) and (b): a bee process IS one dispatch, so a per-process warn still fires every dispatch; (b) needs doctor.rs, outside this cell files. Recorded with alternatives as store fef79243
- **deviation** — Left option (b) unbuilt: bee doctor should tell an un-migrated host to add the two keys. doctor.rs is outside this cell files and knows nothing of models config today — needs its own cell
- **deviation** — Same promise, uncorrected, in 3 owned-skill lines (skills/bee-planning/references/planning-reference.md:167,:215; skills/bee-swarming/references/swarming-reference.md:378), 2 docs (docs/knowledge/areas/workflow-state/cells-authoring-and-revision.md, docs/handbook/stages/planning.md) and .bee/config-sample.json. Capped with --sync-ack: the fix is a regen chain over 6 rendered trees and mrs-w25 holds the regen output reservation
- **deviation** — resolve_advisor collapse adopts resolve_configured cli leniency for a {kind:cli} value with no command string (Cli with empty command instead of None). Unreachable through read_models: normalize_tier_value drops that leaf, so the slot is absent
- **deviation** — sync-ack: The area's owned skills DO carry the same now-narrowed promise (skills/bee-planning/references/planning-reference.md:167,:215 and skills/bee-swarming/references/swarming-reference.md:378 all say an unconfigured role falls through and warns, with no exception), but that sentence is rendered into 6 skill trees plus .bee/config-sample.json and the fix is a bee dev regen chain — whose output tree is reserved by live sibling mrs-w25 (registry_payload.json) right now. Editing through that reservation is refused by contract, so the skill/doc sweep is handed back as its own cell rather than half-done here.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-25 — save as docs/knowledge/patterns/model-role-split-mrs-25-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-25 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-25's capped trace: Verified live behavior against packages/bee-rs/target/release/bee: the vendored .bee/bin/bee symlinks to the main checkout and predates --role, so it refuses t…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-25-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-25.json]
  polarity: pitfall
---

# model-role-split cell mrs-25 — pitfall candidate

## What the cell did

Rewrote docs/config-reference.md onto roles, escalation and the publish-only chain, and gave bee cells add --help the role field and recommended vocabulary

## Recorded evidence (verbatim from .bee/cells/mrs-25.json)

- **deviation** — Verified live behavior against packages/bee-rs/target/release/bee: the vendored .bee/bin/bee symlinks to the main checkout and predates --role, so it refuses the flag
- **deviation** — bee dev regen not run — release-manifest --check reports 270/270 match, so neither edited file is hashed by the manifest (mrs-12: registry_payload.json is hand-edited and regen does not write it)
- **deviation** — Also corrected role keys in the OpenCode and cli-executor examples, the bottom full sample, and the in-page anchor at line 19 — same page, same contradiction, all within the cell's named file

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-26 — save as docs/knowledge/patterns/model-role-split-mrs-26-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-26 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-26's capped trace: Widened scope to status_full/mod.rs (deleted MODEL_NORMALIZE_SLOTS and EFFORT_LEVELS, dead once the copy went) and status_full/tests.rs (3 regression tests); b…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-26-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-26.json]
  polarity: pitfall
---

# model-role-split cell mrs-26 — pitfall candidate

## What the cell did

status reads the one models parser; the third copy and its closed four-slot list are gone

## Recorded evidence (verbatim from .bee/cells/mrs-26.json)

- **deviation** — Widened scope to status_full/mod.rs (deleted MODEL_NORMALIZE_SLOTS and EFFORT_LEVELS, dead once the copy went) and status_full/tests.rs (3 regression tests); both reserved under mrs-w26 first.
- **deviation** — Kept the opencode SEED in status: drivers::default_models leaves opencode null, but the drift check compares against agent files that pin AGENT_TIER_DEFAULTS_OPENCODE. It is a default, not a parser — same split onboard::agents already uses.
- **deviation** — Behaviour improvement not named by the cell: a composite {primary,fallback_policy,fallback} slot now resolves to its primary model in the drift check. The old copy read .get("model") off the composite and reported a correctly-rendered file as stale.
- **deviation** — Did not run a red-first demo of the bug: siblings mrs-w24/mrs-w25 are live in this worktree, so stashing to rebuild the old code was unsafe. The drop is a code-level fact — the old overlay walked MODEL_NORMALIZE_SLOTS and could not insert any other key.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-27 — save as docs/knowledge/patterns/model-role-split-mrs-27-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-27 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-27's capped trace: Bound justification, measured not asserted: the printed role list is capped by a 120-character WIDTH budget, not a role count, then +N more (--json). Measured …"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-27-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-27.json]
  polarity: pitfall
---

# model-role-split cell mrs-27 — pitfall candidate

## What the cell did

bee status prints every configured role and validates every configured role

## Recorded evidence (verbatim from .bee/cells/mrs-27.json)

- **deviation** — Bound justification, measured not asserted: the printed role list is capped by a 120-character WIDTH budget, not a role count, then +N more (--json). Measured on the built binary — 12 short-named claude roles cost 153 chars of role text, the same 12 on opencode cost ~380; a count cannot tell those apart. The first suffix spelling (+N more (bee status --json for all)) cost 35 chars to drop 35 chars of short entries, so it was shortened.
- **deviation** — Printed order changed and two existing assertions with it: order is now derived (slot_for_kind over DISPATCH_KINDS first, then map order), the same rule hooks::model_guard::role_slot_display uses. extraction reads third rather than second because no dispatch kind resolves it any more.
- **deviation** — Fourth defect fixed, same family, one axis over: render.rs printed claude and opencode by hand out of the three RUNTIMES status carries, so a configured models.codex was invisible. A codex line prints when configured; absent when not, so no existing host gains an all-null row.
- **deviation** — Reserved and edited one file outside the cell: status_full/build.rs, two call sites, because Problem.slot had to stop being &'static str for an open name set. Reserved under mrs-w27 before writing.
- **deviation** — ADVICE_CLASS_SLOTS (advisor/review) deliberately LEFT closed, with the reason in code: those are bee's own door names and read-only is a property of what bee does there, not of any name an operator may choose.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-28 — save as docs/knowledge/patterns/model-role-split-mrs-28-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-28 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-28's capped trace: Reserved the 15 regen-written mirror paths under .agents/, .claude/, .claude-plugin/, .codex-plugin/, .opencode/ before commit; all granted, none hand-edited"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-28-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-28.json]
  polarity: pitfall
---

# model-role-split cell mrs-28 — pitfall candidate

## What the cell did

Every shipped statement of the role fall-through rule now names mrs-24's pre-roles migration window, matching validate.rs

## Recorded evidence (verbatim from .bee/cells/mrs-28.json)

- **deviation** — Reserved the 15 regen-written mirror paths under .agents/, .claude/, .claude-plugin/, .codex-plugin/, .opencode/ before commit; all granted, none hand-edited
- **deviation** — Left .bee/decisions.jsonl and .bee/onboarding.json unstaged: a sibling store write and a regen timestamp, not this cell work
- **deviation** — Capped with --sync-ack: affects_skills predicted the two skills by name, the door wants reference paths; bee cells update refuses inside a granted worktree
- **deviation** — sync-ack: affects_skills predicted the two skills by NAME (bee-planning, bee-swarming); the touched paths are those same two skills spelled as reference paths, so the prediction is correct and the mirrors are in step (bee dev regen ran, release-manifest --check 270/270). The spelling fix needs bee cells update, which refuses inside a granted feature worktree and is unreachable from this isolated worker session.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-29 — save as docs/knowledge/patterns/model-role-split-mrs-29-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-29 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-29's capped trace: The audit's before-state was half wrong and the cap records the measured one: an un-migrated host does not return bee-extract for --role read, it refuses role_…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-29-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-29.json]
  polarity: pitfall
---

# model-role-split cell mrs-29 — pitfall candidate

## What the cell did

One job now gets one rendered agent on any host vocabulary, and the chain gate is pinned on the published payload

## Recorded evidence (verbatim from .bee/cells/mrs-29.json)

- **deviation** — The audit's before-state was half wrong and the cap records the measured one: an un-migrated host does not return bee-extract for --role read, it refuses role_not_configured. The divergence is real either way (general-purpose vs a refusal) and the fix is the same.
- **deviation** — ROLE_AGENTS rows left unchanged rather than re-keyed: model_guard.rs and prepare.rs iterate the constant and depend on its rows being agent-unique, and both files are outside this cell. The job normalization lives in agent_for_role instead, which is also the honest split — role_for_agent's answer is resolved against models.<runtime> and must stay the name the agent was rendered from.
- **deviation** — CEILING ON THE CHAIN TEST, so the plan can be corrected rather than satisfied: bee publishes a chain and never walks one (51341f84), so no bee-side test can prove an executor obeys the gate. The new test pins what is provable — every published dispatch carries the gate correctly in both directions. The plan's claim that this test makes the feature "implemented" overstates it.
- **deviation** — --role read on a host that configures no read model is still refused by name (D2). Left as is deliberately and pinned in the test: there is no model to select, and answering an agent while selecting none is the silent-migration defect this feature removes.
- **deviation** — Verify ran ~4 min late: the shared crate was transiently red from sibling mrs-w27's in-flight status_full/tests.rs (their reservation). Polled until it compiled rather than writing through it; zero errors in my files at any point.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-30 — save as docs/knowledge/patterns/model-role-split-mrs-30-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-30 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-30's capped trace: Took fix (b) narrow prepare.rs over fix (a) reserve the name in validate.rs; recorded as decision 52ac81bf touching 97ce5225 — (a) puts a closed word back in D…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-30-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-30.json]
  polarity: pitfall
---

# model-role-split cell mrs-30 — pitfall candidate

## What the cell did

Narrowed the dispatch escalation arm with !from_role so a cell declared role ceiling can no longer take the session model uncounted; added the two-layer test binding the ration predicate to the dispatch channel

## Recorded evidence (verbatim from .bee/cells/mrs-30.json)

- **deviation** — Took fix (b) narrow prepare.rs over fix (a) reserve the name in validate.rs; recorded as decision 52ac81bf touching 97ce5225 — (a) puts a closed word back in D2 open set, inverts the existing D2-guard assertion in cells/tests.rs, and protects only cells authored after it lands while every stored role-ceiling cell keeps the bypass. The add-time assertion is therefore unchanged, deliberately, with the reason written beside it.
- **deviation** — Chose fix (b) narrow prepare.rs over fix (a) reserve the name in validate.rs; recorded as decision 52ac81bf touching 97ce5225. (a) would put a closed word back in D2 open role set, invert the existing D2-guard assertion, and leave every already-stored role-ceiling cell bypassing the ration. The add-time assertion in cells/tests.rs is unchanged on purpose, with the reason written beside it.
- **deviation** — New tests confirmed RED on pre-fix code before the fix landed: the_ration_and_the_dispatch_agree_on_which_cells_are_escalated failed at the c-role-ceiling row (ration says escalated=false, dispatch said true), and a_cell_role_of_ceiling_falls_through_the_resolver_but_the_flag_door_stays_open failed with model None instead of sonnet.
- **deviation** — Landed the commit through the guard-prescribed temp-index route (GIT_INDEX_FILE plus commit-tree plus update-ref) because the plain staging and commit verbs are refused while 2 workers are live in this checkout; the commit carries only my five files.
- **deviation** — Noted but NOT touched: verbs/drivers/guard.rs known_roles doc still says resolve_role answers ceiling with Resolved::Inherit, which the D5 resolver no longer does. That file is reserved by mrs-w32 and their in-flight diff already rewrites that same doc block.
- **deviation** — sync-ack: No skill edit is owed: skills/bee-swarming/references/swarming-reference.md:368 already publishes the correct rule — Escalation is not a role, running on the session model is the cell escalate FLAG. This cell fixes the CODE to match that published text; the skill was right and the dispatch was wrong, so nothing the skills teach changes.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-31 — save as docs/knowledge/patterns/model-role-split-mrs-31-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-31 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-31's capped trace: Shape 2 chosen over one-lock-spans-both: a lock held across a 40k-cell scan refuses every other writer for ~1s, and this migration must run against a live mult…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-31-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-31.json]
  polarity: pitfall
---

# model-role-split cell mrs-31 — pitfall candidate

## What the cell did

backfill-roles re-reads under the lock and merges only its three owned keys, so a write that lands during the scan survives

## Recorded evidence (verbatim from .bee/cells/mrs-31.json)

- **deviation** — Shape 2 chosen over one-lock-spans-both: a lock held across a 40k-cell scan refuses every other writer for ~1s, and this migration must run against a live multi-session store
- **deviation** — Added a scan/lock seam (backfill_roles_interleaved) so the race is pinned deterministically instead of by sleep or thread timing
- **deviation** — Added report field changed_during_pass (JSON + text) to name records that moved under the pass instead of writing over them
- **deviation** — Verified both race tests fail on pre-fix code (whole-object write restored escalate=true on e-1 and recreated g-1) and that the lock test fails when the lock name is mutated
- **deviation** — Reset stale index entries (validate.rs, drivers/models.rs, drivers/prepare.rs, drivers/tests.rs, cells/tests.rs) that held a pre-sibling-commit revert; worktree content untouched
- **deviation** — Capped with --inline-reason: no worker row registered for mrs-w31 and state worker add refuses inside the worktree
- **deviation** — Capped with --sync-ack: no skill documents this verb's locking or report shape
- **deviation** — sync-ack: Concurrency fix inside cells backfill-roles' write half. No bee skill teaches this verb's locking or its report shape, and the operator-facing contract is unchanged: same flags, still idempotent, still re-runnable. The only new surface is one report line naming records another writer moved.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-32 — save as docs/knowledge/patterns/model-role-split-mrs-32-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-32 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-32's capped trace: known_roles now derives from the runtime table plus ceiling only; the slot_for_kind union is deleted rather than gated, because every gated insert would be a s…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-32-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-32.json]
  polarity: pitfall
---

# model-role-split cell mrs-32 — pitfall candidate

## What the cell did

Refuse an unconfigured dispatch-door slot as a role name, and derive the two-agent pinned-type refusal from the agent table instead of the literal generation

## Recorded evidence (verbatim from .bee/cells/mrs-32.json)

- **deviation** — known_roles now derives from the runtime table plus ceiling only; the slot_for_kind union is deleted rather than gated, because every gated insert would be a strict no-op against the table keys (default_models already seeds generation/review/extraction into every runtime)
- **deviation** — open_role_config() and codex_spawn_rules asserted the defect (an unconfigured advisor accepted on claude and on codex); both fixtures now state their precondition and the negative case moved to a named regression test
- **deviation** — cap ran with --inline-reason: the dispatcher never registered worker mrs-w32 in state.json workers[], and bee state worker add refuses inside a granted feature worktree
- **deviation** — follow-up outside this cell's files: onboard/templates.rs default_config table still reads reachable-with-no-config-key = yes for advisor, now false. Doc comment only; its conclusion (ship no advisor default) is unchanged.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

from cell mrs-33 — save as docs/knowledge/patterns/model-role-split-mrs-33-pitfall.md

---
type: bee.pattern
title: model-role-split cell mrs-33 — pitfall candidate
description: "Pitfall candidate mined from cell mrs-33's capped trace: F1 fixed at the advisor slot (the one role with no Budget floor) rather than by refusing every null-valued key: a blanket non-null rule would empty known_roles…"
timestamp: 2026-08-25
bee:
  id: model-role-split-mrs-33-pitfall
  lifecycle: draft
  areas: [doctrine-layer, workflow-state]
  sources: [.bee/cells/mrs-33.json]
  polarity: pitfall
---

# model-role-split cell mrs-33 — pitfall candidate

## What the cell did

A null role slot now reads as off at every dispatch door, and both doors share one case-insensitive predicate

## Recorded evidence (verbatim from .bee/cells/mrs-33.json)

- **deviation** — F1 fixed at the advisor slot (the one role with no Budget floor) rather than by refusing every null-valued key: a blanket non-null rule would empty known_roles on codex, whose default_models seeds every slot null, and would contradict .bee/config-sample.json ("A missing/null slot = prompt-budget"). The null-versus-absent rule is still made once, in role_is_declarable, which asks resolve_advisor instead of re-testing the value.
- **deviation** — F3 fixed by making the comment TRUE rather than rewriting it: prepare now asks known_role_named (case-insensitive, answering in the config spelling) and rebinds role to that spelling, so --role Advisor still reaches the advisor resolver.
- **deviation** — F4 also corrected the extraction row of the templates.rs decision table, which claimed extraction was unreachable without a config key; default_models seeds it on every runtime. The shipping call is unchanged, only its stated reason.
- **deviation** — The dispatcher never registered this worker in state.json workers[], so the cap refused; registered it from the main checkout (bee state worker add --nickname mrs-w33 --cell mrs-33 --tier generation) rather than capping with a false inline reason.

## Status

Candidate only. `bee knowledge promote` proposes; naming the pattern, generalizing it beyond this cell, and moving `bee.lifecycle` to `active` are a human or agent decision.

knowledge promote: 34 capped cell(s) mined, 1 delivery draft, 50 area bullet(s), 34 pattern candidate(s), 0 file(s) written.