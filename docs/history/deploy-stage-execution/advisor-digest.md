# Advisor digest: Deploy-stage execution

## Consults

- Facts and gaps: `job-1789398696892-1612973-1`
- Risks: `job-1789398696892-1612974-1`
- Value: `job-1789397634012-1563183-1`
- Alternatives: `job-1789398696892-1612982-1`
- User impact: `job-1789398696892-1612981-1`

## Synthesis

All five perspectives support one narrow repair after the validated v2 deployment check. The dispatcher must replace the gather brief and worktree cwd only for Pi herding deployment.

The plan now includes the required safeguards:

- It separates harmless pre-merge payload proof from real post-merge publication.
- It names the exact stdin and command assertions.
- It tests main cwd with and without a feature worktree.
- It refuses non-main, wrong-role, wrong-runtime, and no-plan deployment requests before payload creation.
- It keeps the complete authorization refusal matrix and one-use recovery rule.
- It defines the positional version as authoritative and the environment value as audit-only.
- It rebuilds the candidate before regeneration.
- It updates the shared delegation contract and corrects the verification recipe.

## Rejected alternatives

- A cwd-only change leaves the worker read-only.
- A new dispatch kind widens the public surface without need.
- Direct orchestrator release bypasses the deploy worker boundary.
- Authorization during prepare consumes the permit before the worker starts.

## Gate judgment

<!-- bee:not-a-deferral: This paragraph explains the completed cell order; it promises no future work. -->
The three-cell sequence is the smallest honest high-risk shape. Code, shared instructions, and harmless installed proof have separate owners. Dependencies are serial because each later cell verifies output from the prior cell.
<!-- /bee:not-a-deferral -->
