# Codex reliability closeout

## Locked decisions

D1 — Complete F1–F7 from `docs/history/codex-parity-completion/reports/workflow-review-20260913.md`.
Store decision: `b0e62deb-c093-462a-9944-d43011cf2e6d` (user requested completion).

- F1: prevent probe writes to the real home. Use an explicit executable and isolated homes. Restore only proven probe changes through permitted access.
- F2: retain historical-source regression failures and current-source successes. Label new evidence retrospective; do not claim original test order.
- F3: remove only `/var/tmp/bee-codex-auth-1qn4fO/auth.json` through permitted access. Verify absence without reading credentials. A guard refusal remains a named blocker.
- F4: remove source EOF whitespace and verify explicit committed ranges. Preserve raw logs.
- F5: append a correct evidence pointer without changing the historical judge.
- F6: reserve evidence paths before output. Prove a conflicting reservation refuses before file creation or truncation.
- F7: fix the onboarding renderer and regenerate metadata through the CLI, with a regression test.

## Boundaries

Preserve native write and model guards. Report host limitations as unverified or unsupported.
Do not read real credentials, change unrelated user settings, or bypass a physical-worktree refusal.
Existing user acceptance belongs to the previous feature. This work receives its own tests and acceptance.

## Evidence already inspected

`scripts/codex-parity-canary.sh:7` resolves a default through mise and leaves inherited PATH available to nested probes.
`packages/bee-rs/crates/bee/src/onboard/templates.rs:411` owns the obsolete generated Codex note.
Historical commits `0690c1eb1ff72302033dfa5be4b11f753abf471f` and `62408eb152b908ebc6ebeb569866a8c44115f289` remain available.

## Completion

Deliver isolated probe behavior, regression evidence, corrected generated metadata, audit addendum, scoped tests, full suite, and supported live proof.
Report remaining cleanup or host capability blockers with exact evidence. Do not mark unresolved findings closed.
