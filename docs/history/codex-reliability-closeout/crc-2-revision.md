# crc-2 evidence correction

Continue the same assigned cell after your completed process. The leader has not accepted its full requirements yet.

The historical log has three failures and one Claude pass. Keep that truthful result. The original four-fail expectation was a mistaken planning assumption, not permission to invent failing historical source.

The usage regression fails on model identity before checking token counts. In a separate retained test-only transplant, move the model assertion after the existing token-count assertions, preserving all assertions, and run on unchanged 0690c1eb production. Prove the duplicate count defect directly. Record exact diff and failures, then current success. Do not modify current product semantics.

Check the saved commits for a real failing Claude tool-result source. If none exists, state precisely that the historical Claude failure cannot be recovered. You may add a separate explicitly SYNTHETIC mutation check of current source in a second disposable tree, dropping the tool_result exclusion, to prove this test detects the reported bad behavior. Preserve the small mutation diff and restore/current green. Never label this as historical proof. No need to debug old unrelated code.

Remove the unsupported claim that the Claude defect definitely lived in an uncommitted attempt unless a retained source artifact proves it. Describe known outputs and remaining evidence limits.

Append evidence rather than replacing old logs. Reserve extra paths first. Use a scoped source/docs commit with cell trailer; the leader will consolidate cell commits at integration. Do not touch sibling work. Finish via shared control-plane CLI with a report, or name its exact refusal.
