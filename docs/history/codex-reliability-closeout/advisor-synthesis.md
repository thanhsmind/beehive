# Plan consult synthesis

All five configured perspectives returned: facts-gaps, risks, value, alternatives, user-impact.
Three ran read-only Codex CLI; two ran configured herding. Requested models were gpt-5.6-sol, gpt-6-astra and gemini-3.8-flash-high. Effective backend identity was not independently proved.

## Accepted
- Corrected two stale load-bearing anchors from lines 67/81 to 62/76 before the gate.
- F1 distinguishes future probe isolation from historical restoration. No before-image means no speculative rollback.
- Direct executable validation must cover canonical path and nested probes; environment hygiene is not an OS security boundary.
- F2 retains exact historical production identity and the test transplantation diff. Compile failure is not behavioral RED.
- F3 owner is crc-leader. Only the exact approved copy may be removed through permitted access. A file or dangling symlink means cleanup remains open.
- F5 and F6 now name retained addendum and reservation-proof paths.
- F6 proves both refused creation and refused truncation, with a successful uncontested control.
- F7 checks generated output, fresh onboarding and supported refresh of existing metadata. Null defaults remain distinct from configured capabilities.

## Dismissed or qualified
- Value: stop immediately on any historical compile failure — qualified. Resolve a bounded test-only helper mismatch if possible; never repair historical production to make the test run.
- Value: mise which itself caused the old global mutation — rejected. The read digest identifies mise use -g in the PATH wrapper; the canary fallback and nested PATH remain prevention targets.
- User-impact: residual credential exposure — qualified. Presence is verified; unauthorized use or disclosure is not established.
- Suggestions to rewrite historical judge JSON — rejected. The supported review record and new addendum carry the correction.
- Repeating plan consult after the narrowly diagnosed preview alias fix would not add a new risk perspective. The added crc-4 preserves the same preview handler and adds real CLI tests; this is a named form deviation within the once-per-feature consult.

## Additional local finding
The documented gate --preview and state gate --preview spellings refuse. state gate preview succeeds.
The shared boolean table omits preview. crc-4 repairs the input path with regression proof; no approval check is weakened.

## Evidence
Herding reports: .bee/mailbox/job-1789282690741-3910082-1/report-1.md and .bee/mailbox/job-1789282693217-3910914-1/report-1.md.
CLI dispatch ids: d2a3f533-c3ce-48fd-b138-8309241415a3, 82eef114-1f23-4d8f-bf98-c7993c6e08d2, 76d6f7c5-eb04-4302-86f3-aa3e5fcf4633.
CLI outputs inspected in this session; this synthesis preserves their actionable findings. No dropped seat.
