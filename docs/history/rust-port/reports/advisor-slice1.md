# rust-port Slice 1 — Advisor Consult (pre-Gate-3)

Advisor: fable (model-shaped, `models.claude.advisor`), read-only, 2026-07-26.
Evidence bundle: CONTEXT.md, validation-slice1.md, cells rust-port-7..12.

## Verdict

**PROCEED WITH NOTES** — B3 repair verified real and correctly targeted (anchors checked in source); wave order matches dependency reality; `--test <target>` form confirmed sound (missing target exits 101).

## Notes and disposition

1. Allow-fixture triviality was the residual B3 hole (an allow for the wrong reason is invisible) → **applied**: rig discipline now pairs every allow fixture with a twin-deny in the same root, or an explicit engagement signal (cell 7).
2. Stale-registry fixture could not be one symmetric diff (node has no cache; its skip is import-failure) → **applied**: cell 11 now carries two separate fixtures — rust-side stale-cache (accepted allow window) and node-side import-failure — neither satisfiable by a benign command on an intact root.
3. "Executes the real node hooks" was satisfiable with a doctored copy → **applied**: rig asserts sha256 of every seeded oracle file against repo source (cell 7).
4. Min-passed counts paddable → **applied**: rig output lists fixture names per class; cap evidence quotes class coverage, not just counts (cells 7/11 truths).
5. Minor notes: cell 8's live `.bee/cache/` write is sanctioned by its own prohibition wording; git-bash fixtures need git-initialized temp roots → **applied** to cell 11.
6. Re-rank: none.

Advice is data, not approval; no locked decision overridden.
