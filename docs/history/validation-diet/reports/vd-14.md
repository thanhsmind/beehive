# vd-14 — Restore the doctrine tokens the operating-block rewrite dropped

**Outcome:** [DONE] — `scripts/tests/test_gate_bypass_doctrine.mjs` was red
on `packages/bee/AGENTS.block.md` for 4 tokens. Tracing history (`git show
a16db517 -- packages/bee/AGENTS.block.md`) showed the drop actually
originated in the earlier abd-1/abd-2 operating-block diet, not vd-10's
merged-gate rewrite — vd-10's own diff never touched these lines. Restored
each token inside the line/rule it governs, not appended as a bare grep
satisfier:

- **Chain line** — `-> bee-planning ... implement-plan.md` now carries
  `(standard: on-demand; high-risk: always)`, matching README.md's fan-out
  wording.
- **`docs/history/<feature>/` tree note** — now reads `plan.md frozen at
  Gate 2 (D1) - standard/high-risk always, plan.md is opt-in (D4) for
  small, tiny/spike none since the cell is the micro-plan (D3)`, restoring
  the lane-conditional shape the diet had flattened to a bare "frozen at
  Gate 2" pointer.
- **Critical rule 8** — the ceremony-scaling clause now reads "whether or
  not the lane produced a `plan.md` (D3/D4)" in place of the vaguer
  "whatever the lane," ties the rule back to its governing decisions.

Root `AGENTS.md` re-rendered via
`node packages/bee/scripts/onboard_bee.mjs --repo-root . --apply` (never
hand-edited). Sizes: template 13,236 B → 13,396 B; root 13,355 B → 13,515 B
— both comfortably under the 14,000 warn / 15,000 hard fence.

**Verify:** recorded command,
`node scripts/tests/test_agents_budget.mjs && node scripts/tests/test_gate_bypass_doctrine.mjs`.
Both green: budget suite 7/7 (16 rules intact, no gaps/dupes, byte fence
passed, template/root byte-identical between markers); doctrine suite 0
failures, including all 4 previously-red `packages/bee/AGENTS.block.md`
assertions and the sentinel check.

**Files + commit:** `packages/bee/AGENTS.block.md`, `AGENTS.md`. Commit
`65b91661`, path-scoped to exactly these two files via a temp-index +
`commit-tree` (two live siblings in the shared checkout; the concurrent-
worker git guard refuses a plain `git add`/`git commit`). The `--apply`
cascade regenerated the full skill mirror set (`.claude/skills`,
`.agents/skills`, `.claude-plugin/skills`, `.codex-plugin/skills`) as
expected — left uncommitted per `regen_obligation_ack: wave-barrier`, owed
by the orchestrator at wave close. Full trace/evidence:
`.bee/cells/vd-14.json`.

**Deviations:** none.
