# vd-10 — Rewrite the shipped operating block: chain, critical rules, and the new evidence doctrine

**Outcome:** [DONE] — Per D2/D9/D11, updated every claim in
`packages/bee/AGENTS.block.md` that described the removed predictive-validation
layer as current behavior:

- **Chain diagram** — dropped the `bee-validating` / `[GATE 3]` line; Gate 2's
  quoted question now covers shape and execution together.
- **Critical rule 1** (terminal-home, full text required) — restated for the
  merged gate: no source edits until `approved_gates.execution: true`, set
  together with `approved_gates.shape` by the single Gate 2 approval
  (`bee state gate --merge`) at the end of planning.
- **Critical rule 3** — `claim` now refuses on "the merged gate's execution
  approval is missing," not "Gate 3 is unapproved."
- **Guardrails "Gate block" line** — same Gate 3 → merged-gate reword (found
  by a full-file `rg` sweep after the first pass; not named in the cell's
  action text but the same claim class).
- **Working-files table** — `.bee/spikes/<feature>/` line narrowed per D10:
  "opt-in feasibility spikes + exploring's SEE mocks; never an evidence
  store," so it no longer contradicts the new evidence rule.
- **New critical rule 16 (D9)** — evidence doctrine, scoped to evidence only:
  never author an artifact whose only purpose is to be deleted as evidence;
  evidence is what the build already emits (red test output, stack trace,
  verify output, `git diff`/`git show`); a red-first repro ships at its real
  path and is kept. Opt-in feasibility spikes and SEE mocks stay explicitly
  outside the rule, so it does not contradict the working-files table line
  five lines above it.

Appended rather than inserted mid-list, so `TERMINAL_HOME_RULES = [1,5,6,11]`
needed no index shift. `scripts/tests/test_agents_budget.mjs`:
`EXPECTED_RULE_COUNT` 15 → 16 in the same cell, plus a stale check-name label
("all 15 numbered..." → "all 16 numbered...").

Root `AGENTS.md` re-rendered from the template via
`node packages/bee/scripts/onboard_bee.mjs --repo-root . --apply` (never
hand-edited). Sizes: template 12,573 B → 13,236 B; root 12,692 B → 13,355 B —
both well under the 14,000 warn / 15,000 hard fence.

**Verify:** recorded command,
`node scripts/tests/test_agents_budget.mjs && node scripts/tests/test_doctrine_parity.mjs && node packages/bee/tests/test_misc.mjs`.
First two green (7/7, then full pass). `test_misc.mjs` failed on "STALE
skills/bee-swarming/agents/openai.yaml" — confirmed by `git diff` + mtime
that the file's content is sibling vd-11's own in-progress prose edit
(`skills/**`, outside this cell's scope and files list), not caused by this
cell; not fixed here. Capped via `--feature-verify-pending` (R82) since the
recorded run was not a clean pass end-to-end.

**Files + commit:** `packages/bee/AGENTS.block.md`, `AGENTS.md`,
`scripts/tests/test_agents_budget.mjs`, `packages/bee/scripts/onboard_bee.mjs`.
Commit `2e723b9c`, path-scoped to exactly these four files (shared checkout,
two live siblings). Full trace/evidence: `.bee/cells/vd-10.json`. No further
mirror/regen chain run beyond the render above —
`regen_obligation_ack: wave-barrier`, owed by the orchestrator at wave close.

**Deviations:** two, recorded on the cell trace.

1. Fixed a pre-existing bug in `onboard_bee.mjs`'s `validateSkillMarkers()`:
   the `firstFrontmatterOpener` scan didn't check fence state, so a `---`
   inside a fenced ```markdown example in
   `skills/bee-planning/references/planning-reference.md` (a sample
   plan.md frontmatter shown for illustration) was misread as a real
   frontmatter opener, falsely flagging the file's real `<!-- bee:only -->`
   markers and refusing the render whole-tree (`blocked_render`, zero
   writes). This blocked the render this cell needs for its own verify.
   Auto-fixed (deviation rule 3: blocking issue in the required path) by
   adding a `fence === null` guard to that one condition.
2. `onboard_bee.mjs --apply` has no AGENTS.md-only mode — it cascaded into
   the full skill-mirror regen chain (`sync_skill`/`remove_skill`/
   `refresh_legacy_global_skill` across `.claude/skills` and
   `.agents/skills`), which this cell's `regen_obligation_ack: wave-barrier`
   said to defer. `remove_skill bee-validating` deleted 10 still-git-tracked
   mirror files that `test_doctrine_parity.mjs` still reads (its own
   migration off those files is owed by a different, not-yet-landed cell),
   crashing that suite. Restored all 10 files to their exact HEAD content via
   `git show HEAD:<path>` (confirmed byte-identical after, `git diff --stat`
   empty) rather than via `git checkout`/`reset`, both refused by the
   concurrent-worker write guard. The rest of the mechanical mirror-sync
   output (`bee-exploring`/`bee-planning`/`bee-swarming`/`bee-xia` in both
   mirror trees, `.gitignore`, `.bee/onboarding.json` checksums) is left
   uncommitted in the working tree — not part of this cell's path-scoped
   commit, not authored content, and re-derived correctly by the
   orchestrator's wave-close regen once every sibling cell lands.

**Note for the orchestrator:** `trace.behavior_change` landed `false` after
capping via `--feature-verify-pending` without `--behavior-change` (same gap
vd-9 hit and documented) — the cell record carries no top-level
`behavior_change` field for `cells cap` to honor, and `cells
update`/a second `cells cap` both refuse post-cap (door-validated /
already-capped). The cell's real content is a behavior change (the shipped
doctrine block rewired for the merged gate + a new rule). Flagging in case
the swarming-close semantic-judge dispatch keys off this trace field rather
than `change_class: "behavior"`.
