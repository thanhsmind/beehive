# vd-11 — worker report

**Status:** [DONE]
**Outcome:** Retired the validating stage (bee-validating skill, standalone Gate 3) from the 14 declared skill files — merged Gate 2 now approves shape+execution together (`bee state gate --merge`, D2), bee-hive's priority rules renumbered 1-14 contiguously, all "Gates 1-3"/"Gate 3" bypass/precondition references became "Gates 1-2"/"Gate 2", and 11 pre-existing gate-bypass-doctrine failures on `bee-hive/SKILL.md` + `bee-swarming/SKILL.md` (unrelated lane-ceremony-v3 wording debt, confirmed via the failing suite) were cleared alongside.

**Files touched:** `skills/bee-hive/SKILL.md`, `skills/bee-hive/references/routing-and-contracts.md`, `skills/bee-hive/references/go-mode.md`, `skills/bee-swarming/SKILL.md`, `skills/bee-swarming/references/swarming-reference.md`, `skills/bee-swarming/agents/openai.yaml`, `skills/bee-briefing/SKILL.md`, `skills/bee-briefing/references/implement-plan-template.md`, `skills/bee-briefing/references/mini-brief-template.md`, `skills/bee-exploring/references/gray-area-probes.md`, `skills/bee-exploring/references/context-template.md`, `skills/bee-context-locking/references/context-template.md`, `skills/bee-xia/SKILL.md`, `skills/bee-xia/references/xia-protocol.md`.

**Commit:** `8870a145` (path-scoped, built via a temp `GIT_INDEX_FILE` + `commit-tree` + compare-and-swap `update-ref`, since `git add`/`git commit` were refused by the concurrent-worker guard with 2 live siblings in the checkout).

**Verify:** `node scripts/skill_lint.mjs` exits 0 (1 pre-existing advisory warning, unrelated to this cell). `node scripts/tests/test_gate_bypass_doctrine.mjs` — the 11 failures on this cell's files are now green; 4 failures remain, all confined to `packages/bee/AGENTS.block.md`, which is sibling cell vd-10's file (already capped; the residual gap is out of this cell's scope per the dispatch fence, not reached for).

**Finding for the orchestrator (not fixed here, scope discipline):** this cell's cap resolved `trace.behavior_change` to `false` because the cell JSON never carried a top-level `behavior_change: true` field (only `trace.behavior_change: true` was pre-set at cell authoring, which `cells cap` does not read — it reads `cell.behavior_change`, per `packages/bee/lib/cells.mjs:1830-1835`). Checked for a wider pattern before acting: sibling cells `vd-9` and `vd-10` — both `change_class: "behavior"`, `lane: high-risk`, genuine skill-doctrine rewrites — show the identical `behavior_change: false` post-cap, while `vd-1` (`change_class: "migration"`) correctly shows `true`. This is a feature-wide authoring gap on `change_class: "behavior"` cells, not specific to vd-11, and scribing-debt / semantic-judge-tier detection (`packages/bee/lib/cells.mjs:2334`, `:2437`) both key off `trace.behavior_change === true`, so all three cells will silently miss those obligations unless corrected. Did not reopen/re-cap vd-11 alone — that would desync it from vd-9/vd-10 without fixing the shared root cause, and reopening a capped cell to hand-adjust one flag mid-wave in a checkout with live siblings carries its own risk. Recommend a small fix-first cell (or a scribing-run invoked explicitly for the affected cell ids) that patches `behavior_change: true` on vd-9, vd-10, and vd-11 before the feature closes, so the scribing-sync obligation these three genuinely owe is not silently dropped.

Full trace: `.bee/cells/vd-11.json`.
