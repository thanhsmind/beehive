# vd-15 — Bring the skill bodies back under budget and restore bee-planning's routing hand-off

**Outcome:** [DONE] — fixed both defects: `skill_budget_fence.mjs` is green
(17 skills, 0 findings) and `test_cli_state.mjs`'s shipped-routing-caller
assertion is green (120 passed, 0 failed).

## Defect 1 — skill body budget

Ran the fence for live numbers before touching anything; it also flagged
two skills the dispatch's "at last measure" snapshot didn't name
(`bee-swarming` +95, `bee-xia` +116, both genuine small appends from the
same vd-11 commit that grew bee-hive/bee-briefing) plus a provenance
violation in `bee-swarming/SKILL.md:27`. Since the cell's own must-have is
"the fence exits 0" and neither file conflicted with siblings vd-13/vd-14,
trimmed those two alongside the four named skills, same rule, same style —
recorded here as a deviation from the literal file list, not a re-baseline.

**Trimmed by removing text only (no re-baseline), before → after (budget):**

| Skill | Before | After | Budget |
|---|---:|---:|---:|
| `bee-hive` | 8542 | 8182 | 8183 |
| `bee-exploring` | 8677 | 8145 | 8163 |
| `bee-briefing` | 8239 | 8156 | 8158 |
| `bee-swarming` (deviation — not in the cell's file list, needed for the fence to pass) | 8282 | 8171 | 8187 |
| `bee-xia` (deviation — same reason) | 6501 | 6321 | 6385 |

Every doctrine token `test_gate_bypass_doctrine.mjs` and `test_misc.mjs`
pin inside these five bodies (`product files only`, `changes behavior an
existing test asserts`, `weakening, deleting, or replacing existing
proof`, `cell is the micro-plan`, `plan.md is opt-in`, `logged scoping
synthesis`, `dispatched execution worker`, `gate_bypass_level`/`full`,
`confident best answer`, `cite the cell`, `next batch of cells`, `one
dispatched execution worker`, `never implement cells yourself`) verified
present verbatim after trimming — caught and fixed two of my own
verb-tense slips against these pins mid-edit (`test_gate_bypass_doctrine.mjs`
went red once, `bee-swarming`'s "cite the cell" got reworded to "cites the
cell"; both fixed and reverified green).

**`bee-planning` — trimmed hard, then re-baselined the remainder (8139 → 10003):**

Pre-absorption (right before cell vd-9 folded in bee-validating's two
survivors) the file was 8317 bytes — already 178 over the recorded 8139
budget from vd-7's legitimate gate-bypass-doctrine restoration, itself
never re-baselined. At this cell's start it was 11369. Compacted the
SMALLER PATH check and the review wave (D1/D5) to their tightest wording,
cut the spike-lane note and the routing/handoff lines' inline `(D1)`/
`(D6)`/`(D8)`/`(D1/D3)` citations (moved to `references/provenance.md`,
also clearing all 4 fence-flagged provenance violations), and trimmed
ceremony throughout (mode-gate prose, bootstrap paragraph, Gate 2/merged-gate
paragraphs, walking-skeleton/test-cell/scope-reduction/headless
paragraphs) — 995 bytes of pure cuts before any re-baseline, verified
against every pinned doctrine token the whole way.

Landed at 10003 bytes and stopped there: that is what remains once the
absorbed SMALLER PATH check and full review-wave dispatch protocol
(structure/cold-pickup criteria, cost formula, lane-scaling thresholds,
one-shot-then-one-pass escalation ladder) are stated as compactly as
possible without deleting either survivor or losing meaning. Re-baselined
`scripts/skill-body-budget.json`'s `bee-planning` entry 8139 → 10003 (1864
bytes above the pre-absorption 8317, genuinely new content per D1/D5) and
added the required `notes.bee-planning` justification (budget now exceeds
the 8192 ceiling). The repo-wide skill-body total still fell by far more
than this delta when `bee-validating`'s ~8049-byte body was deleted
outright — this re-baseline is the honest remainder of that trade, not a
new cost.

## Defect 2 — routing hand-off

Restored `bee-planning`'s hand-off to `swarming` as a concrete CLI call
(previously prose-only after cell vd-9's cleanup dropped the literal
command while fixing dead references):
`node .bee/bin/bee.mjs state set --owner planning --phase swarming
--next-action "Invoke bee-swarming."` (SKILL.md:78). This is the third
shipped routing caller test_cli_state.mjs's census now finds (with
bee-compounding, bee-exploring). Dropped the test's expected count from
four to three in the same cell (`packages/bee/tests/test_cli_state.mjs:991`)
— bee-validating's caller is correctly gone forever; only bee-planning's
needed restoring.

## Verify

`node scripts/skill_budget_fence.mjs && node
packages/bee/tests/test_cli_state.mjs && node scripts/skill_lint.mjs` —
exit 0. Fence: 17 skills checked, 0 findings. CLI state: 120 passed, 0
failed. Lint: 1 pre-existing advisory warning (`bee-hive/SKILL.md` missing
a "Progress ticks" pointer — present in this file unchanged from HEAD,
not introduced by this cell; lint is advisory-only, never blocks).
Recorded via `bee cells verify` before capping. Full text:
`.bee/cells/vd-15.json` trace.verify_output.

**Files + commit:** `skills/bee-planning/SKILL.md`,
`skills/bee-planning/references/provenance.md`,
`skills/bee-hive/SKILL.md`, `skills/bee-exploring/SKILL.md`,
`skills/bee-briefing/SKILL.md`, `skills/bee-swarming/SKILL.md`,
`skills/bee-swarming/references/provenance.md`, `skills/bee-xia/SKILL.md`,
`scripts/skill-body-budget.json`, `packages/bee/tests/test_cli_state.mjs`.

**Deviations:** trimmed `bee-swarming/SKILL.md` and `bee-xia/SKILL.md`
even though neither is in the cell's declared `files` list — both were
red on the fence at cell start (missing from the dispatch's "at last
measure" snapshot) and the cell's own must-have is the fence exiting 0;
same rule (trim, don't re-baseline) applied for consistency. No other
deviations — `bee-planning` re-baseline is the judgment call the cell
explicitly asked for, not a deviation.

**Regen:** not run — `regen_obligation_ack: wave-barrier`; the four
rendered mirrors are untouched, owed at wave close by the orchestrator.

**Not mine, left alone:** siblings vd-13 (capped, `scripts/tests/test_state_projection_race.mjs`,
`test_doctrine_parity.mjs`, `packages/bee/scripts/test_onboard_bee.mjs`)
and vd-14 (`packages/bee/AGENTS.block.md`, root `AGENTS.md`) were in
flight in the same checkout; the shared `.git/index` carries stray staged
content from vd-13's own scoped-index workaround plus vd-14's in-progress
edits, so this commit lands through the same scoped-temp-index technique
vd-13 used, never touching either sibling's files.
