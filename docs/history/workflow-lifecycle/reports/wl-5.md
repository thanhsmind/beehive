# wl-5

[DONE] Added `runExample` coverage for the `state.workflows.list` and
`state.workflows.close` registry examples (previously never exercised,
failing the "every registry entry had its example executed at least once"
check).

Files touched: `packages/bee/tests/test_bee_cli.mjs`

Full trace/evidence: `.bee/cells/wl-5.json`
