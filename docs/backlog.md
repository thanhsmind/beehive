# Product Backlog

<!--
GENERATED FILE — do not hand-edit.
Rendered by `bee backlog render` from event-sourced PBI records in .bee/backlog.jsonl (backlog-unification D1/D3).
Regenerate: `bee backlog render --write`. Check freshness: `bee backlog render --check`.
Deterministic: byte-identical for the same backlog.jsonl contents — status-grouped, id-sorted entries, LF endings,
never a generation timestamp or any other wall-clock value.
-->

| ID | Story | CoS | Status | Feature |
|----|-------|-----|--------|---------|
| p-727e9529 | Port bee core mjs to single compiled Rust binary queen-bee (CLI + hooks), jsonl storage unchanged, <5ms invocation | queen-bee binary passes full parity harness vs mjs; hooks + 9 command groups run through one binary; host onboarding ships binary only; p95 invocation <5ms | in-flight | rust-port |
| P40 | Each swarm worker gets its own git worktree | Implementation and deterministic Git transaction/fault tests are green, but the required live native-worktree commit/merge acceptance remains blocked because the current Codex checkout mounts `.git` read-only. Complete the live attested commit, transactional merge-back, committed-main verify, and recovery-safe cleanup in a writable Git environment before marking done. | in-flight | worktree-isolation |
| P43 | Installer upgrades never split the release version across Codex, Claude, runtime, or project skills | Both top-level installers fail closed on a mixed release tuple and prove greenfield plus brownfield end to end before reporting success; 1.3.1 released 2026-07-16 with the Linux half proven (Bash E2E 15 cases as mandatory verify suite; read-only pre-confirm, rollback, status-gated refusals; plugin-first tuple coverage; managed-set cleanup fencing) and all 9 hosts onboarded to 1.3.1; Windows/PowerShell E2E remains the open half (owner request 2026-07-16; decisions 55ff17ef, 09b776b5, fc76ce41) | in-flight | installer-version-parity-1-3-1 |
| P69 | Every bee skill reads and writes the knowledge bundle instead of loose markdown | bee-scribing writes concepts, bee-compounding promotes them, bee-hive assembles its context packet from `knowledge context`, bee-grooming hunts stale knowledge — and Claude, Codex and any other runtime consume one shared bundle through per-runtime adapters, so a handover needs only the Markdown bundle, not the bee runtime (deferred by okf-foundation D22; requires P65). **Delivered per clause** — *scribing writes concepts*: okf-switchover-f3 f3-2/f3-3, one bundle-mode predicate + three-layer anti-fork gate. *bee-hive assembles from knowledge context*: f3-1/f3-4 (relevance-ranked criticals, bundle-first reading order) and okf-integration-close-f4 f4-1/f4-3 (the two reference files it defers to, and the session preamble itself, now bundle-routed). *grooming hunts stale knowledge*: f4-2 — its debt hunt counts the conformance check's own findings plus areas with code and no concept. **Remaining** — *compounding promotes them*: the `knowledge promote` verb exists but bee-compounding does not call it; promotion is still hand-authored. *per-runtime adapters*: a bundle-only handover (no bee runtime) is unstarted and unproven. | in-flight | okf-switchover-f3 + okf-integration-close-f4 (3 of 5 clauses — see Delivered/Remaining) |
| p-0530164c | Add bee knowledge scribing-target + emit-frontmatter CLI verbs so scribing stops needing node -e import(knowledge.mjs) | The new internals-reach guard (sqs-a) blocks inline node -e import(bin/lib). bee-scribing/SKILL.md is the only doc using that pattern (2 calls: scribingTarget, emitFrontmatter via knowledge.mjs). CoS: (1) bee knowledge scribing-target and bee knowledge emit-frontmatter verbs exist and are registered; (2) bee-scribing/SKILL.md uses the verbs, not node -e import; (3) a fresh-session scribing run works with the guard live. Until then the guard breaks scribing in fresh sessions. | proposed | state-query-surface |
| p-063d7b18 | Test-prune đợt 2: test_conformance.mjs cắt về 4 scenario doctor độc quyền (~250 dòng); soi test_msn_invariants.mjs cùng tiêu chí |  | proposed | — |
| p-10caed3f | timings report verb: aggregate .bee/logs/timings.jsonl into slowest-command ranking |  | proposed | — |
| p-3416fb38 | multisession-native slice 2: workflow-first state (D1/D6/D7) | Per-workflow state records under .bee/runtime/workflows/<id>/ are the source of truth; state.json+lanes become read-only projections; workers derived from sessions/heartbeats; gates carry approved_for_plan_rev; startFeature creates workflows under workflow:<id> locks. Cells msn-5..10 in docs/history/multisession-native/plan.md. | proposed | multisession-native |
| p-34c37c1f | okf_migrate --check-all: gộp 12 hàng --check <area> trong run_verify thành 1 boot (~7s full verify, verb --verify-pins đã có tiền lệ) |  | proposed | — |
| p-4072163a | Widen decisions * verbs from the narrow door to the wide door so a granted feature worktree can read/write its own workspace-local decision store | From inside a granted worktree: decisions log/active/search/redact/render operate on the worktree's own .bee/decisions.jsonl copy without refusal; the narrow-door refusal remains for genuinely control-plane verbs; roots.rs:98-100 follow-up comment resolved; regression test proves a granted-worktree decisions call succeeds | proposed | decision-supersede-hygiene |
| p-4f055a6f | multisession-native slice 5: integration queue + 15-invariant closure | Serialized integration queue with no filesystem lock across long ops; all 15 issue #56 invariants green as automated tests; legacy shims retired; knowledge area rewritten; release + issue #56 closed with invariant results. Cells msn-22..25 in plan.md. | proposed | multisession-native |
| p-50f3af4d | bee cells schedule should detect shared regen-obligation side-effects, not rely solely on declared cell files | Two cells in the same wave that each carry a regen_obligation_ack (or otherwise touch a hashed root requiring release_manifest.mjs --write / onboarding ledger regen) are automatically treated as sharing those regen-target files for scheduling purposes, even when the cell author forgot to declare them in files. Recurred 3x within worktree-concurrency-guard (wcg-1/wcg-2/wcg-3); caught once by an adversarial plan-checker, not mechanically. | proposed | scheduler-regen-side-effect-detection |
| p-71d014e7 | Reap orphaned workflow record on default feature swap | state set --feature <swap> leaves the old feature's workflow record live/non-terminal (bee.mjs:2218-2231 carve-out): a later startFeature for the old name is blocked by an unreferenced workflow, and set --feature <old> re-adopts stale phase/gates. Close-or-reap the prior record on swap; add a determinism guard for same-feature .find() (goal-check LOW+INFO, slice 2). | proposed | multisession-native |
| p-722dfd0a | Tách god-module templates/lib/state.mjs thành module hẹp để giảm impacted fan-out |  | proposed | — |
| p-726a4881 | Port host-project verify registry (run_verify impacted doctrine) beyond bee mechanics | host JS test surfaces get graph-based impacted runs from queen-bee | proposed | — |
| p-759bd218 | Test-prune đợt 2: test_okf_pins.mjs cắt nhóm case 4-11 test hạ tầng pin trỏ docs/specs đã đóng băng (~150 dòng) |  | proposed | — |
| p-78448452 | Quy ước import trong suite scripts/: 8 suite import .bee/bin/lib thay vì templates/lib — thống nhất theo test_compaction_module convention |  | proposed | — |
| p-808487c4 | Session-close mid-phase warning omits the decision-0017 remedy: work finished inline mid-phase should stub to capture-queue + close, not only 'finish/cap or HANDOFF' | When Stop fires mid-phase and the work is already done (no claimed cells, no open resume point), the warning names the third remedy: log a capture stub (bee capture add) and close the workflow so capture can flush later; wording stays byte-consistent between Node wrapper and Rust port (session_close/mod.rs); test covers the new remedy line | proposed | — |
| p-81a97109 | Pure-binary installer: Node-free onboarding once queen-bee ships | host install works with no Node on PATH; binary download + managed files only | proposed | — |
| p-842955b5 | verify_policy deferred cho host project — cap nhận targeted-run, full chain thuần CI |  | proposed | — |
| p-9c48a67c | bee worktree new/register copies ALL .bee/cells files into a new worktree, including other features' stale uncapped claimed cells | REVISED after ips-1 (2026-08-10): disk prune of tracked foreign cells is forbidden (it manufactures mergeable deletions — P1 caught live); shipped state = fill arm + untracked-stray prune + git-clean invariant. The remaining ask is a READ-side filter: island cell reads (status counts, claim scans, worker views) exclude foreign-feature cells even when tracked files sit on disk. | proposed | worktree-store-hygiene |
| p-9d7b36fc | repeat bash install rewrites managed files (timestamp churn) | scripts/test_installers_e2e.mjs --installer bash passes: a repeat install reports current and rewrites no managed file | proposed | verify-red-triage |
| p-acc0e108 | Test-prune đợt 2: race-harness scaffold chung cho 6 suite race (argVal/spawnRacer trùng byte-for-byte, ~250-350 dòng) |  | proposed | — |
| p-c15fb6f5 | As a doctrine author, I want the pointer checks to read a citation naming more than one heading, so its target file and every heading it names are verified instead of the citation being skipped. | The advisory file-and-heading check recognises a citation whose parenthetical carries two or more quoted headings (or a trailing non-heading item) and checks the target's existence plus every heading named, rather than matching nothing and skipping the citation entirely; the blocking pointer gate binds every heading a citation names, not only the first; negative controls prove each failure mode is detected on every run; verified by execution against fixtures, not by reading the pattern. | proposed | tick-contract-inline |
| p-c6e61dfb | P7 stage-instruction offload: self-contained pipeline stages (exploring/scribing/compounding) run in workers loading their own skill; orchestrator keeps hive+swarming+gates only | Cumulative orchestrator instruction load ~halved on top of P1-P6; dispatch topology decision spec'd and approved | proposed | — |
| p-cb0e94e3 | Test-prune đợt 2: cắt scripts/test_worktree_store.mjs phần grants trùng test_worktree_cli, giữ replayLog; đổi tên tránh trùng basename với templates/tests/test_worktree_store.mjs |  | proposed | — |
| p-d4a5dcff | Test-prune đợt 2: gộp test_bypass_matrix + test_gate_bypass_doctrine thành một doctrine gate table-driven (~150 dòng) |  | proposed | — |
| p-dbe8b1a5 | reservationStoreCorrupt still checks the legacy reservations.json file | guards.mjs:474 reads the legacy file as a torn-write detector, but nothing writes it synchronously anymore (msn-16 shim) — repoint at lease-store integrity or retire the check (worker observation, slice 3). | proposed | multisession-native |
| p-e20d82c9 | multisession-native slice 3: sharded leases + handoff mailboxes (D4/D5) | reservations.json retired behind a shim; per-resource epoch leases under controlRoot/leases/; intent-scope vs write-lease split; cross-workspace advisory except exclusive-marked resources; per-workflow handoff mailboxes replace single HANDOFF.json. Cells msn-11..16 in plan.md. | proposed | multisession-native |
| p-ed2de0d0 | multisession-native slice 4: workspace isolation by default (D2/D3) | resolveContext() splits projectRoot/controlRoot/workspaceRoot/localRuntimeRoot; control plane shared across worktrees; workspace registry with single write_owner_session; second write-capable session auto-gets an isolated worktree (observe/shared-disjoint/isolated policy modes). Cells msn-17..21 in plan.md. | proposed | multisession-native |
| p-fb5764e6 | run_verify D6: trần kích hoạt mà level-1 direct = 0 suite thì exit 0 im lặng — cân nhắc note to hơn hoặc delegate loud |  | proposed | — |
| P25 | Bee can select Codex custom agent roles when the collaboration runtime exposes reliable role selection | Explorer, worker, and reviewer profiles are generated from one dispatch contract; swarming selects the intended profile per call, proves the effective model/sandbox/instructions, and detects unused or drifting profiles (deferred from codex-runtime-parity D4; re-verified NOT unlockable on codex-cli 0.144.4 — .codex/agents/*.toml not discovered, spawn_agent accepts built-ins only; capability matrix codex-native-runtime-v2 cnr2-4; re-probe on future Codex versions) | proposed | — |
| P29 | A headless outer loop runs validated cells through fresh sessions without a human at the keyboard | `while claim-next; do claude -p "execute <cell>"; done` — each cell gets a brand-new context; builds on fresh-session-handoff's cross-session claims and planned-next handoff; interactive flow must be proven first (deferred from fresh-session-handoff exploring, owner conversation 2026-07-13) | proposed | — |
| P33 | Turn-count discipline becomes an explicit rule, proven by the logger | Batching tool calls (one compound `Bash` beats five `Read`s) attacks the first-order cost term directly, but it is prose guidance and advisor-and-orchestration is deliberately about mechanism over exhortation — revisit once `tools.jsonl` can prove the effect on a real session (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P34 | Advisor thresholds are tuned from measured data, not guessed | "Two consecutive red verifies" is an invented number. Once the tools logger and advisor records run on real sessions, re-derive the trigger thresholds — and the false-positive rate of the decision-conflict detector — from what actually happened (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P35 | A session's bill is attributable to lanes and cells | A cost snapshot written at session close, so spend can be traced to the lane and cell that caused it instead of read off a status bar (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P36 | Decision records carry a machine-readable claim, so conflicts become detectable | Prerequisite for the advisor's "two locked decisions in conflict" trigger (AO2(c), deferred by AO9 — not dropped). Today `decision`/`rationale` are free prose with no structured claim, `scope` is the default `"repo"` in 106 of 135 records and absent from every `supersede` event, and the store's only mechanical relation (`supersedes`) is the *resolution* of a conflict, not its detection — so two active decisions can never be in a machine-visible contradiction. Give records a subject/predicate/value claim. Needs its own exploring: it is a data-model change to an append-only log with 135 existing events (schema, migration, CLI shape, compatibility with `activeDecisions`/`supersede`) (owner decision 2026-07-14) | proposed | — |
| P38 | Installer offers the status line as a one-flag opt-in | A real user installed bee and reported "I don't see the status line" — it is deliberately opt-in (decision 102efe08: onboarding never touches settings.json uninvited) and today enabling it is a manual 2-file copy + settings edit from `.claude/skills/bee-hive/templates/statusline/`. Add `--statusline` to install.sh/install.ps1 (and an onboard flag): with explicit consent it copies the pair to `~/.claude/` (or repo-level with the repo-scoped command) and merges the `statusLine` entry into settings.json with a `.bak`, same merge discipline as the hooks merge. Requires jq present — the flag should check and say so. Claude Code only (source: user report 2026-07-15) | proposed | — |
| P41 | Write-time deny gains a wait/queue option | Today a file conflict is a hard deny and a `[BLOCKED]` worker idles until orchestrator rescue. Once computed waves (P39) land, measure whether real stalls remain; only then consider a bounded wait/queue primitive at the write guard (deferred from parallel-scheduler exploring D4, 2026-07-15) | proposed | — |
| P47 | A claim wedged by a hard crash frees itself | The `.adopting` marker has no TTL/stale-reclaim: a SIGKILL mid-adoption wedges the claim permanently and the cell can never be claimed again without hand surgery. Independently corroborated by two reviewers in the v0.1.44 review (reliability + code-quality). Add a TTL/stale-sweep consistent with the existing claim heartbeat rules (source: review finding, promoted from machine backlog 2026-07-17) | proposed | — |
| P48 | Lane and session records stop being freely hand-writable gate inputs | Gate decisions read lane/session records that any process can hand-edit — a forged record can flip a gate or force compounding-complete. Decide the integrity mechanism (checksums, CLI-only writes like decisions.jsonl, or hook-guarded paths) and close the hole (source: v0.1.44 review finding, security + architecture, promoted from machine backlog 2026-07-17) | proposed | — |
| P50 | The two-terminal fresh-session handoff is proven by a real owner walkthrough | fresh-session-handoff (P28) shipped race-proven at the store level, but the real flow — two terminals, one `/clear`, planned-next adoption at the fresh boundary — has never been walked through by the owner. One recorded UAT session closes this (outstanding since v0.1.33, 2026-07-13) | proposed | — |
| P51 | Worktree feature-parallelism reaches the host projects | P44 shipped proven in beegog only; host rollout was explicitly D6-deferred and only anphabe-gogl is registered in dogfood_repos. Decide the rollout shape under the no-host-propagation directive (hosts pull via onboard themselves) and document the enable steps for a host owner (deferred from worktree-feature-parallelism D6, 2026-07-16) | proposed | — |
| P53 | The plan freeze is machine-enforced, not prose-ruled | Hash plan.md at Gate 2 approval; a hook denies post-approval content writes to it (approval stamp exempt). Deferred from lane-ceremony-v3 (D1 ships prose-only) | proposed | — |
| P54 | Cells carry an explicit slice id for long multi-slice features | `slice_id` on cells + slice-aware claim/status for standard/high-risk features that run many slices; v3 defines active slice as the open cells only (deferred from lane-ceremony-v3 D2) | proposed | — |
| P55 | Lane graduation is automated when evidence crosses a threshold | Mid-flight tiny→small→standard escalation beyond D8's re-run rule: mechanical trigger when file count or flags grow past the lane cap during execution (deferred from lane-ceremony-v3) | proposed | — |
| P59 | CLI reads land under 100ms — lazy module loading first, a Rust write-guard hook binary second | Trigger-conditioned (decision `2e224c90`, user follow-up 2026-07-21): (1) lazy-load the dispatcher so each verb imports only its own module slice — target simple reads ~30ms, status ~200ms; (2) if per-tool-call hook tax still hurts after that, port ONLY the write-guard hook to a small Rust binary (48-53ms → ~3ms per tool call), keeping node orchestration + copy-in distribution; full-CLI rust rewrite stays rejected while remaining cost is OS-bound I/O+git. Prove with the same 5-run median methodology as cli-performance | proposed | — |
| P60 | The behaviour corpus is language-neutral, so any implementation can be validated by the same tests | **Phase 0 of the Rust migration (decision `51d54157`, [strategy](decisions/0025-rust-migration-strategy.md)) and the gate on every later phase.** 35 of 56 suites already drive the CLI across a process boundary and validate any implementation as-is; the other 18 import JS modules directly and are the only structural lock-in. CoS: (1) those 18 suites are expressed as data — `(input, repo state) → expected verdict/output` — executed against whatever binary is under test; (2) no expectation encodes a JS internal (a module name, an export, an error class); (3) the full corpus runs green against today's JS runtime; (4) a deliberately broken build fails it (the corpus must bite). Valuable even if the migration is abandoned | proposed | — |
| P61 | Rust write-guard hook as a deferring fast path | **Phase 1.** Scoped in [CONTEXT](history/rust-write-guard/CONTEXT.md) (D1-D5). CoS: hook ≤5ms measured by the 10-run median method **against the import-trimmed JS baseline (~25ms), not today's 42ms**; the P60 corpus green against the binary; anything the binary does not fully recognize defers to JS; absent/incompatible binary ⇒ JS runs unchanged. Stop-condition from the strategy: if the real-session win is under ~15ms/call, the migration stops after hooks | proposed | — |
| P62 | Binaries ship reproducibly for five targets before the second port | **Phase 2 — the real cost of the migration, and it must exist before porting beyond the first hook.** bee ships today by copying files with no build step anywhere. CoS: CI cross-compiles linux x64/arm64, macOS x64/arm64, windows x64; checksums published and verified at install; the installer selects by platform and silently falls back to JS on no match; a tagged release proves all five plus the fallback | proposed | — |
| P63 | The runtime is Rust, one process at a time, JS deleted as it goes | **Phases 3-5**, in this order: remaining hooks → read-only verbs (status/list/show/search) → single-writer mutators → **concurrency layer last** (lock/claims/reservations/worktree-holds — three genuine correctness bugs surfaced there in one day, and its cost is I/O, not CPU). CoS per unit: corpus green, the unit's JS **deleted in the same feature** (no dual implementations — every drift incident this repo has had was two copies of one truth), store format unchanged in that step, and the phase independently revertible by deleting a binary | proposed | — |
| P67 | Finishing work promotes knowledge instead of accumulating it | `bee knowledge promote --work <id>` turns cell traces and `delivery.md` into updated Area/Feature concepts, new Decisions and new Patterns, and explicitly drops the temporary detail — so knowledge improves per session without growing per session (deferred by okf-foundation D22) | proposed | — |
| P68 | Stale, dangling, and self-contradicting knowledge is found by machine, not by memory | `bee knowledge stale` and `bee knowledge links` surface concepts whose area changed underneath them, dangling `required_context`/`supersedes` targets, and duplicate authority claims beyond what `check --strict` already catches (deferred by okf-foundation D22) | proposed | — |
| P72 | The ~65% context handoff rule is measurable, then enforceable | Today AGENTS.md critical rule 6 is prose only — nothing reads context usage and nothing writes HANDOFF.json at a threshold. The compaction counter from P71 is the first measurement that makes the rule checkable; this row is the rule itself (a threshold, a trigger, and a test that proves it fires) | proposed | — |
| P73 | Codex hook execution is a proven fact, not an open question | A probe that establishes whether an installed Codex CLI actually discovers and executes `.codex/hooks.json`, with a recorded result per codex-cli version. This is the unknown that forces the helper-floor constraint (compaction-hardening D3) and the hedge in the doctrine text (D2); resolving it lets a later feature state hook parity outright | proposed | — |

## Done / Declined

- [p-0a0fda78] 22 promote-proposals.md la kenh chet: bee close sinh proposal moi feature nhung 0/22 delivery draft duoc apply — duong promote thuc te la capture-queue — done
- [p-0a47ea8d] Per-check filter in suite harness (BEE_CHECK_ONLY): cell verify runs only relevant checks, full suite stays at slice tail + CI — done
- [p-0aa807b9] run_verify --impacted gains --level 1: direct-edge-only selection for the dev loop, transitive closure reserved for wave-close/merge — done
- [p-10e22a70] knowledge check bat con tro chet: path trong frontmatter sources/body Integration Points tro file khong ton tai — lop ri set lam ks-2 BLOCKED (bee-executing khai tu) — done
- [p-21583c96] Cua kiem luc close: solution co pham pattern da biet khong — check nhe truoc khi cap/close doi chieu diff voi critical patterns cua area cham toi — done
- [p-355d4740] Nhan 'critical' loang: 85/101 pattern (84%) gan critical — digest 4 dong chon tu tap gan-toan-bo, mat gia tri loc — done
- [p-3d6877c2] Skill token diet wave 2: migrate remaining skills (exploring, reviewing-remainder, briefing, herding, compounding, executing, qualifying...) to thin-body doctrine, remove grandfather exceptions — done
- [p-47d864b5] Do tai pham: KPI that cua knowledge la incident cung loai khong lap lai — dem va bao cao repeat-incident rate theo pattern — done
- [p-4ae119b0] Port worktree-concurrency-guard onto bee 1.18.2's new packages/bee layout and controlRoot architecture — done
- [p-50de38d7] Ship visibility: draft PR on first cap, walking-skeleton slice 1, evidence-based lane demotion, progress ticks (spec ak/plans/reports/spec-260727-1632-bee-ship-visibility.md) — done
- [p-6ec778c5] Step ticks: mandatory ak-style per-step progress lines (route/gates/dispatch/cap/verify/barrier/sync/close), fixed format, user language, bypass never silences — done
- [p-7037485e] Anchor miss lam digest roi ve recency fallback ngay ca khi feature dang bound (vd decisions-worktree-door) — pattern lien quan khong duoc uu tien — done
- [p-72af01ca] Status diet: status --brief fast path + workers stop paying full status at startup (dispatch embeds state line) — done
- [p-7f564c5e] Workflow-record lifecycle: every startFeature path creates a record; close covers by feature; workflows list/close CLI verb — done
- [p-7fceeba1] Feature-close events: scribing+compounding run once at full feature completion; per-slice keeps only capture stubs — done
- [p-8aae1301] Skill token diet: thin-body doctrine + byte budget fence for bee skills (spec ak/plans/reports/spec-260727-1619-bee-skill-token-diet.md) — done
- [p-8afb88a4] Test batching: slice test cell replaces per-cell red-first on feature work (spec ak/plans/reports/spec-260727-1626-bee-test-batching.md) — done
- [p-91ceee70] Foundation fixes: workflow close transition kills state-clobber zombies; Windows suite split kills 600s timeout — done
- [p-94ecc5a2] bee state gate does not reject an unknown gate name or a non-boolean --approved — done
- [p-b11732c2] bee worktree new must require --with-companion (or refuse) when a live concurrent session touches a shared companion checkout — done
- [p-b595a094] Main-verifies: feature-level verify by the orchestrator; workers implement+commit+report only; cap pending path + close-door gate — done
- [p-c7db35b1] Verb pull len tang always-loaded: preamble/AGENTS nêu bee knowledge search mot dong — session plain-turn/off-rail biet duong keo ma khong can load skill nao — done
- [p-cc2500e0] Explicit triage: machine-readable route record (class\|lane\|flags\|files) required at feature start, surfaced in status+preamble, updated by re-lane — done
- [p-ccc558a0] Installer verify fails: bee.mjs status reports drift=true post-apply when repo-copy sync leaves an orphaned extra lib file — done
- [p-d494b04b] Bootstrap bundle mot lenh cho host repo: goglbe khong co docs/knowledge nen khong digest/pattern/search — can duong dung bundle re tu specs hien co — done
- [p-d7c88155] Duong keo giua dong: bee knowledge search — tra pattern/area theo trieu chung (error string, hanh vi sai, ten co che) ngay giua luc solve, khong chi 2 diem bom co dinh — done
- [p-e8a153e2] Validation speedup: delta validation, merged review wave, deferred presentation (spec ak/plans/reports/spec-260727-1610-bee-validation-speedup.md) — done
- [p-f893dcba] Ap suat flush cho capture queue: qua nguong (vd 5 stub hoac 7 ngay) thi close/preamble chuyen tu nhac nhe sang ep manh, va flush chay duoc tu session thuong — done
- [p-fa847e3a] Parallel-by-default doctrine: cells in a slice run concurrently on disjoint ownership; serial names its conflict; wave-barrier regen — done
- [P1] A greenfield repo with no build gets an init lane on its first onboard — done
- [P10] Gate 4 walkthrough can quiz the approver — done
- [P11] SEE gray areas can be locked by reacting to a throwaway HTML mock — done
- [P12] The orchestrator measures worker results itself before accepting them — done
- [P13] Advisor mode is dogfooded end-to-end — done
- [P14] A worker tier can resolve to an external executor (multi-provider swarm) — done
- [P15] Settlements are recorded without blocking the flow — done
- [P16] Review work can pin its own model, separate from generation — done
- [P17] A tier can carry a reasoning-effort knob — done
- [P18] Bee learns from its own dogfood data and ships the improvement (evolving loop) — done
- [P19] Bee updates its own skill set by script, not by hand-copy — done
- [P2] Backlog rows can be ranked automatically instead of hand-ordered — done
- [P20] Socratic questions pass an explicit materiality test before being asked — done
- [P21] CONTEXT.md pins fuzzy domain terms in a glossary as they crystallize — done
- [P22] Every subagent dispatch is audit-logged with its resolved model/tier — done
- [P23] Bee runs one unified fan-out pattern: best model orchestrates every phase, mechanical work goes down-tier — done
- [P24] Codex receives the same bee skills and lifecycle guardrails as Claude Code — done
- [P26] Full independent review runs only when the user explicitly requests it — done
- [P27] A stuck worker consults a configured advisor before returning [BLOCKED] — done
- [P28] Multiple terminals work one project without stomping each other, and a finished session hands work to a fresh one — done
- [P3] Backlog status renders as README badges — done
- [P30] Tracked append-only .jsonl records merge without conflicts across branches — done
- [P31] `bee.mjs <group> <verb>` is the only CLI an agent ever sees or ships — done
- [P32] Bee knows when to ask a second opinion, and stops doing the work itself — done
- [P37] Slice 1d — SRC source-identity classifier — done
- [P39] Parallel cells stop politely waiting: the scheduler computes waves from declared files+deps — done
- [P4] Gates 2–3 are reviewed on a single human-readable implement plan — done
- [P42] Codex subagent waits do not spam empty completion panels — done
- [P44] A single session fans work into independent-feature git worktrees, each with its own gates, merged back as event-sourced state — done
- [P45] Exploring asks fewer, batched rounds instead of one question per message — done
- [P46] Git/VCS commands are exempt from the intake gate — done
- [P49] `--force-downgrade` names its blast radius before acting — done
- [P5] Capture-mode engages in-flight, not only when a human remembers — done
- [P52] Small work starts from an executable work packet, not a shrunken feature plan — done
- [P56] A crashed session's unsettled context is recovered from the harness transcript — done
- [P57] A reversed decision propagates to every artifact that cites it, a backlog row only flips done when its CoS is actually met, and decisions stay findable at scale — done
- [P6] Model tiers are config-driven and runtime-keyed — done
- [P64] Bee's knowledge lives in one validated OKF bundle instead of five scattered document kinds — done
- [P65] An agent loads only the knowledge its current task needs, within a token budget — done
- [P66] Every area spec becomes small typed concepts, and the legacy docs tree is retired — done
- [P7] Keep the strong model scarce, measurably — done
- [P70] A Codex session runs bee with the same experience as Claude Code — done
- [P71] A session that gets compacted lands back on the work it was doing — done
- [P75] A cell cannot be authored without the regen obligations its own file list implies — done
- [P77] The product backlog is per-item files with a generated index, so concurrent sessions never contend on one markdown table and a session reads only the rows it needs — done
- [P8] Advisor pattern (cheap main loop, ceiling on demand) is first-class — done
- [P9] Exploring teaches before asking when the user is in unfamiliar territory — done
- [p-179bdcae] AGENTS doctrine block is over its 20 KiB hard budget — declined
- [p-86f2bfc1] compact-check mutates .bee/ across consecutive runs — declined
- [P74] Provider-native compaction transport stays out of bee — recorded, not re-litigated — declined
- [P76] Split `bee-exploring` into an automatic triage stage (`bee-qualifying`), a shared CONTEXT.md-writer (`bee-context-locking`), and a narrowed human `bee-exploring`, so a clear backlog item flows unattended to ready-for-pickup while an ambiguous one parks with a brief instead of blocking on a synchronous question — declined
