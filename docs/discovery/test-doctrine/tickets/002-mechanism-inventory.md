---
type: research
status: closed
claimed-by: session-wayfinding-1
blocked-by: none
---

## Question

Inventory every place the current test doctrine is mechanically
enforced or textually taught — CLI code paths (cap tests-key
vocabulary, close/merge auto-run, any skip/override flags), hook and
preamble text, skill text refrains, AGENTS.md lines — so the feature
knows its full touch surface and the cap-report fields available to
carry scope + reason.

## Answer

Full digest: ../research/002-findings.md. Load-bearing facts: the cap
report has no scope/reason field (shape change needed in
finish_support.rs + worker-cell.md); the only boundary skip is the
`commands.test: "none"` sentinel — no flag; the preamble red-check line
is byte-pinned by a test; ~16 skill-text passages carry the refrain;
AGENTS.md:86-89 and AGENTS.block.md:79-82 are stale (pre-13ce1858) and
the block is hash-pinned.
