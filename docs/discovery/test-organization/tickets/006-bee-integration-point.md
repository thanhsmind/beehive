---
type: grilling
status: open
claimed-by: none
blocked-by: 003-enough-not-overload-stop-rule.md
---

## Question

Where does the skill sit inside bee, and what points at it?

Sub-questions: the skill's name — `bee-testing` reads broader than the
locked scope (organization and isolation only), so it may over-promise;
whether the worker contract in bee-swarming references it or only the
DoD does; and how it stays clear of the locked proof-per-change-type
doctrine (D-58ec9664, D-1f534837), which owns *which tests to run*
while this convention owns *how test files are shaped*. Those two must
not blur.

Blocked until 003 settles, because the stop rule decides whether the
skill has anything to say at cap time or only at authoring time.

## Answer

<open>
