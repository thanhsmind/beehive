interface:
  display_name: "Bee Swarming"
  short_description: "Run approved cells to done — orchestrate bounded workers over gate-approved cells, or execute exactly one assigned cell inside a dispatched worker. Use when the merged shape+execution gate is approved and current-slice cells are open, or when running as a worker that received an assigned cell id."
policy:
  allow_implicit_invocation: true
