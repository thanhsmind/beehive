{
  "id": "wgh-2",
  "title": "Decide per-target denials before the delegate escape can fail open",
  "feature": "write-guard-hardening",
  "lane": "standard",
  "files": [
    "packages/bee-rs/crates/bee/src/hooks/write_guard/main.rs",
    "packages/bee-rs/crates/bee/src/hooks/write_guard/tests.rs"
  ],
  "read_first": [
    "docs/history/write-guard-hardening/CONTEXT.md"
  ],
  "decisions": [
    "D2"
  ],
  "deps": [
    "wgh-1"
  ],
  "judge_obligation_ack": "standard lane: feature-close independent review pass covers the judge read",
  "must_haves": {
    "truths": [
      "With .bee/companion-session.json present, a command mixing a shell-syntax target and a containment-failing literal target DENIES (exit 2) instead of delegating fail-open, in both target orders (the ADV-A/ADV-B probes become tests)",
      "The same holds when guards.memory_root is declared instead of the marker",
      "A command whose ONLY undecidable target genuinely needs delegation still delegates — the change never converts a legitimate lone delegate into a deny of an allowed write, and never allows anything previously denied"
    ]
  },
  "action": "Per CONTEXT.md D2: restructure the Bash per-target loop in main.rs so native verdicts (shell-syntax denial, containment denial) are decided and retained BEFORE any Err(Nd) from companion_mount_rel or memory_root_hit can escape as Outcome::Delegate — if any target already produced a native denial, the hook returns that denial; Nd escapes only when no denial exists. Mirror the same ordering on the Edit branch (main.rs:290/:300) where the identical class lives. Red-first tests from probes ADV-A/ADV-B (both orders, marker and memory_root variants) plus a control proving a lone legitimately-delegating payload still delegates.",
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml",
  "acceptance": "All three must_haves truths hold with named tests; suite green",
  "status": "capped",
  "trace": {
    "worker": "w-wgh-2",
    "outcome": "Deferred companion_mount_rel/memory_root_hit Nd escape past per-target native denial decision in Bash and mirrored Edit branch; 7 new tests (ADV-A/ADV-B both orders, marker+memory_root, lone-delegate and native-allow controls) plus full suite green (1825 passed)",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/hooks/write_guard/main.rs",
      "packages/bee-rs/crates/bee/src/hooks/write_guard/tests.rs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-08-16T02:56:49.064Z",
    "behavior_change": false,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "e499dfc3-c889-41e5-b4a7-381b38596b5e",
    "claimed_at": "2026-08-16T02:47:39.038Z",
    "warnings": [],
    "tests": "green",
    "results": ".bee/logs/test-results.json",
    "ran_at": "2026-08-16T02:56:29.526Z"
  }
}
