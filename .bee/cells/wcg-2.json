{
  "id": "wcg-2",
  "feature": "worktree-concurrency-guard",
  "title": "Wire isSharedNestedCheckoutTarget into the write-guard hook (D1b)",
  "lane": "high-risk",
  "deps": [
    "wcg-1"
  ],
  "decisions": [
    "D1",
    "D3",
    "D4",
    "D5"
  ],
  "files": [
    "hooks/bee-write-guard.mjs",
    ".bee/bin/hooks/bee-write-guard.mjs",
    "hooks/test_write_guard.mjs",
    "docs/history/codex-harness-hardening/release-manifest.json",
    ".bee/onboarding.json"
  ],
  "read_first": [
    ".bee/bin/lib/guards.mjs",
    "docs/history/worktree-concurrency-guard/CONTEXT.md",
    "docs/history/worktree-concurrency-guard/approach.md",
    "docs/history/worktree-concurrency-guard/reports/wcg-1.md"
  ],
  "action": "Per D1(b), call guards.isSharedNestedCheckoutTarget(root, absoluteTargetPath) inside hooks/bee-write-guard.mjs's main() dispatch, at the same point resolveCompanionMountedRelPath/describeCrossWorktreeTarget already run (around lines 791 and 804 for Bash and Edit/Write targets respectively) -- for each resolved rel target, if isSharedNestedCheckoutTarget returns true, deny the write with a typed, actionable message BEFORE calling guards.checkWrite (this is a new hard-fail-closed check per D3, not a checkWrite branch, since it needs the raw absolute target path guards.checkWrite doesn't receive). The denial message MUST redirect to `bee worktree new --with-companion` as a FRESH worktree (per D4 -- do not word it as upgrading the CURRENT worktree in place; it cannot be, per plan.md's Rollback Plan note). Per D5, this check must run unconditionally regardless of gate_bypass -- it lives in the hook, never consult .bee/config.json gate_bypass at all (existing hook-level guards in this file never do). This cell is `behavior_change: true` -- a real write that used to succeed can now be refused. Prove it red-first: write a test where a plain nested checkout write while concurrent currently SUCCEEDS (today, pre-wiring), confirm that red/passing-when-it-shouldn't state, THEN wire the check and confirm the same write now correctly denies, while a solo-session write to the same target and a verified-companion-mount write both remain allowed exactly as wcg-1's helper already proves. Do not modify guards.mjs itself in this cell -- isSharedNestedCheckoutTarget already exists and is proven; call it, do not change it.",
  "must_haves": {
    "truths": [
      "A write into a plain nested checkout inside a shared checkout, or into a verified companion mount by an UNVERIFIED path, is denied when isConcurrentMode() is true",
      "The exact same writes remain ALLOWED when no other session is concurrently live (D6 backward-compat, matches wcg-1's own no-op proof)",
      "The denial message names `bee worktree new --with-companion` as the fix, worded as opening a fresh worktree, never as upgrading the current one in place"
    ],
    "artifacts": [
      {
        "path": "hooks/bee-write-guard.mjs",
        "substantive": "new denial branch calling guards.isSharedNestedCheckoutTarget before checkWrite, with a typed, actionable refusal message"
      },
      {
        "path": "hooks/test_write_guard.mjs",
        "substantive": "red-before/green-after regression rows proving the new denial fires only when concurrent, and only for a genuinely shared/nested target"
      }
    ],
    "key_links": [
      "hooks/bee-write-guard.mjs imports and calls guards.isSharedNestedCheckoutTarget"
    ],
    "prohibitions": [
      "Do not modify guards.mjs's isSharedNestedCheckoutTarget in this cell",
      "Do not gate this check on gate_bypass -- it is hook-level per D5, never bypassed",
      "Do not word the refusal as an in-place upgrade of the current worktree -- always a fresh `bee worktree new --with-companion`"
    ]
  },
  "verify": "node --test hooks/test_write_guard.mjs && node scripts/ledger_parity.mjs --check",
  "behavior_change": true,
  "regen_obligation_ack": "release_manifest.mjs --check stays dropped from verify for the same pre-existing, already-filed, unrelated file-mode-drift reason recorded on wcg-1 (2026-07-23 friction: mode differ on ~500 files, main + every worktree). The action still runs release_manifest.mjs --write for real content-hash regen of the touched files.",
  "status": "capped",
  "trace": {
    "worker": "wcg-worker-2",
    "outcome": "Wired guards.isSharedNestedCheckoutTarget into bee-write-guard.mjs's Edit/Write and Bash dispatch (both hook copies) as a hard fail-closed pre-checkWrite refusal (D1b/D3/D5): a write into a genuinely shared nested checkout another live session can also reach, not covered by a verified companion marker, is denied with a typed refusal directing to a FRESH bee worktree new --with-companion (D4). Verified companion mounts and solo sessions stay allowed (D6). Never consults gate_bypass.",
    "files_changed": [
      "hooks/bee-write-guard.mjs",
      ".bee/bin/hooks/bee-write-guard.mjs",
      "hooks/test_write_guard.mjs",
      "docs/history/codex-harness-hardening/release-manifest.json",
      ".bee/onboarding.json"
    ],
    "deviations": [],
    "friction": "release_manifest.mjs --write rewrites ~510 mode-drift lines plus 2 unrelated pre-existing .bee-render.json content-hash entries; per the cell regen_obligation_ack the wholesale write was reverted and only the 2 touched-file sha256 entries were surgically applied. Same pre-existing mode-drift friction already filed on wcg-1 (2026-07-23).",
    "capped_at": "2026-07-24T09:11:14.950Z",
    "behavior_change": true,
    "verification_evidence": "{\n  \"tests_inspected\": [\n    \"hooks/test_write_guard.mjs rows 65-77 (existing companion-mount recognition + wcg-1 primitive baselines) to model the concurrency-dimension fixtures and confirm the verified-companion exemption boundary\"\n  ],\n  \"tests_added_or_changed\": [\n    \"hooks/test_write_guard.mjs rows 78-82 (new): 78 Edit into plain nested .git while concurrent -> DENY (status 2); 79 denial message names `bee worktree new --with-companion` and is worded as a FRESH/new worktree; 80 same Edit ALLOWED when only the acting session is live (D6 no-op / session exclusion); 81 Bash write into plain nested .git while concurrent -> DENY (Bash branch); 82 write into a verified companion mount stays ALLOWED even when concurrent (verified marker covers it -- paved road preserved)\"\n  ],\n  \"red_failure_evidence\": \"Before wiring, rows 78/81 returned status 0 (the plain-nested concurrent write SUCCEEDED -- STR65's exact unguarded shape) and row 79 found no refusal message. Captured '4 FAILURE(S)': row78 status=0 stderr empty, row79 message absent (both assertions failed), row81 status=0 stderr empty. Rows 80/82 already passed pre-wiring (solo allowed, verified companion allowed), proving the change is scoped to the concurrent+unverified-shared case.\",\n  \"green_after_state\": \"After wiring both hook copies + regen, node --test hooks/test_write_guard.mjs = ALL PASS (tests 1 / pass 1 / fail 0, 82 rows) and node scripts/ledger_parity.mjs --check clean; exit 0.\",\n  \"verification_run\": \"node --test hooks/test_write_guard.mjs && node scripts/ledger_parity.mjs --check -> exit 0\"\n}\n",
    "verify_output": "node --test: tests 1 / pass 1 / fail 0 (ALL PASS, 82 rows incl. new wcg-2 rows 78-82); ledger_parity --check: .bee/bin/** matches the .bee/onboarding.json managed-hash ledger. exit 0.",
    "verify_passed": true,
    "claim_session": "346c5e4d-65e6-40c1-b07c-57cca160d2e3",
    "claimed_at": "2026-07-24T08:56:39.946Z",
    "verify_command": "node --test hooks/test_write_guard.mjs && node scripts/ledger_parity.mjs --check",
    "verified_at": "2026-07-24T09:10:41.345Z",
    "attempts": [
      {
        "n": 1,
        "at": "2026-07-24T09:10:41.345Z",
        "claim_session": "346c5e4d-65e6-40c1-b07c-57cca160d2e3",
        "claimed_at": "2026-07-24T08:56:58.556Z",
        "acquired_at": "2026-07-24T08:56:39.945Z",
        "worker": "wcg-worker-2",
        "verdict": "pass",
        "failure_signature": null,
        "note": null
      }
    ],
    "semantic_judge": [
      {
        "schema": "judge-verdict/1",
        "verdict": "PASS",
        "checks": [
          {
            "id": "must_haves_truths",
            "status": "PASS",
            "evidence": "hooks/bee-write-guard.mjs:867-875 denies via guards.isSharedNestedCheckoutTarget(root,cand.abs,{excludeSessionId:sessionId}); rows 78/81 DENY concurrent, row80 ALLOW solo, row82 ALLOW verified companion; refusal at :44-55 names `bee worktree new --with-companion` as FRESH ('The current worktree cannot be converted') -- all green (ALL PASS, 82 rows)"
          },
          {
            "id": "decision_citations",
            "status": "PASS",
            "evidence": "D1b wired at main() dispatch (hooks/bee-write-guard.mjs:826-837,861-869), D3 hard pre-checkWrite fail-closed (:865-875), D4 fresh-worktree wording (:50-53); only gate_bypass token in file is a comment at :863 asserting it is never consulted -- no code reads config gate_bypass (D5 honored)"
          },
          {
            "id": "task_diff_alignment",
            "status": "PASS",
            "evidence": "git show 264daae --stat: guards.mjs NOT in diff (function only called, not modified); changes confined to declared files (both bee-write-guard.mjs copies, test_write_guard.mjs, release-manifest.json, onboarding.json) plus cell/regen artifacts"
          }
        ],
        "failure_signature": null,
        "fixability": "automatic",
        "confidence": "high",
        "builder_model": "sonnet",
        "judge_model": "opus",
        "model_independence": "confirmed",
        "recorded_at": "2026-07-24T09:15:41.037Z"
      }
    ]
  },
  "tier": "ceiling"
}
