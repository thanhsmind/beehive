{
  "id": "rust-port-10",
  "feature": "rust-port",
  "title": "bee-model-guard port: dispatch evaluation + economics audit line + conformance",
  "lane": "high-risk",
  "status": "capped",
  "deps": [
    "rust-port-7",
    "rust-port-8",
    "rust-port-9"
  ],
  "decisions": [
    "D2",
    "D7"
  ],
  "files": [
    "crates/bee-core/*",
    "crates/queen-bee/*"
  ],
  "read_first": [
    ".bee/bin/hooks/bee-model-guard.mjs",
    ".bee/bin/lib/dispatch-guard.mjs",
    ".bee/bin/lib/state.mjs",
    "docs/history/rust-port/CONTEXT.md"
  ],
  "action": "Port bee-model-guard onto the queen-bee hook runtime: dispatch-guard evaluateDispatch semantics with ALL SEVEN deny classes enumerated (dispatch-guard.mjs): codex-spawn-unmarked, generic-type-denied, param-tier-mismatch, param-on-nameless-tier, param-not-configured, cli-tier-denied, bare-denied — plus the allow paths. Tier resolution reproduces state.mjs resolveTier, resolveAdvisor, modelForTier over CONFIGURABLE_SLOTS from .bee/config.json, advisor slots included (extend the rust-port-8 config reader here if it lacks them). Economics audit line appended fail-open to the root's .bee/logs/dispatch.jsonl on EVERY evaluated dispatch, allowed or denied ({ts, tool, transport, model, tier, subagent_type, description, economics: logical_tier, requested_model, effective_model, effective_model_status, channel, enforcement}). Exit contract: allow 0, deny 2 plus stderr reason, crash 0 plus hooks.jsonl line. CONFORMANCE via the rust-port-7 rig in ONE integration target modelguard_conformance (verify uses cargo --test modelguard_conformance), ORACLE SEEDING per panel B3 (seeded temp roots; deny fixtures assert node oracle exit 2 first; negative-control unseeded root detected): one or more fixtures per deny class, allowed dispatches, crash fixture, dispatch.jsonl line-shape parity (volatile fields normalized).",
  "must_haves": {
    "truths": [
      "cargo --test modelguard_conformance runs (missing target exits 101) and reports at least 10 passed covering all seven deny classes",
      "all deny-class fixtures deny identically (exit 2 plus reason class) in node and rust, node verdict asserted non-trivial first",
      "dispatch.jsonl lines schema-identical after volatile normalization",
      "crash fixture exits 0 with a crash line"
    ],
    "artifacts": [
      {
        "path": "crates/bee-core/",
        "substantive": "dispatch evaluation (7 deny classes) plus economics derivation incl. advisor-slot resolution"
      },
      {
        "path": "crates/queen-bee/",
        "substantive": "model-guard hook plus modelguard_conformance corpus"
      }
    ],
    "key_links": [
      "rig drives the real bee-model-guard.mjs as oracle",
      "tier and config lookups via rust-port-8 readers (extended with advisor slots if missing)"
    ],
    "prohibitions": [
      "No edits to .bee/bin/ or packages/bee/ (D1)",
      "No wiring-file edits (flip is a later slice)",
      "Tests never touch the live .bee/ store",
      "No edits to crates/Cargo.toml members list"
    ]
  },
  "verify": "cargo test --manifest-path crates/Cargo.toml -p queen-bee --test modelguard_conformance",
  "trace": {
    "worker": "Jerry",
    "outcome": "Ported bee-model-guard onto the queen-bee hook runtime: bee_core::dispatch_guard::evaluate_dispatch reproduces dispatch-guard.mjs's evaluateDispatch verbatim (all seven deny classes: codex-spawn-unmarked, generic-type-denied, param-tier-mismatch, param-on-nameless-tier, param-not-configured, cli-tier-denied, bare-denied, plus the allow transports codex-spawn-marker/model-param/marker), backed by a new bee_core::config tier-resolution layer (normalize_models/resolve_tier/resolve_advisor/model_for_tier, extending rust-port-8's raw config reader with state.mjs's DEFAULT_MODELS defaulting + normalizeTierValue shape validation, incl. advisor-slot resolution) and a pure derive_economics port. queen_bee::hooks::model_guard is a thin wrapper turning the verdict into the hook exit/stderr/audit-log contract, appending one dispatch.jsonl economics line per evaluated dispatch (fail-open) and reusing write_guard's run_fail_open for the crash boundary. Conformance proven in ONE integration target (modelguard_conformance, 13 tests): a fixture per deny class paired with an allow twin (node-oracle-exits-2-first discipline), dispatch.jsonl line-shape parity (ts-normalized) for both an allow and a deny line, a node-side crash fixture (dispatch-guard.mjs fault-injected) and a rust-side crash fixture (direct run_fail_open panic), rig self-check, negative control, and a diff-detector meta-proof.",
    "files_changed": [
      "crates/bee-core/src/config.rs",
      "crates/bee-core/src/dispatch_guard.rs",
      "crates/bee-core/src/lib.rs",
      "crates/queen-bee/src/hooks/model_guard.rs",
      "crates/queen-bee/src/hooks/mod.rs",
      "crates/queen-bee/tests/modelguard_conformance.rs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-26T08:40:28.353Z",
    "behavior_change": false,
    "verification_evidence": "{\"new_suite_reason\": \"modelguard_conformance is the D7b conformance harness proving queen-bee hook model-guard matches the frozen bee-model-guard.mjs + dispatch-guard.mjs oracle across all seven deny classes plus allow paths, dispatch.jsonl economics-line shape, and the fail-open crash contract -- a permanent CI suite per the rust-port epic (D7), not a one-off test.\"}\n",
    "verify_output": "running 13 tests ... test result: ok. 13 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.31s. Covers all seven deny classes (bare_dispatch_denies_and_marker_twin_allows, generic_type_denied_for_pinned_tier_on_general_purpose_and_pinned_type_twin_allows, param_tier_mismatch_denies_and_matching_param_twin_allows, param_on_nameless_tier_denies_for_ceiling_plus_model_and_marker_alone_twin_allows, param_not_configured_denies_and_configured_model_twin_allows, cli_tier_denied_for_marker_only_cli_slot_and_plain_model_twin_allows, codex_spawn_unmarked_denies_and_marker_twin_allows) plus dispatch_jsonl_line_shape_parity_for_allow_and_deny, both crash fixtures, rig self-check, negative control, and the rig-diff-detector meta-proof.",
    "verify_passed": true,
    "claim_session": "d5476d93-d2c1-4805-acaa-441d371690f6",
    "claimed_at": "2026-07-26T08:23:04.832Z",
    "verify_command": "cargo test --manifest-path crates/Cargo.toml -p queen-bee --test modelguard_conformance",
    "verified_at": "2026-07-26T08:40:10.218Z",
    "attempts": [
      {
        "n": 1,
        "at": "2026-07-26T08:40:10.218Z",
        "claim_session": "d5476d93-d2c1-4805-acaa-441d371690f6",
        "claimed_at": "2026-07-26T08:23:04.830Z",
        "acquired_at": "2026-07-26T08:23:04.830Z",
        "worker": "Jerry",
        "verdict": "pass",
        "failure_signature": null,
        "note": null
      }
    ],
    "warnings": []
  }
}
