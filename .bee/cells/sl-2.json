{
  "id": "sl-2",
  "feature": "staging-lane",
  "lane": "standard",
  "tier": "generation",
  "title": "staging rebuild and status: invariant re-derivation",
  "behavior_change": true,
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/staging/mod.rs",
    "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
  ],
  "read_first": [
    "docs/history/staging-lane/plan.md",
    "packages/bee-rs/crates/bee/src/verbs/staging/mod.rs"
  ],
  "action": "Subcommands `staging rebuild [--without <slug,...>]` and `staging status` per D0a trigger 3. rebuild: refuse when no staging record; in the staging worktree run git reset --hard main; recompute the awaiting-UAT set = staged features whose uat gate is unapproved (read the feature's workflow/lane record gates from main) AND whose branch still exists; drop the rest from staged[] (auto-drop on UAT pass/merge — the invariant re-derives); merge each remaining branch minus --without exclusions; per-feature conflict: abort that merge, typed STAGING_MERGE_CONFLICT naming the feature, CONTINUE with the rest, report all conflicts at the end; update base_sha; run commands.staging_build when present. status: staged set with each feature's uat gate state, plus stale-base warning when base_sha != current main sha (the trigger-3 reminder). Tests: rebuild resets to new main and re-merges only unapproved-gate features; --without excludes; UAT-approved feature auto-drops; one conflicting feature does not block the others; status reports stale base.",
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml",
  "must_haves": {
    "truths": [
      "rebuild = reset --hard main + re-merge awaiting-UAT features",
      "UAT-passed features drop out automatically",
      "one conflict never blocks the remaining features"
    ]
  },
  "decisions": [
    "staging-lane D0a"
  ],
  "deps": [
    "sl-1"
  ],
  "status": "capped",
  "trace": {
    "worker": "sl-w2",
    "outcome": null,
    "files_changed": [
      "packages/bee-rs/crates/bee/src/verbs/staging/mod.rs",
      "packages/bee-rs/crates/bee/src/generated/registry_payload.json",
      "packages/bee-rs/crates/bee/src/catalog.rs"
    ],
    "deviations": [
      "catalog.rs (not in cell.files) needed a PINNED_FLAG_COUNT bump for the new --without flag to keep the pinned flag-vocabulary test green — a mechanical, test-forced follow-on of the declared change."
    ],
    "friction": null,
    "capped_at": "2026-08-17T11:01:21.264Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "f5668bee-17cf-4109-aed8-0b530e26f636",
    "claimed_at": "2026-08-17T10:47:36.445Z",
    "inline_reason": "worker sl-w2 dispatched directly by orchestrator prompt, not pre-registered in state.json workers[]",
    "warnings": [
      "advisory: this cap's commit changes 592 line(s) (over the 150-line finish.advisory_untested_lines threshold) but touches no test-shaped path — consider adding test coverage."
    ],
    "tests": "green",
    "results": ".bee/logs/test-results.json",
    "ran_at": "2026-08-17T11:01:02.062Z"
  }
}
