{
  "id": "da-3",
  "feature": "decision-attribution",
  "lane": "standard",
  "role": "code",
  "title": "Add a narrow reattribute verb and correct the mis-stamped decisions",
  "action": "Add `bee decisions reattribute` (decision-attribution D5) to correct decisions whose feature stamp contradicts their own text. PREDICATE, deliberately narrow: act only where the record's `decision` text opens with `<slug> D<n>` (the convention the 23 provable cases all follow) AND that slug differs from the record's stamped `feature`. Anything else is left alone - the verb corrects a contradiction the record itself carries, it NEVER invents an attribution. Writes ONLY the `feature` field; never decision, rationale, alternatives, date, id, tags, relation, supersedes or touches. Offer --dry-run and report scanned/would-change/changed counts plus each id with its old and new value. Idempotent: a second run reports zero. Hold the decisions lock across the WHOLE pass and re-read each record inside the lock before writing it - cells backfill-roles shipped a scan-outside-the-lock that silently reversed a concurrent write, and that is a live risk here because sibling sessions append to decisions.jsonl continuously. Serve the verb in decisions try_native AND declare it in generated/registry_payload.json in the same change; registry_dispatch enforces both directions. Edit that payload as DATA, it is one line of JSON. Finally run it for real against the live store and report the counts in the cap.",
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/decisions/verbs_read.rs",
    "packages/bee-rs/crates/bee/src/verbs/decisions/tests.rs",
    "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
  ],
  "must_haves": {
    "truths": [
      "A record whose text opens with another feature's `<slug> D<n>` and whose stamp differs is corrected to that slug",
      "A record whose text names its OWN stamped feature is left untouched",
      "A record with no `<slug> D<n>` prefix is left untouched whatever its stamp says",
      "Only the feature field changes - every other field on a corrected record is byte-identical",
      "--dry-run reports the same counts and writes nothing",
      "A second run reports zero changes",
      "The scan and the writes both happen under the decisions lock, with each record re-read inside the lock",
      "The verb is both served and declared, and registry_dispatch stays green"
    ]
  },
  "verify": "cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml",
  "behavior_change": true,
  "decisions": [
    "decision-attribution D5"
  ],
  "affects_skills": [],
  "affects_specs": [],
  "deps": [
    "da-1"
  ],
  "status": "capped",
  "read_first": [],
  "trace": {
    "worker": "main-inline",
    "outcome": "bee decisions reattribute lands and corrects 67 contradicted feature stamps on the live store; a second run reports 0",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/verbs/decisions/verbs_write.rs",
      "packages/bee-rs/crates/bee/src/verbs/decisions/verbs_read.rs",
      "packages/bee-rs/crates/bee/src/verbs/decisions/tests.rs",
      "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
    ],
    "deviations": [
      "Executed inline instead of via a dispatched execution worker; reason recorded as trace.inline_reason.",
      "Corrected 67 records rather than the 23 named in D5 — same predicate and same mechanism, a wider blast radius than the first sample showed."
    ],
    "friction": null,
    "capped_at": "2026-08-25T13:31:23.909Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "c44aee6b-32e8-4e45-a218-0b53c6637f4d",
    "claimed_at": "2026-08-25T13:25:33.039Z",
    "inline_reason": "Same named deviation as da-1 and da-2: this session does not spawn subagents unless the user asks, and the orchestrator held the diagnosis, the predicate design and the lock-ordering lesson already.",
    "report": {
      "outcome": "decisions reattribute added, served and declared. Applied to the live store: 2358 scanned, 67 corrected, 0 on re-run. Proven by diff that exactly 67 lines changed and feature is the only field differing on any of them. 67 not 23 — the earlier figure sampled only the model-role-split stamp. human-mailbox recovered all 18 of its decisions; 0 records now contradict their own text. Residual filed as P3: 5 prompt-work-record decisions make no D-number claim so the predicate correctly declined them.",
      "commit": "a3c6cbdc",
      "files": [
        "packages/bee-rs/crates/bee/src/verbs/decisions/verbs_write.rs",
        "packages/bee-rs/crates/bee/src/verbs/decisions/verbs_read.rs",
        "packages/bee-rs/crates/bee/src/verbs/decisions/tests.rs",
        "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
      ],
      "tests": "cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml — green (2981 passed, 2 failed) — the 2 are the opencode contract tests needing node on PATH, environmental and unchanged; plus a live idempotence run on the real 2358-record store",
      "deviations": [
        "Executed inline instead of via a dispatched execution worker; reason recorded as trace.inline_reason.",
        "Corrected 67 records rather than the 23 named in D5 — same predicate and same mechanism, a wider blast radius than the first sample showed."
      ]
    },
    "warnings": [],
    "tests": "boundary"
  }
}
