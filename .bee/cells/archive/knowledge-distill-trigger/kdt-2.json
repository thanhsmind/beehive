{
  "id": "kdt-2",
  "feature": "knowledge-distill-trigger",
  "lane": "standard",
  "tier": "generation",
  "title": "Deferred-decision trigger registry: .bee/triggers store, triggers add/list/resolve, orient blocker line",
  "behavior_change": true,
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/triggers/mod.rs",
    "packages/bee-rs/crates/bee/src/verbs/mod.rs",
    "packages/bee-rs/crates/bee/src/verbs/status_full/orient.rs",
    "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
  ],
  "read_first": [
    "docs/history/knowledge-distill-trigger/CONTEXT.md",
    "docs/history/knowledge-distill-trigger/plan.md",
    "packages/bee-rs/crates/bee/src/verbs/cells/handlers_write.rs"
  ],
  "action": "Per plan v2 C2 (D2). New verbs/triggers module: store one JSON file per trigger at the CONTROL root .bee/triggers/<slug>__<short8>.json (control-root resolution like no_route_claim_counts_dir, handlers_write.rs:815-870; atomic write, fail-open read — a worktree session must hit the shared store, never a per-worktree copy). Record fields: id, decision (short8 of the deferring decision), condition (prose), tier (predicate|manual), optional predicate (path-exists:<p> | path-missing:<p>), status (waiting|due|resolved), created/updated dates, outcome (set on resolve). Verbs (wire into the dispatch table + registry_payload.json so registry_contracts.rs stays green): triggers add --decision <id> --condition <text> [--predicate <spec>] (tier derived: predicate present => predicate, else manual); triggers list [--due] [--json] evaluating predicates on read — a true predicate flips waiting->due and persists (write-on-read precedent: sweep_on_orient, orient.rs:260-289); triggers resolve --id <id> --outcome <text> writes outcome+status into the record ONLY — it never logs a decision. Corrupt/unreadable trigger file: fail open to a visible line 'unreadable trigger <file> — remedy: delete the file', never a crash, never silently skipped. Orient surfacing: one blocker line 'N trigger(s) due, M awaiting confirmation' via an Option<String> helper in the blocker sequence (orient.rs:322-388, template capture_queue_blocker_line orient.rs:174-189), counting due + manual-waiting. Tests (module tests): add/list/resolve round-trip; predicate flip persists across reads; corrupt file yields the remedy line; orient line appears only when due or manual-waiting exists; store path resolves to control root when called from a linked worktree.",
  "verify": "PATH=\"$HOME/.cargo/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml",
  "must_haves": {
    "truths": [
      "a trigger with predicate path-exists flips to due exactly when the path exists, and the flip is persisted",
      "manual-tier triggers never auto-fire; they surface as awaiting confirmation",
      "resolve writes outcome into the record and logs no decision",
      "a corrupt trigger file produces the delete-remedy line and never a crash",
      "registry_contracts.rs is green with the new verbs registered"
    ]
  },
  "status": "capped",
  "deps": [],
  "decisions": [],
  "trace": {
    "worker": "orchestrator",
    "outcome": "New verbs/triggers module: control-root .bee/triggers store (add/list/resolve), predicate evaluation on read with persisted flip, manual tier never auto-fires, resolve writes outcome only, corrupt-file fail-open remedy line, orient blocker line. Wired into dispatch + registry_payload.json; catalog.rs pinned flag count bumped 150->153 with checked-first reuse analysis.",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/verbs/triggers/mod.rs",
      "packages/bee-rs/crates/bee/src/verbs/mod.rs",
      "packages/bee-rs/crates/bee/src/verbs/status_full/orient.rs",
      "packages/bee-rs/crates/bee/src/generated/registry_payload.json",
      "packages/bee-rs/crates/bee/src/catalog.rs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-08-16T09:48:21.117Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "3ffdb508-af83-4ed1-b5ce-2d68f4e6f780",
    "claimed_at": "2026-08-16T09:26:23.946Z",
    "inline_reason": "cell was claimed under trace.worker=orchestrator by the dispatching session before delegating to execution worker kdt2-worker (now registered in state.json workers[]); kdt2-worker performed the implementation, tests, and this cap",
    "warnings": [
      "advisory: this cap's commit changes 750 line(s) (over the 150-line finish.advisory_untested_lines threshold) but touches no test-shaped path — consider adding test coverage."
    ],
    "tests": "green",
    "results": ".bee/logs/test-results.json",
    "ran_at": "2026-08-16T09:48:01.793Z",
    "semantic_judge": [
      {
        "schema": "judge-verdict/1",
        "verdict": "PASS",
        "checks": [
          {
            "id": "predicate-flip-persists",
            "status": "PASS",
            "evidence": "packages/bee-rs/crates/bee/src/verbs/triggers/mod.rs:245-252 — a predicate-tier record still `waiting` whose predicate_true() holds is set to `due`, updated_at refreshed, and written back with write_json_atomic on the read itself. predicate_true (mod.rs:143-159) handles path-exists:/path-missing: and resolves relative paths against the control root. Ran prebuilt test binary `bee-dbbd56077b8a9375 triggers::`: verbs::triggers::tests::predicate_flip_persists_across_reads ... ok (asserts waiting before the file lands, due after, and due again on a fresh read_json with no re-evaluation). 9 passed, 0 failed."
          },
          {
            "id": "manual-never-auto-fires",
            "status": "PASS",
            "evidence": "mod.rs:245 gates the flip on `rec.tier == \"predicate\"`; due_and_manual_counts (mod.rs:290-303) counts manual+waiting into the awaiting-confirmation bucket only, and `triggers list` surfaces manual/waiting via `surfaced` at mod.rs:438. Ran: verbs::triggers::tests::manual_tier_never_auto_fires_even_with_a_predicate_shaped_condition ... ok (two consecutive reads both yield (due,manual)=(0,1)). Live: `.bee/bin/bee triggers list --json` shows the two real records with tier manual / status waiting, none flipped."
          },
          {
            "id": "resolve-writes-outcome-only",
            "status": "PASS",
            "evidence": "run_resolve (mod.rs:458-496) reads the one trigger file, sets status=resolved + outcome + updated_at and write_json_atomic's it back — nothing else. `rg -n \"decisions\" packages/bee-rs/crates/bee/src/verbs/triggers/mod.rs` returns only comment lines (5, 269, 275) and test lines (580, 592); no decisions-store call exists in the module. Test verbs::triggers::tests::add_list_resolve_round_trip ... ok asserts status/outcome round-trip and `!tmp.join(\".bee/decisions.jsonl\").exists()`. Note: that test exercises the record round-trip via write_json_atomic rather than calling run_resolve itself; the no-decision guarantee rests on the module-wide grep above."
          },
          {
            "id": "corrupt-file-remedy-no-crash",
            "status": "PASS",
            "evidence": "read_and_evaluate maps ReadJson::Corrupt and shape-invalid records to TriggerEntry::Unreadable (mod.rs:241-243), surfaced as `unreadable trigger <path> — remedy: delete the file` (mod.rs:213-215, emitted by triggers list at mod.rs:433-435 and by resolve at mod.rs:480/483). Ran: verbs::triggers::tests::corrupt_file_yields_the_delete_remedy_line_never_a_crash ... ok and a_shape_valid_json_file_missing_a_required_field_is_also_unreadable ... ok; due_and_manual_counts stays (0,0) instead of panicking."
          },
          {
            "id": "registry-contracts-green",
            "status": "PASS",
            "evidence": "Ran the prebuilt integration binary packages/bee-rs/target/release/deps/registry_contracts-e0c9a571bc31d5c1 (built 2026-08-16 17:32, after the 17:10 kdt-3 commit) against the live payload: 7 tests, `test result: ok. 7 passed; 0 failed` (includes every_example_is_spelled_against_a_declared_command and known_flags_parser_would_catch_a_new_handler_flag). registry_payload.json carries commands 83/84/85 = triggers.add / triggers.list / triggers.resolve with descriptions and one example each; `.bee/bin/bee triggers add --help` and `triggers list --json` both answer natively, so the dispatch wiring is live."
          }
        ],
        "failure_signature": null,
        "fixability": "automatic",
        "confidence": "high",
        "builder_model": null,
        "judge_model": null,
        "model_independence": "unverified",
        "recorded_at": "2026-08-16T10:45:31.529Z"
      }
    ]
  }
}
