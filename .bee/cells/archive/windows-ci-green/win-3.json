{
  "id": "win-3",
  "feature": "windows-ci-green",
  "title": "Match the new-worktree instruction path to what the product emits",
  "lane": "tiny",
  "role": "test",
  "status": "capped",
  "deps": [],
  "decisions": [
    "D1",
    "D3",
    "00967a93-511b-46bb-8b0e-f06e9bef8623"
  ],
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs"
  ],
  "read_first": [
    "packages/bee-rs/crates/bee/src/verbs/worktree/handlers.rs",
    "packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs"
  ],
  "affects_skills": [],
  "affects_specs": [],
  "action": "In packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs inside `fn enter_and_merge_and_new_carry_session_runtime_and_instruction_with_injected_caller`, section 3 (new_worktree_transition_result_and_text) expects `/move {target_str}`, where target_str is canonical. The product builds that instruction from `p(&created.worktree_root)` (handlers.rs, new_worktree_transition_result_and_text), the raw path, so Windows run 35454713330 failed at line 6535 with RUNNER~1 vs runneradmin. Build that one expectation from `p(&created.worktree_root)`. Leave section 1 and section 4 on target_str: both go through enter_worktree_core, which emits the canonical path. No product change.",
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml -p bee --bin bee",
  "must_haves": {
    "truths": [
      "The new-worktree expectation matches the raw path the product emits",
      "The enter expectations stay canonical",
      "The full bin suite stays green on Linux"
    ],
    "artifacts": [
      {
        "path": "packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs",
        "substantive": "section 3 expectation built from p(&created.worktree_root)"
      }
    ],
    "key_links": [
      "each expectation mirrors the path builder of the product function it asserts"
    ],
    "prohibitions": [
      "No product source change",
      "No weakening of an assertion condition"
    ]
  },
  "trace": {
    "worker": "leader",
    "outcome": "new-worktree instruction expectation matches the raw path the product emits",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs"
    ],
    "deviations": [
      "tiny cell run inline by the leader, no worker — one-line test edit, which the tiny lane allows — found a better route"
    ],
    "friction": null,
    "capped_at": "2026-09-19T16:31:32.589Z",
    "behavior_change": false,
    "verification_evidence": "the cell verify; 3689 passed 0 failed on Linux, Windows proof comes from the branch CI run",
    "verify_output": "green:unit",
    "verify_passed": true,
    "claim_session": "9cdec5eb-2d88-4223-af6c-35da0848c665",
    "verify_command": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml -p bee --bin bee",
    "claimed_at": "2026-09-19T16:30:30.313Z",
    "mistakes": [
      {
        "wrong": "win-2 traced only the failing assertion at worktree/tests.rs:6517 and missed the raw path at :6535",
        "better": "trace every later assertion that shares the expected value"
      }
    ],
    "report": {
      "outcome": "new-worktree instruction expectation built from p(&created.worktree_root); enter expectations stay canonical",
      "commit": "a0e992da2",
      "files": [
        "packages/bee-rs/crates/bee/src/verbs/worktree/tests.rs"
      ],
      "tests": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --no-fail-fast --manifest-path packages/bee-rs/Cargo.toml -p bee --bin bee — green:unit — the cell verify; 3689 passed 0 failed on Linux, Windows proof comes from the branch CI run",
      "deviations": [
        "tiny cell run inline by the leader, no worker — one-line test edit, which the tiny lane allows — found a better route"
      ],
      "mistakes": [
        "win-2 traced only the failing assertion at worktree/tests.rs:6517 and missed the raw path at :6535 — trace every later assertion that shares the expected value"
      ]
    },
    "warnings": [],
    "tests": "boundary"
  }
}
