{
  "id": "tht-1",
  "feature": "tmux-herding-transport",
  "title": "Read herding.transport and give both transport probes a tmux arm",
  "lane": "standard",
  "status": "capped",
  "deps": [],
  "decisions": [
    "D1"
  ],
  "files": [
    "packages/bee-rs/crates/bee/src/herding.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs"
  ],
  "read_first": [
    "docs/history/tmux-herding-transport/CONTEXT.md",
    "packages/bee-rs/crates/bee/src/herding.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs"
  ],
  "affects_skills": [],
  "affects_specs": [],
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml -p bee transport_",
  "action": "Per D1, add to herding.rs: `pub(crate) enum TransportKind { Herdr, Tmux }` with `fn as_str(&self) -> &'static str` (\"herdr\"/\"tmux\"), a pure `pub(crate) fn transport_kind(cfg: &Value) -> Result<TransportKind, String>` (key `herding.transport`: absent → Herdr, \"herdr\" → Herdr, \"tmux\" → Tmux, anything else → Err naming exactly the two legal values), and `pub(crate) fn transport_kind_at(main_root: &Path) -> Result<TransportKind, String>` that reads `<main_root>/.bee/config.json` (missing or unparseable file → Ok(Herdr), the same fail-open posture `read_command_template_tokens` at `herding.rs:674` takes) and calls `transport_kind`. Probes: KEEP the two existing functions and their signatures untouched so every current test at `herding.rs:897,907,915,923` and `verbs/drivers/tests.rs:1229-1289` stays byte-for-byte unchanged. Add beside each a kind-taking sibling: `transport_state_for(kind: TransportKind, lookup: impl Fn(&str) -> Option<String>) -> Map<String, Value>` in herding.rs (anchor `pub(crate) fn transport_state_with` at 577 — make the old function delegate to the new one with `TransportKind::Herdr`), and `herding_transport_probe_for(kind: TransportKind, env: &dyn Fn(&str) -> Option<String>) -> (bool, String, Option<String>)` in prepare.rs (anchor `pub(crate) fn herding_transport_probe(` at 553 — same delegation). The Tmux arm: ready iff `TMUX` is non-empty AND `TMUX_PANE` is non-empty; reasons, modelled on the herdr arm of the SAME file (the two files already word theirs differently — mirror each file's own style, not the other file's): ready → `TMUX and TMUX_PANE=<pane> are set`; TMUX set but TMUX_PANE empty/missing → `TMUX is set but TMUX_PANE is empty or not set`; TMUX missing → `TMUX is not set — this session is not inside a tmux pane`; the third tuple slot carries the pane id when present. In herding.rs's map add one key `kind` (the `as_str` value) in BOTH arms — this is an additive key, so `bee herding status --json` gains `transport.kind`; the existing keys keep their values. Wire the two production call sites to the new siblings with the kind from `transport_kind_at`: `transport_state()` at `herding.rs:611` (it has `explicit`/main-root resolution in scope from `status`; resolve the root the way the surrounding function does), and the `herding_transport_probe(&|k| std::env::var(k).ok())` call at `prepare.rs:929` (prepare already holds the main root / cfg it used to build the payload — pass that). On `transport_kind_at` Err, `bee herding status` reports `ready:false` with the Err text as the reason and `bee dispatch prepare` sets `transport_ready:false` with the same reason — never a panic. Tests ride the cell, named with the prefix `transport_` so the verify filter reaches them: both tmux reasons and the ready case, the unknown-value Err text, the absent-key default, and the `kind` key on each arm.",
  "must_haves": {
    "truths": [
      "With herding.transport absent, the herdr arm's existing keys and reason strings are unchanged; transport.kind == \"herdr\"",
      "With herding.transport=tmux, TMUX set and TMUX_PANE=%3, bee herding status --json reports transport.ready true and names %3",
      "With herding.transport=nope, status and dispatch prepare report not ready with a reason naming herdr and tmux"
    ],
    "artifacts": [
      {
        "path": "packages/bee-rs/crates/bee/src/herding.rs",
        "substantive": "TransportKind, transport_kind, transport_kind_at, transport_state_for with tests"
      },
      {
        "path": "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs",
        "substantive": "herding_transport_probe_for with a tmux arm, wired at the payload site"
      }
    ],
    "key_links": [
      "prepare.rs:929 call site passes the kind from transport_kind_at",
      "herding.rs transport_state() passes the kind from transport_kind_at"
    ],
    "prohibitions": [
      "No auto-detect: with the key absent, TMUX vars are never consulted (D1)",
      "Existing probe tests are not edited"
    ]
  },
  "trace": {
    "worker": "w-tht-1",
    "outcome": "herding.transport selects the probe; both probes gained a tmux arm and transport.kind",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/herding.rs",
      "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs",
      "packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs"
    ],
    "deviations": [
      "Added #[allow(dead_code)] to the two kept herdr-only delegators: production now calls the _for siblings, so the bin build warned. Signatures and existing tests untouched.",
      "Pre-existing red at base, untouched by this cell: crates/bee/tests/opencode_plugin_contracts.rs two failures (activity PreToolUse matcher, opencode invalid-tool anchor) — confirmed failing with my changes stashed.",
      "sync-ack: The cell's files list carries no skill path: phase 1 of the plan puts the docs/skill/knowledge sync in its own later cell, and this cell is the config key plus the two probe arms only."
    ],
    "friction": null,
    "capped_at": "2026-08-22T16:47:21.401Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "6462a2f6-395a-468b-a50d-9af405debe2c",
    "claimed_at": "2026-08-22T16:37:02.265Z",
    "sync_ack": "The cell's files list carries no skill path: phase 1 of the plan puts the docs/skill/knowledge sync in its own later cell, and this cell is the config key plus the two probe arms only.",
    "report": {
      "outcome": "herding.transport selects the probe; both probes gained a tmux arm and transport.kind",
      "commit": "3897dad3",
      "files": [
        "packages/bee-rs/crates/bee/src/herding.rs",
        "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs",
        "packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs"
      ],
      "tests": "cargo test --release --manifest-path packages/bee-rs/Cargo.toml -p bee transport_ (10 tests) plus -p bee --bin bee (2451) — green — the transport probes and the whole bee bin unit suite that owns the two touched modules",
      "deviations": [
        "Added #[allow(dead_code)] to the two kept herdr-only delegators: production now calls the _for siblings, so the bin build warned. Signatures and existing tests untouched.",
        "Pre-existing red at base, untouched by this cell: crates/bee/tests/opencode_plugin_contracts.rs two failures (activity PreToolUse matcher, opencode invalid-tool anchor) — confirmed failing with my changes stashed."
      ]
    },
    "warnings": [],
    "tests": "boundary",
    "semantic_judge": [
      {
        "schema": "judge-verdict/1",
        "verdict": "PASS",
        "checks": [
          {
            "id": "truth-1-absent-key-herdr-arm-unchanged-plus-kind",
            "name": "With herding.transport absent the herdr keys and reason strings are unchanged and transport.kind is herdr",
            "status": "PASS",
            "evidence": "herding.rs:117-144 the herdr match arms carry the pre-tmux string literals verbatim; herding.rs:171-178 keeps ready, reason, pane_id and then appends kind. Live run of bee herding status json against a temp main-root with an empty config, TMUX and TMUX_PANE=%3 exported: transport ready true, reason HERDR_ENV=1 and HERDR_PANE_ID=w4:p5 are set, pane_id w4:p5, kind herdr. Unit herding::tests::transport_state_carries_the_kind_on_the_herdr_arm_too pins key order ready, reason, pane_id, kind."
          },
          {
            "id": "truth-2-tmux-arm-ready-names-the-pane",
            "name": "With herding.transport tmux and both vars set the status is ready and names the pane",
            "status": "PASS",
            "evidence": "Live run with config herding.transport=tmux and TMUX_PANE=%3: transport ready true, reason TMUX and TMUX_PANE=%3 are set, pane_id %3, kind tmux. Units herding::tests::transport_state_for_tmux_reads_only_the_tmux_vars and verbs::drivers::tests::transport_probe_tmux_arm_reports_ready_when_both_vars_set."
          },
          {
            "id": "truth-3-illegal-value-not-ready-in-both-surfaces",
            "name": "An illegal value surfaces as not ready in status and in dispatch prepare with a reason naming both legal values",
            "status": "PASS",
            "evidence": "Live run with herding.transport=nope: transport ready false, reason herding.transport is nope - the only legal values are herdr and tmux, pane_id null, kind null, exit 0, no panic; arm at herding.rs:193-203. prepare.rs:494-497 maps the same Err to (false, reason, None); unit verbs::drivers::tests::transport_payload_reports_not_ready_on_an_unknown_transport_value."
          },
          {
            "id": "prohibition-no-auto-detect-d1",
            "name": "No auto-detect: with the key absent the TMUX vars are never consulted (D1)",
            "status": "PASS",
            "evidence": "herding.rs:118-144 the Herdr arm reads only HERDR_ENV and HERDR_PANE_ID; the kind comes from transport_kind_at over config.json, never from the environment. The absent-key live run above had TMUX and TMUX_PANE exported and ignored; unit verbs::drivers::tests::transport_probe_herdr_arm_never_reads_the_tmux_vars."
          },
          {
            "id": "prohibition-existing-probe-tests-not-edited",
            "name": "Existing probe tests are not edited",
            "status": "PASS",
            "evidence": "In commit 3897dad3 both test hunks are pure additions: herding.rs at old line 929 and verbs/drivers/tests.rs at old line 1320 carry zero removed lines; every removal in the commit sits inside the two probe function bodies. herding::tests::transport_state_probe_with_both_env_states and verbs::drivers::tests::herding_transport_probe_reports_ready_when_both_vars_set still pass unedited."
          },
          {
            "id": "key-links-call-sites-pass-the-configured-kind",
            "name": "Both production call sites pass the kind from transport_kind_at",
            "status": "PASS",
            "evidence": "herding.rs:188-194 transport_state(explicit) resolves the main root with resolve_main_root(explicit), the same resolution enable_marker_state uses at herding.rs:519, then transport_kind_at then transport_state_for; prepare.rs:494 matches transport_kind_at(root) into herding_transport_probe_for."
          },
          {
            "id": "proof-verify-command-green",
            "name": "Cell verify command re-run by the judge",
            "status": "PASS",
            "evidence": "cargo test release -p bee filter transport_ printed test result: ok. 20 passed; 0 failed; 2476 filtered out."
          }
        ],
        "failure_signature": null,
        "fixability": "automatic",
        "confidence": "high",
        "builder_model": "opus",
        "judge_model": "opus",
        "model_independence": "same-model",
        "recorded_at": "2026-08-22T17:20:36.194Z"
      }
    ]
  },
  "tier": "generation",
  "role": "code"
}
