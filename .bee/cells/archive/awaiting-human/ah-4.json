{
  "id": "ah-4",
  "feature": "awaiting-human",
  "title": "The agent can actually mark and unmark a wait from the command line",
  "lane": "standard",
  "status": "capped",
  "deps": [
    "ah-2"
  ],
  "decisions": [
    "D1",
    "D2",
    "D3"
  ],
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml",
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/state_group/mod.rs",
    "packages/bee-rs/crates/bee/src/verbs/state_group/waiting_on.rs",
    "packages/bee-rs/crates/bee/src/verbs/state_group/tests.rs",
    "packages/bee-rs/crates/bee/src/generated/registry_payload.json"
  ],
  "read_first": [
    "docs/history/awaiting-human/plan.md",
    "docs/history/awaiting-human/CONTEXT.md",
    "packages/bee-rs/crates/bee/src/verbs/workflow_store/record.rs",
    "packages/bee-rs/crates/bee/src/verbs/state_group/mod.rs"
  ],
  "action": "Close the gap that makes this feature unusable: the waiting mark exists and can end three ways, but NOTHING can set or clear it from outside the crate, so the agent can never actually declare a wait. ah-1 added the store functions and ah-2 added the clear and reap; ah-2's own deviation names this gap explicitly and says wiring it needs files outside its declared set. Read both commits before writing — `git show b306201f` (ah-1: the waiting_on structure, kind/subject/asked_at/session, on the workflow record AND the default state record) and `git show 39facee4` (ah-2: the hook clear, the explicit clear, the dual-condition stale reap). Take the real function names from those diffs, not from this brief; ah-2 reported them as `set_workflow_waiting_on` and `set_default_state_waiting_on`, but verify. Add the agent-facing verb — `bee state waiting-on set` and `bee state waiting-on clear` unless the crate's own naming says otherwise. `set` takes the kind, the subject (the gate name, or the question the agent asked), and resolves the owning session through the same precedence chain the rest of the crate uses (search for `resolve_session_id`); `clear` takes nothing beyond the usual routing flags and is a no-op when no mark is live, per ah-2. Per D3 BOTH must work with no active feature, writing the session-scoped mark on the default record — that is the case the user specifically named, and it is the one most likely to be missed because every other state verb assumes a live feature. Every verb takes `--json`. Follow the crate's existing verb-registration path exactly rather than inventing one: find how a sibling `state` subcommand is declared and routed, and mirror it. If the CLI help surface is generated rather than hand-written, regenerate it in the same cell — a flag that ships without appearing in `bee --help --json` is a known, already-filed defect in this repo and must not be repeated. Prove it end to end, not just at the unit level: set a mark through the CLI with no feature active, read it back, and confirm the run reports awaiting-approval.",
  "must_haves": {
    "truths": [
      "bee state waiting-on set records a mark the store can read back",
      "bee state waiting-on clear removes a live mark and is a no-op when none exists",
      "Both verbs work with no active feature, writing the session-scoped mark on the default record",
      "After a CLI set with no feature active, the run reports awaiting-approval",
      "The new verbs and their flags appear in the generated CLI help surface"
    ],
    "artifacts": [
      {
        "path": "packages/bee-rs/crates/bee/src/verbs/state_group/waiting_on.rs",
        "substantive": "set and clear handlers wired through the crate's real registration path, no TODO stubs"
      },
      {
        "path": "packages/bee-rs/crates/bee/src/verbs/state_group/tests.rs",
        "substantive": "end-to-end tests through the CLI surface including the no-feature case and the help-surface presence"
      }
    ],
    "key_links": [
      "the verbs call ah-1's and ah-2's store functions rather than reimplementing the mark"
    ],
    "prohibitions": [
      "No second implementation of the mark or its clearing",
      "No change to the three clearing paths ah-2 shipped",
      "No change to the mark's shape — ah-1 owns it"
    ]
  },
  "trace": {
    "worker": "bob2",
    "outcome": "Wired bee state waiting-on set/clear onto ah-1/ah-2's existing store functions, with D3 target resolution and projection sync",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/verbs/state_group/mod.rs",
      "packages/bee-rs/crates/bee/src/verbs/state_group/set_gate.rs",
      "packages/bee-rs/crates/bee/src/verbs/state_group/waiting_on.rs",
      "packages/bee-rs/crates/bee/src/verbs/state_group/store.rs",
      "packages/bee-rs/crates/bee/src/verbs/state_group/tests.rs",
      "packages/bee-rs/crates/bee/src/verbs/workflow_store/record.rs",
      "packages/bee-rs/crates/bee/src/generated/registry_payload.json",
      "packages/bee-rs/crates/bee/src/catalog.rs",
      "packages/bee-rs/crates/bee/tests/workflow_verbs.rs"
    ],
    "deviations": [
      "File list corrected: added waiting_on.rs (the new module), catalog.rs (PINNED_FLAG_COUNT bump for --subject), and tests/workflow_verbs.rs (true through-the-binary CLI proof) beyond the cell's guessed set."
    ],
    "friction": null,
    "capped_at": "2026-08-14T13:49:16.652Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "d0cba344-c9f5-4d6a-b084-d10ff11e0560",
    "claimed_at": "2026-08-14T13:24:36.337Z",
    "warnings": [],
    "tests": "green",
    "results": ".bee/logs/test-results.json",
    "ran_at": "2026-08-14T13:48:49.700Z",
    "semantic_judge": [
      {
        "schema": "judge-verdict/1",
        "verdict": "PASS",
        "checks": [
          {
            "id": "truth1-set-records-a-readable-mark",
            "status": "PASS",
            "evidence": "waiting_on.rs:182 calls ah-1's set_workflow_waiting_on and :194 set_default_state_waiting_on. Proven through the BUILT BINARY: tests/workflow_verbs.rs:461 runs `state waiting-on set --kind question --subject 'which approach?' --session-id sess-ah4 --json` and asserts kind/subject/session on the returned record (:475-477)."
          },
          {
            "id": "truth2-clear-removes-and-is-a-noop",
            "status": "PASS",
            "evidence": "workflow_verbs.rs:490 clear -> waiting_on null, :496 status confirms null, then :500 a SECOND clear on an already-idle repo returns exit 0 with waiting_on null (:502) — the no-op case, through the binary. waiting_on.rs:234/:244 delegate to ah-2's clear functions."
          },
          {
            "id": "truth3-both-verbs-work-with-no-feature-active",
            "status": "PASS",
            "evidence": "workflow_verbs.rs:461 is named ..._with_no_feature_active and runs in a repo with no workflow; the D3 fallback chain is unit-pinned separately at state_group/tests.rs:607 (lane -> session binding -> default feature -> none) and :721 (None in a C1 repo with zero workflow records). Refusals also proven: :571 an explicit --lane naming no live workflow, :618 no session resolves, :553 unknown kind with zero mutations."
          },
          {
            "id": "truth4-cli-set-then-run-reports-awaiting-approval",
            "status": "PASS",
            "evidence": "workflow_verbs.rs:474 asserts run_state==\"awaiting-approval\" on the set response and :485 re-reads it through `bee status --json` (:486 subject too) — end to end, no feature active. The feature-scoped twin :506 additionally asserts the workflow record (:530), the rebuilt projection (:535-536) and bee status (:540), then the same three go null after clear."
          },
          {
            "id": "truth5-verbs-and-flags-appear-in-the-generated-help-surface",
            "status": "PASS",
            "evidence": "Judge ran the built binary read-only: packages/bee-rs/target/release/bee --help --all --json lists `state.waiting-on.set` (flags kind, subject, session-id, lane, no-lane, json) and `state.waiting-on.clear` — 150 commands vs 148 at base. Machine-guarded, not just present: tests/registry_dispatch.rs:131 every_declared_command_dispatches_exactly_as_the_registry_says_it_does walks every payload example, and both new commands carry examples."
          },
          {
            "id": "registry-payload-and-pinned-flag-count-are-consistent",
            "status": "PASS",
            "evidence": "Judge diffed the payload JSON structurally (python, base 82eea948 vs HEAD): commands 148 -> 150, new = {state.waiting-on.set, state.waiting-on.clear}, none removed; distinct flag names 147 -> 148, new = ['subject'] exactly, none removed. catalog.rs:328 PINNED_FLAG_COUNT = 148 matches, and the bump is for a genuinely new flag name (kind/session-id/lane/no-lane/json all reuse existing spellings). catalog.rs:69 builds the catalog FROM this payload, so there is no second source to drift from; the file has no generator in-tree (scripts/export_registry_payload.mjs is absent), so the hand-edit was the only available path."
          },
          {
            "id": "prohibition-no-second-implementation-of-the-mark",
            "status": "PASS",
            "evidence": "waiting_on.rs contains no mark construction of its own — rg shows only calls to set_workflow_waiting_on / set_default_state_waiting_on / clear_workflow_waiting_on / clear_default_state_waiting_on (:182,:194,:234,:244). build_waiting_on is referenced only in a comment (:133)."
          },
          {
            "id": "prohibition-no-change-to-ah-2s-three-clearing-paths-or-the-marks-shape",
            "status": "PASS",
            "evidence": "git show 00c67cb6 on record.rs and state_group/store.rs: the ONLY change in each is deleting the `#[allow(dead_code)] // not yet wired to a CLI verb` line and rewording the doc comment. No behavioral line touched. prompt_context.rs is not in the commit at all."
          },
          {
            "id": "recorded-deviation-is-honest",
            "status": "PASS",
            "evidence": "The file-list correction (waiting_on.rs, catalog.rs, tests/workflow_verbs.rs) matches 00c67cb6 --stat exactly, and its stated reason for putting the CLI proof in tests/workflow_verbs.rs (run_* reads cwd via prelude(), so an in-process unit test would mutate the shared test-runner cwd) is consistent with that file's own header note."
          },
          {
            "id": "verify-reproduced-by-judge",
            "status": "PASS",
            "evidence": "cargo test --release --manifest-path packages/bee-rs/Cargo.toml re-run read-only: 1768 passed, 0 failed, 11 ignored, exit 0; tests/workflow_verbs.rs alone reports 21 passed."
          }
        ],
        "failure_signature": null,
        "fixability": "automatic",
        "confidence": "high",
        "builder_model": null,
        "judge_model": null,
        "model_independence": "unverified",
        "recorded_at": "2026-08-14T14:01:56.632Z"
      }
    ]
  },
  "role": "code"
}
