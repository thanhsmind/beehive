{
  "id": "gc-3",
  "feature": "guard-completion",
  "lane": "standard",
  "change_class": "bugfix",
  "behavior_change": false,
  "title": "Restore two bee-scribing routing rules the thin-body migration dropped",
  "action": "CI-CAUGHT (Windows run 30353155534, reproduced locally): `node packages/bee/tests/test_bundle_mode.mjs` -> 49 passed, 2 failed. (1) 'bee-scribing no longer routes new area truth into docs/specs/ when a bundle exists' — assertion message 'the routing section has a bundle branch and a fallback branch'. (2) 'bee-scribing documents the three-layer gate: normalization, the two refusals, and the chain backstop' — assertion message 'the empty-subject refusal is specified'. Cause: the skill-diet-wave2 migration compressed skills/bee-scribing/SKILL.md and dropped or reworded prose these checks pin — the same census class that bit three times during that wave. IMPORTANT: this suite is NOT in the local boundary set MAIN has been running; CI found it, which is exactly the division of labour, but it means you must read the check bodies rather than guess. FIX: read packages/bee/tests/test_bundle_mode.mjs around lines 340-360 and the second failing check to learn precisely what each asserts, then restore the missing content in skills/bee-scribing/SKILL.md (or its references, if the check accepts a reference and the body only needs the pointer) so both checks pass with their meaning intact. Body budget: bee-scribing is a migrated skill with a recorded budget — stay at or under it, paying for restored text by trimming reassurance in the same body if needed (report the before/after byte size). Never weaken or re-anchor a check to make it pass: these two rules are load-bearing routing law (where new area truth is written when a knowledge bundle exists, and the empty-subject refusal). You MAY run `node packages/bee/tests/test_bundle_mode.mjs`, `node scripts/skill_budget_fence.mjs` and `node scripts/skill_lint.mjs`, and must quote them; run nothing else. GIT SAFETY (shared checkout): commit through your OWN temp index (GIT_INDEX_FILE + read-tree/update-index/write-tree/commit-tree/update-ref); NEVER plain git add, NEVER reset/stash/checkout/restore.",
  "must_haves": {
    "truths": [
      "node packages/bee/tests/test_bundle_mode.mjs => 0 failed (quote the final line).",
      "Both restored rules keep their original meaning; state what each now says and where it lives.",
      "skills/bee-scribing/SKILL.md is within its recorded budget; report before/after bytes.",
      "skill_budget_fence and skill_lint green."
    ],
    "prohibitions": [
      "Do NOT weaken, re-anchor, or delete either check.",
      "Do NOT touch any other skill.",
      "Do NOT run other suites."
    ]
  },
  "verify": "node packages/bee/tests/test_bundle_mode.mjs && node scripts/skill_budget_fence.mjs && node scripts/skill_lint.mjs",
  "read_first": [
    "packages/bee/tests/test_bundle_mode.mjs",
    "skills/bee-scribing/SKILL.md"
  ],
  "files": [
    "skills/bee-scribing/SKILL.md",
    "skills/bee-scribing/references/scribing-reference.md"
  ],
  "deps": [],
  "regen_obligation_ack": "wave-barrier: regen owned by MAIN at feature close (parallel-default D2)",
  "status": "capped",
  "decisions": [],
  "trace": {
    "worker": "exec-gc3",
    "outcome": "Restored bee-scribing routing law dropped by skill-diet-wave2: 2a/2b bundle-vs-no-bundle routing split (bundle branch never routes to docs/specs/<area>.md; no-bundle branch keeps 'One area = one file, forever.' verbatim) and the subject_required/duplicate_authority/duplicate_authoritative_for refusal chain. Paid for restored bytes by trimming reassurance prose in Capture/Merge/Red Flags. Body: 8135 -> 8145 bytes (budget 8151).",
    "files_changed": [
      "skills/bee-scribing/SKILL.md"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-28T11:27:52.882Z",
    "behavior_change": false,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "9343141e-4732-4077-8eef-f2f16317a423",
    "claimed_at": "2026-07-28T11:21:40.464Z",
    "warnings": [],
    "feature_verify": "pending"
  },
  "tier": "generation"
}
