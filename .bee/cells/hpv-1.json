{
  "id": "hpv-1",
  "feature": "herding-prompt-verify",
  "lane": "tiny",
  "tier": "generation",
  "title": "Verify the pointer prompt landed in the pane; resend bounded when it did not",
  "behavior_change": true,
  "files": [
    "packages/bee-rs/crates/bee/src/herding/run.rs"
  ],
  "read_first": [
    "docs/history/herding-prompt-verify/CONTEXT.md",
    "packages/bee-rs/crates/bee/src/herding/run.rs"
  ],
  "action": "Per D1: Herdr trait gains agent_text(job_id) -> Option<String> (real impl: herdr agent read <job_id> --source recent-unwrapped, transcript text; None on any trouble). New deliver_pointer helper: loop up to 5 attempts — agent_prompt(pointer), short injected-clock wait, agent_text contains the brief file name (e.g. brief-1.txt) => delivered; else resend. Exhaustion = typed failure keeping the pane. Wire into execute_new and execute_continue in place of the bare agent_prompt calls. Update FakeHerdr (agent_text serves a script: N empty reads then the pointer) and PanicHerdr. Tests: delivered on first try sends once; two empty reads then visible => exactly 3 sends... (assert resend count matches script); exhaustion keeps pane and names delivery in the message.",
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml herding::run",
  "must_haves": {
    "truths": [
      "A pointer invisible in the pane is resent, bounded at 5 attempts",
      "Delivery exhaustion is a typed failure that keeps the pane",
      "A visible pointer stops the resends"
    ]
  },
  "decisions": [
    "herding-prompt-verify D1"
  ],
  "deps": [],
  "status": "capped",
  "trace": {
    "worker": "w-hpv-1",
    "outcome": "Pointer delivery verified against pane text with bounded resends; silent drop impossible",
    "files_changed": [
      "packages/bee-rs/crates/bee/src/herding/run.rs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-08-20T03:18:43.204Z",
    "behavior_change": true,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": "1adb69d1-67be-4463-8349-af8832af8a32",
    "claimed_at": "2026-08-20T03:14:51.428Z",
    "report": {
      "outcome": "deliver_pointer: send, read pane, resend bounded; typed exhaustion",
      "commit": "head",
      "files": [
        "packages/bee-rs/crates/bee/src/herding/run.rs"
      ],
      "tests": "cargo test --release --manifest-path packages/bee-rs/Cargo.toml --bin bee herding::run — green (36 passed) — touched module, 2 new tests: resend-until-echo, exhaustion",
      "deviations": []
    },
    "warnings": [
      "advisory: this cap's commit changes 190 line(s) (over the 150-line finish.advisory_untested_lines threshold) but touches no test-shaped path — consider adding test coverage."
    ],
    "tests": "boundary"
  }
}
