{
  "id": "hr-2",
  "feature": "herd-registry",
  "lane": "standard",
  "tier": "generation",
  "title": "Tier slots carry the herd name: kind:herding gains agent; prepare appends --agent",
  "behavior_change": true,
  "files": [
    "packages/bee-rs/crates/bee/src/verbs/drivers/models.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/tests.rs",
    "packages/bee-rs/crates/bee/src/hooks/model_guard.rs"
  ],
  "read_first": [
    "docs/history/herd-registry/CONTEXT.md",
    "packages/bee-rs/crates/bee/src/verbs/drivers/models.rs",
    "packages/bee-rs/crates/bee/src/verbs/drivers/prepare.rs"
  ],
  "action": "Per D2: normalize_tier_value keeps a trimmed optional string field agent on the kind:herding shape (empty/whitespace dropped); Resolved::Herding carries agent: Option<String>; resolve_tier passes it through; prepare_dispatch's herding-exec arm appends --agent \"<name>\" to the command when present (quoted, before --json or after — pick one and pin it in the test). model_guard: touch ONLY if the Resolved variant change forces a compile fix — behavior unchanged. Tests: normalize round-trips agent; prepare command string carries --agent exactly when the slot names one; slot without agent emits the unchanged command.",
  "verify": "PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml drivers && PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" cargo test --release --manifest-path packages/bee-rs/Cargo.toml model_guard",
  "must_haves": {
    "truths": [
      "{kind:herding, agent:X} produces a prepare command carrying --agent \"X\"",
      "A herding slot without agent emits the pre-existing command byte-identically"
    ]
  },
  "decisions": [
    "herd-registry D2"
  ],
  "deps": [],
  "status": "open",
  "trace": {
    "worker": null,
    "outcome": null,
    "files_changed": [],
    "deviations": [],
    "friction": null,
    "capped_at": null,
    "behavior_change": false,
    "verification_evidence": null,
    "verify_output": null,
    "verify_passed": null,
    "claim_session": null
  }
}
