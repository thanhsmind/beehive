===== CASE onboarding-ok =====
## bee v<BEE_VERSION>
- Onboarding: ok (bee <BEE_VERSION>)
- Phase: swarming | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved

### Standard commands (host project)
- setup: `npm ci`
- test: `npm test`
- verify: `node scripts/run_verify.mjs`
- CI status gate: before your first `cells claim` of this session, check CI instead of running anything locally — the latest full-verify run on the base branch plus any open verify-red issue; red is surfaced and becomes its own fix-first tiny cell — never build on red. No local full-suite run is ever owed: the dev loop runs registry-scoped tests only, and the full suite is CI-owned. The claim is the trigger, not arrival: a session that claims no cell owes no CI check.

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE onboarding-missing-bypass-total =====
## bee v<BEE_VERSION>
- Onboarding: MISSING — run bee-hive onboarding before anything else.
- Phase: swarming | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved
- ⚡⚡⚡ GATE BYPASS: TOTAL AUTOPILOT — ZERO STOPS. Every gate (any lane, high-risk/hard-gate included), secret-file reads, and review P1 findings auto-proceed; NO human checkpoint remains. Turn off: bee-bypass-gate off
  The agent does NOT stop for these gates — it records the recommended choice, logs a one-line audit decision, and continues. This includes secret-file reads and review P1 findings: nothing pauses for the human.

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE onboarding-drift-bypass-normal =====
## bee v<BEE_VERSION>
- Onboarding: installed at bee 0.0.1 but plugin is <BEE_VERSION> — re-run onboarding to refresh vendored helpers.
- Phase: reviewing | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved | review: pending
- ⚡ GATE BYPASS: NORMAL — Gates 1-3 auto-approved for tiny/small/standard work only; high-risk/hard-gate, secret reads, and Gate 4 UAT still stop. Turn off: bee-bypass-gate off

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE handoff-pause =====
## bee v<BEE_VERSION>
- Onboarding: ok (bee <BEE_VERSION>)
- Phase: swarming | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved

### HANDOFF present — present it and WAIT — never auto-resume
- Phase: swarming | Feature: demo | Mode: standard
- Cells in flight: k-1, k-2
- Saved next action: resume k-1

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE handoff-planned-next-refused =====
## bee v<BEE_VERSION>
- Onboarding: ok (bee <BEE_VERSION>)
- Phase: swarming | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved

### HANDOFF present — present it and WAIT — never auto-resume
- Phase: swarming | Feature: demo | Mode: standard
- Cells in flight: k-1
- Saved next action: start k-2
- Adoption not applied: a planned-next handoff never auto-adopts on source "compact" — only "clear"/"startup" qualify (D1).

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE handoff-adopted =====
## bee v<BEE_VERSION>
- Onboarding: ok (bee <BEE_VERSION>)
- Phase: swarming | Mode: standard | Feature: demo
- Gates: context: approved | shape: approved | execution: approved

### PLANNED-NEXT ADOPTED — starting now, no confirmation needed (D1)
- Cell: k-9 — Cell k-9
- Lane: small
- Verify: `node scripts/test_thing.mjs`

- No knowledge work item for "demo" — offer to author docs/knowledge/work/demo/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
===== CASE lane-bound =====
## bee v<BEE_VERSION>
- Onboarding: ok (bee <BEE_VERSION>)
- Phase: executing | Mode: small | Feature: lane-feat
- Gates: context: approved | shape: approved | execution: approved
- 1 other active lane(s): other-lane

- No knowledge work item for "lane-feat" — offer to author docs/knowledge/work/lane-feat/work-item.md (template: docs/knowledge/areas/okf-profile/concept-model-and-authoring.md, Templates) so the next session starts from curated context.

### Project map
- Project map missing (Q1/Q2 unanswerable from repo) — bee-scribing bootstrap available.

### Critical patterns (digest)
PAT01: never build on red.
PAT02: the trace is the record.

### Recent decisions
- «The golden is committed, never reconstructed» (2026-01-02)

Everything above is already read — do not re-fetch it. Run `node .bee/bin/bee.mjs status --json` (and `decisions active`) yourself when you are about to ROUTE WORK — claim, plan, change phase — or need detail this block does not carry (agent-run — never hand bee commands to the user). Route via bee-hive.
