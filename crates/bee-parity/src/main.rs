//! bee-parity: the D7a CLI parity harness (same command, same fixture
//! store, mjs vs rust stdout/exit/side-effect files diffed).
//!
//! Slice 0 scope: stub. The mjs-vs-mjs self-parity baseline runner lands in
//! the parity-harness cell of this slice.

fn main() {
    println!("bee-parity: stub (Slice 0 workspace skeleton)");
}
