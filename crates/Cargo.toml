[workspace]
resolver = "2"
members = [
    "queen-bee",
    "bee-core",
    "queen-bench",
    "bee-parity",
]

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
